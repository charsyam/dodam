# Dodam TODO

## Done

### Core Execution

- Built a vectorized Arrow `RecordBatch` execution path for local Parquet scan, filter, projection, limit, sort, distinct, aggregation, and join.
- Added local Parquet file and directory planning with stable fragment order, schema/statistics discovery, metadata cache, projection pushdown, and row-group pruning.
- Added global and grouped aggregation for `COUNT(*)`, `COUNT(col)`, `SUM`, `AVG`, `MIN`, and `MAX`.
- Added faster grouped aggregation paths for common numeric and string keys.
- Added `INNER`, `LEFT OUTER`, `RIGHT OUTER`, `FULL OUTER`, and `LEFT SEMI` joins.
- Added hash join, sort-merge join, partitioned spill hash join, and chunked file-hash join execution.
- Added multi-column equality joins, build-side selection from estimates, partition count calculation, spill repartitioning, heavy-hitter separation, and join metrics.
- Added join memory safeguards: recursive repartitioning, bounded output chunks, bloom-style filtering metrics, skew handling, and nested-loop fallback for pathological spill partitions.
- Added output projection pushdown into hash join and partitioned/spill hash join where semantics allow it.
- Added SQL aggregate execution over streamed join output batches, including global aggregate, grouped aggregate, `HAVING`, aggregate aliases, `ORDER BY`, and `LIMIT`.
- Added dense `Int32` unique join key fast path and `Int64` single-key fast paths.
- Added dense `Int32` bitmap matched-build tracking for outer joins.

### SQL, CLI, COPY

- Added SQL parsing through `sqlparser`.
- Supported projection, column aliases, table aliases, qualified columns, filters, grouping, `HAVING`, `ORDER BY`, `LIMIT`, `DISTINCT`, joins, and `EXPLAIN`.
- Added streaming SQL execution for simple joins to avoid collecting all join output before CLI output.
- Added Dodam `COPY (SELECT ...) TO 'file' (FORMAT CSV)` for fair DuckDB-style benchmark comparison.
- Added Dodam `COPY (SELECT ...) TO 'file' (FORMAT PARQUET)`.
- Added configurable Parquet COPY options:
  - `COMPRESSION SNAPPY|UNCOMPRESSED`
  - `DICTIONARY true|false`
  - `ROW_GROUP_SIZE <rows>`
  - `WRITE_BATCH_SIZE <rows>`
  - `DATA_PAGE_ROW_COUNT_LIMIT <rows>`
- Added execution sink integration: `RecordBatchSink`, stream-to-sink drain helper, `PhysicalPlan::execute_to_sink`, and engine-level sink execution helpers.
- Routed CLI COPY and streaming SELECT through the engine sink boundary.
- Added `HashJoinExec::execute_to_sink` and SQL-level `try_execute_sql_to_sink`.
- Added configurable COPY output buffer:
  - CLI: `--copy-buffer-size <bytes>`
  - env: `DODAM_COPY_BUFFER_BYTES`
  - default: `8 MiB`
- Replaced `arrow-csv` writer path with a custom CSV sink using batch-level `Vec<u8>`, `BufWriter<File>`, `itoa`, and `ryu`.
- Added CSV serialization hot paths for single `Int32` and `Int32, Utf8` output shapes.
- Guarded direct sink/streaming join output so outer joins with SELECT-specific projection fall back to the correct materialized projection path.

### Storage And Catalog Layering

- Split major layers into catalog, storage, engine, logical execution, physical execution, optimizer, cost model, and SQL.
- Added `TableScanSource`, `StorageLocation`, `StorageFormat`, `ObjectStore`, and format-dispatching `ScanExec`.
- Added local filesystem object-store abstraction and Parquet table/source planning.
- Kept S3/object storage on hold until the local OLAP engine and execution model are stronger.
- Added a persistent local workspace catalog under `.dodam/catalog.json`.
- Added `dodam catalog register <name> <path>` and `dodam catalog list`.
- Added SQL resolution for registered table names such as `SELECT * FROM orders` while preserving direct path queries.
- Added catalog registration snapshots for schema fields, table statistics, and discovered partition columns.
- Added catalog registration snapshots for file fragments, fragment statistics, and fragment partition values.
- Changed registered table scans to use the catalog fragment snapshot instead of recursively rediscovering the directory on every query.
- Added `dodam catalog refresh <name>` to rebuild a registered table snapshot from its stored location.
- Added Hive-style directory partition discovery on file fragments.
- Added directory partition pruning for positive `=` and `IN` predicates before Parquet row-group planning.
- Added scan-time materialization of Hive partition columns so they can be selected, ordered, filtered after pruning when needed, and grouped.
- Added Parquet scan compatibility coverage for nullable and richer Arrow types:
  - nullable `Boolean`, `Float64`, and `Utf8`
  - `Decimal128(10, 2)`
  - `Timestamp(Millisecond)`
  - `List<Int32>`
  - `Struct<rank: Int32, label: Utf8>`
- Added filter execution support and SQL coverage for richer scalar Parquet types:
  - boolean literals and boolean comparison
  - decimal comparison against numeric/string literals
  - timestamp comparison against `YYYY-MM-DD` and `YYYY-MM-DD HH:MM:SS` string literals
  - timezone-aware timestamp comparison using the same epoch semantics as Arrow
  - `Date32`/`Date64` comparison against `YYYY-MM-DD` string literals
- Added Parquet row-group pruning for richer scalar statistics:
  - boolean min/max statistics
  - `Float64` min/max statistics
  - `Date32`/`Date64` statistics
  - decimal statistics stored as `Int32`, `Int64`, `ByteArray`, or `FixedLenByteArray`
  - timezone-aware and timezone-less timestamp statistics stored as `Int64`

### Correctness And Compatibility

- Added a DuckDB differential test harness that skips cleanly when the `duckdb` CLI is unavailable.
- Added initial DuckDB differential coverage for:
  - scan/filter/null predicates, including `IS NULL`, `IS NOT NULL`, `NOT`, and `IN`
  - global and grouped aggregate matrix over count/sum/avg/min/max
  - inner/left/right/full/semi joins with duplicate build-side keys and unmatched rows
  - multi-key joins that combine integer and string keys
  - deterministic `ORDER BY` + `LIMIT`
  - scalar type filters over boolean, float, decimal, timestamp, and date columns
  - derived-table subqueries in `FROM`
  - uncorrelated `IN (SELECT ...)` / `NOT IN (SELECT ...)` subqueries
  - uncorrelated scalar subquery filters
  - uncorrelated top-level `EXISTS` / `NOT EXISTS` filters
- Expanded DuckDB differential coverage for NULL semantics:
  - `IN` / `NOT IN` with literal `NULL` inside `OR`
  - scalar subquery `NULL` comparisons
  - left join behavior for NULL join keys
- Expanded type compatibility coverage and fixes:
  - decimal literals with trailing fractional zeros beyond column scale
  - timezone offset timestamp literals such as `+09:00`, normalized to epoch semantics
  - matching filter and row-group pruning interpretation for decimal/timestamp literals
  - SQL projection coverage for nested `List` and `Struct` columns
- Added first-stage SQL scalar projection expressions for single-table non-aggregate SELECT queries:
  - numeric arithmetic with `+`, `-`, `*`, `/`
  - `CAST(... AS VARCHAR/INT/DOUBLE)`
  - `COALESCE(...)`
  - DuckDB differential coverage for arithmetic, cast, and coalesce projection output
- Extended the same scalar expression evaluator to single-table non-aggregate `WHERE` predicates:
  - arithmetic/cast/coalesce comparisons
  - expression `IS NULL` / `IS NOT NULL`
  - DuckDB differential coverage for computed `WHERE` output
- Added first-stage string functions and searched `CASE` support in scalar expressions:
  - `lower`, `upper`, and `length` in projection and computed `WHERE`
  - `CASE WHEN ... THEN ... ELSE ... END` for searched CASE expressions
  - DuckDB differential coverage for string functions and CASE output
- Added a TPC-H-lite DuckDB differential suite over lineitem/orders/customer-shaped Parquet fixtures:
  - Q1-style date-filtered grouped aggregate with multi-column `GROUP BY`
  - Q6-style date/discount/quantity filtered aggregate
  - Q3-style customer/orders join aggregate
- Added a full TPC-H 22-query support inventory harness:
  - records the current parser/planner support status for each canonical TPC-H query
  - turns unsupported reasons into a regression-tested baseline
  - current dominant blockers are `WITH`, `count(DISTINCT ...)`, and richer nested/correlated subquery shapes
- Improved TPC-H Q1/Q6 coverage:
  - added aggregate input expressions such as `sum(l_extendedprice * l_discount)` by materializing deterministic temporary expression columns before aggregation
  - added constant folding for numeric literal arithmetic in predicates
  - added `DATE 'YYYY-MM-DD' +/- INTERVAL` folding for day/month/year intervals
  - added `BETWEEN` / `NOT BETWEEN` lowering to comparison predicates
  - TPC-H inventory now advances Q1 and Q6 to `needs-data:lineitem`
  - TPC-H-lite Q6 now executes expression aggregate + `BETWEEN` + date interval arithmetic under DuckDB differential comparison
- Added a canonical-shape TPC-H Q6 DuckDB differential fixture:
  - uses ratio-style `l_discount` values such as `0.06`
  - compares `sum(l_extendedprice * l_discount)` with decimal literal bounds `0.06 +/- 0.01`
  - fixed literal folding so numeric `0.06 + 0.01` preserves decimal scale before converting to `Float64`; naive f64 folding produced `0.069999...` and incorrectly excluded the `0.07` boundary row
- Added first-stage SQL `LIKE` / `NOT LIKE` filters for `Utf8` columns:
  - supports `%`, `_`, and single-character `ESCAPE`
  - keeps `NULL LIKE pattern` as SQL unknown/null in filter masks
  - added DuckDB differential coverage for `LIKE`, `NOT LIKE`, `_`, `%`, `ESCAPE`, and null inputs
- Advanced TPC-H Q13 planning:
  - explicit table aliases are no longer required for JOIN inputs; unaliased joins use the table/file stem as the execution prefix
  - unqualified equality columns in JOIN `ON` are accepted left-to-right, covering canonical `c_custkey = o_custkey` style predicates
  - right-side-only residual predicates in `INNER`/`LEFT`/`SEMI` JOIN `ON` clauses are pushed into the right input filter, covering `AND o_comment NOT LIKE ...`
  - unqualified join projection, `GROUP BY`, and aggregate arguments can now be inferred from TPC-H-style column prefixes such as `c_` and `o_`
  - added `UInt64` group key support so outer grouping by `count(...)` aliases such as Q13's `c_count` preserves the count result type
  - TPC-H inventory now advances Q13 to `needs-data:customer`
- Added first-stage SQL `SUBSTRING(expr FROM start FOR length)` scalar expression support:
  - works in projection and computed `WHERE` expression filters, including expression `IN (...)`
  - uses SQL 1-based character indexing and preserves NULL propagation
  - added DuckDB differential coverage for projection and WHERE `IN`
  - TPC-H Q22 advanced past SELECT expression parsing; later expression-filter/subquery work advances it to `needs-data:customer`
- Added first-stage 2-table comma join planning:
  - lowers `FROM a, b WHERE a_key = b_key AND ...` to the existing inner hash join execution path
  - extracts one or more equality join keys from top-level `AND` predicates and keeps the remaining predicates as post-join filters
  - supports explicit aliases and TPC-H-style inferred unqualified columns through the existing join column resolver
  - added SQL coverage comparing comma join output with the existing join path
  - TPC-H inventory now advances Q12/Q14/Q19 from FROM-list parsing to join aggregate expression blockers, while 3+ table queries report the explicit 2-table comma join limitation
- Added correctness-first multi-table comma join planning:
  - supports 3+ table `FROM a, b, c...` inner joins by materializing left-deep hash joins over equality predicates from top-level `AND` filters
  - preserves residual filters, grouped aggregates, aggregate input expressions, `ORDER BY`, `LIMIT`, aliases, and `DISTINCT` over the final joined batches
  - added SQL coverage for a 3-table TPC-H-shaped `customer/orders/lineitem` grouped aggregate
  - TPC-H inventory now advances Q2/Q3/Q5/Q7/Q8/Q9/Q10/Q11/Q18/Q21 past the previous multi-table `FROM` parser/planner blockers to `needs-data:*`
- Added join aggregate input expression support:
  - materializes deterministic temporary expression columns over join output before aggregation, matching the existing single-table aggregate expression path
  - supports arithmetic and searched `CASE` inside join aggregate inputs
  - disables join projection narrowing for aggregate-expression joins to keep correctness simple
  - Q19 now advances to join equality extraction under OR branches
- Added column-to-column comparison filters:
  - supports `column op column` predicates in single-table filters and join residual filters
  - compares Arrow arrays directly and keeps row-group/partition pruning conservative for these predicates
  - added SQL coverage for single-table and comma-join residual column comparisons
  - TPC-H inventory now advances Q12 to `needs-data:orders`
- Added aggregate-output scalar projection for join aggregates:
  - supports expressions such as `100.00 * sum(...) / sum(...)` by evaluating scalar expressions over the aggregate result batch
  - reuses aggregate input expression materialization for nested `sum(l_extendedprice * (1 - l_discount))` and searched `CASE` inputs
  - added scalar predicate `LIKE` support inside expression `CASE` evaluation for join aggregate inputs
  - TPC-H inventory now advances Q14 to `needs-data:lineitem`
- Added comma join key extraction from OR branches:
  - detects equality join keys that are common to every top-level OR branch, then keeps the original OR predicate as the residual filter
  - added `BETWEEN` lowering for join residual filters
  - added SQL coverage for comma joins whose equality predicate appears inside OR branches
  - TPC-H inventory now advances Q19 to `needs-data:lineitem`
- Added expression-filter aggregate and derived projection coverage:
  - supports aggregate queries with scalar expression predicates such as `avg(c_acctbal) WHERE substring(c_phone...) IN (...)`
  - supports scalar expression projection over materialized derived-table rows
  - expression and subquery predicates can be split across top-level `AND` conjuncts so each side uses the appropriate evaluator
  - TPC-H inventory now advances Q22 to `needs-data:customer`
- Added aggregate support to the row-bound correlated subquery fallback:
  - supports grouped aggregate outer queries filtered by correlated `EXISTS` / `NOT EXISTS`
  - binds both explicit alias references and TPC-H-style unqualified outer columns such as `o_orderkey`
  - added SQL coverage for a Q4-shaped grouped aggregate with correlated `EXISTS`
  - TPC-H inventory now advances Q4 to `needs-data:orders`
- Added first-stage `count(DISTINCT column)` aggregate support:
  - supports global and grouped distinct counts through the generic aggregate accumulator
  - keeps fast aggregate paths on non-distinct aggregates and falls back to generic for distinct counts
  - added SQL coverage for grouped `count(DISTINCT id)`
  - TPC-H inventory now advances Q16 to the join residual `NOT IN (subquery)` blocker
  - TPC-H Q17/Q20 also advance past earlier FROM-list routing blockers to their scalar/nested subquery blockers
- The TPC-H-lite suite caught missing `Date32` `min`/`max` aggregate support; added `Date32` aggregate state and `Date32Array` result materialization.
- The TPC-H-lite join aggregate case caught an aggregate join output projection correctness issue around qualified column names when the cost model chooses the left side as the hash build input.
- Fixed build-side-aware join output projection mapping for hash/materialized join output schemas and restored aggregate join output projection pushdown.
- Extended temporal `min`/`max` aggregate support beyond `Date32`:
  - `Date64`
  - `Timestamp(Millisecond)` with timezone metadata preserved in SQL result batches
  - Added SQL tests that validate output Arrow types and epoch values directly; DuckDB CSV differential is intentionally avoided for these output values because DuckDB CLI formats `Date64`/timestamp results differently.
- The first differential grouped-aggregate case caught a real correctness bug: grouped `min(Utf8)` / `max(Utf8)` on the single-key fast path produced `NULL` because empty min/max state defaulted to `Int64(NULL)`.
- Fixed the grouped aggregate fast path so min/max states keep the expected output null type (`Int64`, `Float64`, or `Utf8`) even when a group starts with nulls.
- Added first-stage subquery support for `FROM (SELECT ...) AS alias` by materializing the inner query to in-memory `RecordBatch`es and applying the outer projection/filter/group/order/limit over those batches.
- Added `DISTINCT` support over derived tables.
- Added first-stage derived table join support by materializing join inputs and reusing `HashJoinExec`.
- Added aggregate and `DISTINCT` support over derived table joins.
- Added first-stage uncorrelated `IN (SELECT ...)` support by materializing the subquery to a literal `IN` list.
- Fixed `IN (SELECT ...)` / `NOT IN (SELECT ...)` SQL 3-valued logic when the RHS contains `NULL`.
- Added first-stage uncorrelated scalar subquery filters such as `WHERE id = (SELECT id FROM ...)`.
- Fixed scalar subquery edge semantics:
  - 0 rows returns scalar `NULL`
  - 1 `NULL` row returns scalar `NULL`
  - multiple rows return an error
- Added first-stage uncorrelated top-level `EXISTS` / `NOT EXISTS` filters.
- Added first-stage correlated top-level `EXISTS` / `NOT EXISTS` support for single-table non-aggregate outer queries by binding outer alias references per row.
- Expanded DuckDB differential correctness coverage further:
  - boolean/NULL predicate matrix for `= NULL`, `<> NULL`, `IS NULL`, `IS NOT NULL`, nested `NOT`, and mixed `AND`/`OR`
  - `IN (SELECT ...)` / `NOT IN (SELECT ...)` when the RHS contains `NULL`
  - scalar subquery `NULL` result and multiple-row error behavior
  - full outer join filtering around `NULL` join keys
  - direct projection and filtering of boolean, float, decimal, `Utf8`, and `Date32` values
  - Parquet `COPY` readback by writing Dodam COPY output and comparing the generated Parquet through Dodam and DuckDB
- Closed the previous scalar display compatibility gap:
  - `EvaluatedScalar` now preserves `Decimal128(precision, scale)` through scalar expression projection instead of immediately lowering it to `Float64`.
  - `CAST(Date32/Timestamp/Decimal/Float64 AS VARCHAR)` now has active DuckDB differential coverage, including fixed-scale decimal output and `.0` preservation for whole `Float64` values.
- Expanded typed expression compatibility further:
  - same-scale `Decimal128` comparisons and `+`/`-` scalar expressions now use raw decimal values and preserve decimal scale instead of always falling back to `Float64`.
  - Added typed scalar casts for `CAST(string AS DATE)`, `CAST(string AS TIMESTAMP)`, `CAST(timestamp AS DATE)`, and `CAST(date AS TIMESTAMP)`.
  - Added multi-level struct field projection/filter support such as `attrs.detail.score`.
  - Extended negative error coverage for scalar type mismatches and invalid date/timestamp casts.
  - Extended the correlated `EXISTS` semijoin optimizer rule to support `NOT EXISTS` as an anti-semijoin mask, including aggregate outer queries.
- Moved several TPC-H fast-path building blocks toward reusable optimizer/execution rules:
  - moved sorted `i64` lookup from Q-specific SQL code into a reusable `SortedI64Lookup`
  - moved packed `u32,u32` pair distinct counting into reusable `PackedU32PairDistinct`
  - moved dense `i64` set batch insertion into `AdaptiveI64Set::try_insert_dense_values`
  - rewired Q03/Q12-style sorted lookups, Q16 packed distinct supplier counting, and Q22 dense customer-key set build to use the shared helpers
  - retained query-level routing and env rollbacks while reducing the amount of hard-coded per-query data-structure logic
- Added correctness-first correlated subquery fallback for single-table non-aggregate outer queries:
  - correlated `IN (SELECT ...)`
  - correlated scalar subquery filters
  - `EXISTS` / `NOT EXISTS` inside `AND` / `OR` boolean expressions
- Current subquery limitations:
  - `IN (SELECT ...)` currently requires a single result column
  - correlated subquery support is currently correctness-first and row-bound; it is not optimized for large outer inputs yet
- Expanded the SQL compatibility safety net:
  - Added row-wise list indexing support for expressions such as `tags[array_length(tags)]`, with DuckDB differential coverage for projection and filter use.
  - Generalized JOIN `ORDER BY` resolution so a projected scalar expression alias can also be referenced by the original expression text, such as `ORDER BY COALESCE(...)`.
  - Added DuckDB differential coverage for aggregate queries filtered by scalar aggregate subqueries and `NOT IN (SELECT ...)` with `NULL` RHS semantics.
  - Added Parquet COPY schema fidelity coverage for decimal, date, timestamp, and boolean output, while keeping timestamp display normalization separate from raw schema checks.
  - Added long randomized differential replay knobs: `DODAM_LONG_DIFF_SEED`, `DODAM_LONG_DIFF_SEEDS`, `DODAM_LONG_DIFF_CASES`, and `DODAM_LONG_DIFF_CASE`.
- Added explicit negative/error contract coverage:
  - unknown projection/filter/order columns must return `UnknownColumn`
  - ambiguous unqualified join columns must explain ambiguity
  - unknown join/table qualifiers must be reported clearly
  - scalar subqueries returning multiple rows must fail with a SQL-compatible error
  - invalid COPY Parquet options must return Dodam errors instead of panicking inside the Parquet writer

### Performance Work

- Added DuckDB e2e benchmark coverage for scan/filter, aggregation, grouped aggregation, inner join, left/right/full outer join, and semi join.
- Fixed benchmark fairness so Dodam CLI and DuckDB CLI both use `COPY ... TO '<benchmark-output>.csv' (FORMAT CSV)` and pay CSV file write cost.
- Changed DuckDB e2e benchmarks to use a shared output format option for every COPY case:
  - default: `DODAM_E2E_COPY_FORMAT=PARQUET`
  - optional CSV comparison: `DODAM_E2E_COPY_FORMAT=CSV`
  - Criterion groups are separated as `duckdb_e2e_parquet` and `duckdb_e2e_csv` so CSV and Parquet histories are not compared against each other.
- Fixed benchmark flow so `target/release/dodam` is rebuilt before CLI runs.
- Fixed Dodam e2e benchmark batch size at `16 KiB`.
- Tested direct COPY join output bypassing `RecordBatch` materialization, then removed it from the default path because it was slower.
- Tested dense `Int32` index-driven CSV writing, then removed it because it regressed inner join CLI performance.
- Optimized hash join output materialization:
  - contiguous build row refs use slice instead of `take_record_batch`
  - single build chunk output skips `concat_batches`
  - output schemas are cached per probe batch
- Added COPY profiling with `DODAM_PROFILE_COPY=1`.
- Added `gen_e2e_data` helper binary for benchmark-shaped Parquet data generation.
- Added direct dense `Int32` unique hash build for `FastSingleKey` joins.
- Fixed RIGHT/FULL join build-side selection so the cost model can build the smaller side.
- Expanded DuckDB e2e join benchmarks with narrow fact, wide-pruned fact, and small-row-group duplicate-key datasets to check that scan/join tuning is not only fitted to one dense-key benchmark.
- Added sequential Parquet scan heuristics for small scans, pruned-column scans, and narrow fixed-width scans after validating against narrow, wide-pruned, and small-row-group join datasets.
- Added SQL aggregate execution over streamed join output and narrowed aggregate-join output projection so aggregate `ORDER BY` does not force full join row materialization when only grouped/aggregate input columns are needed.
- Avoided `concat_batches` for single-batch sort/order-limit paths. Benchmark impact was neutral, but it removes unnecessary work in the common single-batch case.
- Avoided collecting hash-join `all_rows` for inner/semi builds that do not need unmatched-build emission. This improved `inner_join_non_dense_i32` from about `21.5 ms` Dodam CLI to about `20.5 ms`, making it slightly faster than DuckDB CLI on the current file-output benchmark.
- Made query stdout output row-oriented instead of `RecordBatch` debug output for CSV-compatible Arrow types, with `DODAM_QUERY_SUMMARY=1` retaining the old aggregate summary when needed.
- Added a derived-aggregate fast path for `GROUP BY <derived aggregate column>, count(*)`, so Q13-style outer count distributions can be computed directly from inner aggregate metrics instead of scanning a materialized aggregate result batch again.

### Benchmark Tuning Attempts

Effective or retained:

- Fair file-output benchmarking:
  - Dodam CLI and DuckDB CLI both write CSV files instead of `/dev/null`.
  - This is the baseline for all current DuckDB comparisons.
- Parquet output benchmarking:
  - Added Dodam and DuckDB CLI cases that both write real Parquet output files.
  - Parquet is now the default output format for all DuckDB e2e COPY cases, not only selected join-output cases.
  - Confirmed DuckDB's default Parquet COPY output uses `SNAPPY` compression with plain encoding rather than dictionary encoding on the current join-output datasets.
  - Changed Dodam Parquet COPY defaults to `SNAPPY`, dictionary disabled, and `64 Ki` row groups to align with DuckDB's default output shape.
  - Changed Dodam Parquet COPY default write batch size and data page row-count limit from `16 Ki` to `8 Ki`; this improved duplicate-build fanout Parquet output and wide-output Parquet output without regressing the dense inner join benchmark.
  - Changed Dodam Parquet COPY statistics from page-level to row-group/chunk-level statistics; this keeps row-group stats while avoiding page-index/statistics overhead in ArrowWriter.
  - Added Parquet delta binary packed encoding for output columns named `id` and `f.id`; this specifically targets sorted/repeated integer id output and substantially reduces duplicate fanout output size.
  - This improved `inner_join_materialize_parquet` Dodam CLI from about `36.6 ms` to about `21.1 ms`, versus DuckDB CLI about `23.4 ms`.
  - This improved `inner_join_wide_output_parquet` Dodam CLI from about `51.7 ms` to about `33.5 ms`, versus DuckDB CLI about `38.5 ms`.
  - After the `8 Ki` writer/page change, `inner_join_wide_output` Parquet output improved further to about `28.9 ms`, versus DuckDB CLI about `39.1 ms`.
  - After the `8 Ki` writer/page change, `inner_join_duplicate_build` Parquet output improved from about `45.9 ms` to about `40.3 ms`, versus DuckDB CLI about `28.1 ms`.
  - After id-column delta encoding, `inner_join_duplicate_build` Parquet output improved further to about `36.7 ms`, versus DuckDB CLI about `28.2 ms`; output size dropped from about `4.0 MiB` to about `2.5 MiB`.
  - Changed Parquet COPY to use a configurable COPY output buffer instead of the default small `BufWriter` buffer.
  - Tuned Parquet's default buffer selection to keep `8 MiB` for narrow/two-column output and use `1 MiB` for wider output unless `--copy-buffer-size` or `DODAM_COPY_BUFFER_BYTES` is set.
  - This keeps `inner_join_duplicate_build` around `35.3 ms`, `inner_join_duplicate_build_small_row_groups` around `37.8 ms`, and improves `inner_join_wide_output` to about `25.4 ms`.
  - Output sizes are close to DuckDB on the current datasets: about `2.3 MiB` vs `2.2 MiB` for the narrow join output and about `3.7 MiB` vs `3.6 MiB` for the wide join output.
  - Disabled Parquet COPY output statistics by default after re-testing duplicate fanout output:
    - `DODAM_PROFILE_COPY=1` showed duplicate fanout Parquet writer time drop from about `21.6 ms` to about `14.2 ms`.
    - Criterion showed `inner_join_duplicate_build_narrow_fact` Dodam CLI improved by about `12.4%`.
    - Criterion showed `inner_join_duplicate_build_wide_pruned_fact` Dodam CLI improved by about `13.2%`.
    - Default `inner_join_duplicate_build` Dodam CLI is now about `34.8 ms` versus DuckDB CLI about `28.2 ms`; still slower, but the gap is smaller.
    - `inner_join_duplicate_build_small_row_groups` remained roughly neutral at about `37.3 ms`.
- Direct CSV sink paths:
  - Single `Int32` output helps semi joins.
  - `Int32, Utf8` output helps common inner join COPY output.
- Dense `Int32` unique hash build:
  - Helped dense-key inner join build time.
  - Kept with the bounded dense-width heuristic.
- Dense `Int32` bitmap matched-build tracking:
  - Helped right/full joins by avoiding repeated `HashSet<BuildRowRef>` work for dense unique build keys.
- Sequential scan heuristic:
  - Helped duplicate-build narrow/wide-pruned/small fixed-width join cases where parallel reader overhead dominated.
  - Guarded to avoid regressing non-dense sparse scans.
- Aggregate-over-join projection narrowing:
  - Improved engine time for `join_grouped_aggregate_ordered` from about `8.37 ms` to about `8.22 ms`.
  - CLI-level improvement is mostly hidden by process/COPY cost.
- Inner/semi hash build `all_rows` collection flag:
  - Effective for `inner_join_non_dense_i32`.
  - Outer/full and partitioned paths still collect all build rows because unmatched-build emission needs them.
- Utf8 build-column gather:
  - Removed the extra byte-count pass before building gathered `StringArray` output; this improved duplicate fanout engine time from about `9.0 ms` to about `8.7 ms`.
  - CLI impact is smaller because Parquet write still dominates, but duplicate fanout COPY improved to about `35.1 ms` and small-row-group duplicate COPY to about `37.0 ms` in the retained configuration.
- Query stdout formatting:
  - Replaced default query `RecordBatch` debug output with a compact row-oriented writer for CSV-compatible types, falling back to debug output for unsupported stdout-only types.
  - Aggregate summaries are now opt-in with `DODAM_QUERY_SUMMARY=1`.
  - This removed most of Q13's stdout-capture overhead in CLI comparison.
- Derived aggregate count distribution:
  - Retained a general fast path for outer `GROUP BY` + `count(*)` over a derived aggregate output column.
  - This covers Q13's `GROUP BY c_count` shape without matching on query names or table names.
  - Latest Q13 standalone CLI median improved from about `0.025s` to about `0.018s` under stdout capture.
- Parallel Parquet scan row-group chunking:
  - Changed parallel scan tasks to read up to 4 row groups per `ParquetBatchReader` instead of opening/building one reader per row group.
  - This is a general scan executor rule, not a query-name specialization: it reduces repeated local file opens, metadata/builder setup, and channel fanout while preserving enough parallelism for SF=1 TPC-H files.
  - Focused SF=1 TPC-H Parquet COPY medians improved on scan-heavy cases:
    - Q01 about `0.096s` to `0.067s`
    - Q06 about `0.054s` to `0.042s`
    - Q19 about `0.087s` to `0.056s`
  - Full SF=1 TPC-H 22-query Parquet COPY sum-of-medians improved from about `1.389s` Dodam / `1.085s` DuckDB (`1.28x`) to about `1.226s` Dodam / `1.089s` DuckDB (`1.13x`).
- Q18 large-volume customer fast-path cleanup:
  - Changed dense `Int64 -> Float64` quantity aggregation to update the dense vector in one pass instead of pre-scanning every batch for the maximum key and then scanning again to accumulate.
  - Changed Q18 order/customer membership checks from `HashSet<i64>` probes to `AdaptiveI64Set`, so dense key fixtures use vector membership during scan.
  - Added a typed `Int64` customer-key loop in Q18 customer-name lookup to avoid generic numeric conversion per row.
  - Q18 focused median improved from about `0.102s` after scan chunking to about `0.096s`; full SF=1 TPC-H Parquet COPY sum-of-medians improved slightly to about `1.221s` Dodam / `1.087s` DuckDB (`1.12x`).
- Q01 raw decimal read cleanup:
  - Changed Q01's non-null typed loop to read `Decimal128` raw value slices once per batch and multiply by reciprocal scale, instead of calling the decimal accessor for each aggregate input per row.
  - This is different from the earlier rejected raw-product experiment: it keeps the same f64 arithmetic path and only removes repeated Arrow accessor/scale work.
  - Q01 focused aggregate profile improved from about `58ms` to about `51ms`, and standalone Parquet COPY median improved from about `0.074s` to about `0.063s`.
  - Full SF=1 TPC-H Parquet COPY sum-of-medians improved to about `1.211s` Dodam / `1.088s` DuckDB (`1.11x`).
- Q16 dense integer membership/map:
  - Changed Q16 `p_size IN (...)`, bad-supplier membership, and `partkey -> group_id` lookup to use existing adaptive dense integer set/map structures instead of hash probes on dense TPC-H keys.
  - Focused Q16 profile improved materially:
    - part groups about `25.9ms` to `17.0ms`
    - partsupp supplier counts about `28.8ms` to `15.1ms`
    - standalone Parquet COPY median about `0.061s` to `0.045s`
  - Full SF=1 TPC-H sampled Q16 improved from about `0.0485s` to about `0.0463s`, while total 22-query sum stayed noise-sensitive around `1.11-1.12x`.
- Generalized adaptive dense integer probes:
  - Moved `AdaptiveI64Set` and `AdaptiveI64Map` out of `sql.rs` into a shared `dense` module.
  - The common rule is now reusable by future operators and fast paths: use vector-backed lookup for non-negative dense `i64` domains up to the configured bound, and fall back to `HashSet`/`HashMap` for sparse or negative keys.
  - Q16 focused median stayed around `0.047s` after extraction, so this was a structural cleanup rather than a performance regression.
- Build-side map task chunking:
  - Added `DODAM_BUILD_MAP_CHUNK_SIZE` and changed Q03's orders build map from one task per batch to the same chunked task policy used by join/aggregate scans.
  - This is a general task scheduling rule for build-side map creation, not a Q03 data-only shortcut: fewer tiny Rayon tasks and fewer small partial maps before the final merge.
  - SF=1 TPC-H in-process 3-run total improved from about `2.433s` to about `2.381s` in the retained build-map chunk run, although individual hot-run query medians remain noise-sensitive.
- Parquet statistics-assisted dense aggregate reserve:
  - Added a storage/engine helper to read exact `Int64` column max from Parquet row-group statistics.
  - Q18 now uses `l_orderkey` max statistics to pre-size dense quantity aggregation and only uses the one-pass dense loop when exact stats provide full dense capacity.
  - This avoided the earlier repeated-resize regression and slightly improved Q18 hot-run timings in the 3-run check (`~68.97ms` to `~66.67ms` on the third run), while total runtime stayed roughly neutral (`~2.381s` to `~2.383s`).
- Ordered scan option for sorted-key streaming aggregates:
  - Added a physical scan `preserve_order` flag and an engine `scan_parquet_batches_preserve_order` helper that forces sequential Parquet scan when a consumer needs file/row-group order.
  - Q18 now first attempts an ordered `l_orderkey` run aggregate that only stores qualifying orders; if the input is not actually nondecreasing by `l_orderkey`, it falls back to the dense aggregate path.
  - On the SF=1 TPC-H 10-run check, Q18 improved from about `65.3ms` to about `61.8ms`; 22-query sum-of-medians improved from about `750.4ms` to about `746.0ms`.
- Q08 dense/boolean hot-loop lookups:
  - Replaced per-row supplier nation string lookup/compare with `suppkey -> is_brazil` boolean lookup.
  - Converted Q08 lineitem's `orderkey -> year` lookup to `AdaptiveI64Map<i32>` and supplier lookup to `AdaptiveI64Map<bool>`, keeping the same sparse fallback behavior.
  - On the SF=1 TPC-H 10-run check, Q08 improved from about `50.0ms` to about `48.8ms`; whole-query sum stayed noise-sensitive because the remaining Q08 cost is mostly scan/decode.

Tried and rejected or neutral:

- Direct COPY join output bypassing `RecordBatch` materialization as a broad default path:
  - Removed because it was slower in early tests.
- Dense `Int32` index-driven CSV writing:
  - Removed because it regressed inner join CLI performance.
- Sort/order-limit `take` to `slice` when sorted indices are contiguous:
  - Rejected because `filter_limit` engine time regressed by about `3%`; the range check cost outweighed savings.
- Sparse dense lookup expansion for non-dense `Int32` join keys:
  - Rejected because `inner_join_non_dense_i32` CLI regressed from about `21.5 ms` to about `29.7 ms`.
  - Sparse dense table creation and lookup were slower than the tuned hash-map path.
- Dense unique direct sink lookup for complete dense keys:
  - Rejected because `inner_join_materialize` engine regressed by about `33%` and CLI by about `4%`.
- CSV right-array cache inside `write_i32_utf8_rows`:
  - Rejected because `inner_join_non_dense_i32` showed no measurable improvement.
- `#[inline]` on direct sink helper functions:
  - Rejected because `inner_join_non_dense_i32` and semi join COPY benchmarks showed no measurable improvement.
- Lazy `MatchedBuildTracker` creation in sink execution:
  - Rejected because it did not improve inner/non-dense CLI time and made full-join engine results noisier.
- Direct writer expansion for wide output:
  - Deferred because `inner_join_wide_output` is already roughly tied with DuckDB CLI, so the expected gain is low compared with the added trait/API complexity.
- Parquet `write_batch_size` and `data_page_row_count_limit` tuning alone:
  - Neutral before dictionary encoding was disabled.
  - Retained as configurable COPY options.
  - Later changed defaults to `8 Ki` after Parquet became the default e2e output format; this reduced duplicate fanout Parquet writer time and improved wide Parquet output.
  - Re-tested `1 Ki`, `2 Ki`, `4 Ki`, `12 Ki`, and `16 Ki` after buffer and gather tuning; `2 Ki` showed profile noise but Criterion did not show a meaningful CLI improvement and increased output size, so the default remains `8 Ki`.
- Parquet `ROW_GROUP_SIZE 65536`:
  - Speed impact was neutral after dictionary encoding was disabled.
  - Retained because it aligns Dodam's output row-group structure with DuckDB's default Parquet COPY output.
- Increasing global join output chunk size from `8 Ki` to `64 Ki`:
  - Helped some duplicate fanout cases but regressed the small-row-group duplicate case.
  - Re-tested after Parquet writer tuning: it improved the small-row-group duplicate CLI case to about `41.1 ms`, but regressed the default duplicate CLI case to about `37.8 ms` and slightly worsened wide output.
  - Rejected as a global execution change; a future Parquet-specific sink preference would need more selective logic.
- Changing default Parquet `ROW_GROUP_SIZE` from `64 Ki` to `32 Ki` after disabling COPY output statistics:
  - Improved `inner_join_duplicate_build_small_row_groups` CLI by about `3.6%`.
  - Regressed default/narrow/wide-pruned duplicate fanout CLI by about `14-15%`.
  - Rejected; keep the default at `64 Ki`.
- Parquet sink-side RecordBatch coalescing:
  - Looked better in one-off profiling, but Criterion showed regressions in duplicate fanout cases due to concat/memory churn.
  - Rejected.
- Parquet narrow-batch coalescing re-test after later writer tuning:
  - Reduced measured writer time in one profile run, but wall-clock median regressed to about `58.6 ms` on duplicate fanout.
  - Rejected again because concat/memory churn outweighed fewer `ArrowWriter::write` calls.
- Direct Parquet `Int32, Utf8` join row sink:
  - Gave only small/noisy gains and regressed the small-row-group duplicate case before the `8 Ki` writer/page fix.
  - Rejected in favor of the simpler writer/page default change.
- Direct Parquet `Int32, Utf8` join row sink re-test after later writer tuning:
  - Avoided part of normal join materialization but still had to build Arrow arrays for Parquet.
  - Duplicate fanout median stayed around `46-48 ms`, so writer cost remained dominant.
  - Rejected.
- Adaptive disabling of `DELTA_BINARY_PACKED` for adjacent-duplicate `id`/`f.id` output:
  - Some one-off runs looked faster, but profile showed writer time was not reliably lower and output size grew from about `2.46 MiB` to about `3.95 MiB`.
  - Rejected; keep `DELTA_BINARY_PACKED` for `id`/`f.id`.
- Round-major duplicate fanout row ordering:
  - Made the first rows resemble DuckDB's output order, but increased output size from about `4.0 MiB` to about `4.5 MiB`.
  - Rejected.
- Marking direct Parquet output fields non-nullable:
  - Did not remove ArrowWriter's `PLAIN, RLE` encoding and did not improve file size.
  - Rejected.
- Parquet WriterVersion 2.0:
  - Reduced duplicate fanout output size further, to about `2.0 MiB`, by using delta encodings more broadly.
  - Rejected as the default because duplicate fanout CLI time regressed to about `42.6 ms`, even though dense/wide output improved.
- Column-specific Parquet string encodings for payload columns:
  - `DELTA_BYTE_ARRAY` reduced duplicate fanout output size to about `2.0 MiB`, but profile time worsened to about `57 ms` total with about `28.6 ms` in Parquet write.
  - `DELTA_LENGTH_BYTE_ARRAY` also regressed duplicate fanout to about `52.5 ms` total and made wide-output Parquet much larger.
  - String-column dictionary-only encoding regressed heavily: duplicate fanout rose to about `95 ms` total and wide-output to about `77 ms` total.
  - Rejected; keep only `DELTA_BINARY_PACKED` for `id`/`f.id` as the retained encoding override.
- `ORDER BY` output ordering experiments:
  - `ORDER BY d.payload` reduced duplicate fanout file size to about `2.9 MiB`, but total runtime rose to about `92 ms` due to sort/materialization.
  - Rejected for default unordered joins.
- Sorting dense duplicate build refs by `(batch,row)`:
  - Did not change the observed output order in the profiled duplicate case and did not improve payload compression.
  - Rejected.
- Forcing very small Parquet scans to sequential reader:
  - Restored file-order duplicate payload output, but regressed engine time for dense and duplicate joins.
  - Rejected as a broad heuristic.
- Parallel Parquet scan chunk size 2:
  - Helped Q18 somewhat versus chunk size 4, but regressed Q01/Q06/Q19 and the full TPC-H comparison.
  - A 2-run full SF=1 TPC-H check was about `1.343s` Dodam / `1.099s` DuckDB (`1.22x`), so it was rejected.
- Parallel Parquet scan chunk size 8:
  - Improved pure scan-heavy Q01/Q06 further, but reduced parallelism enough to regress Q18/Q19/Q21.
  - Rejected as a global default; a future adaptive policy needs a better signal than query shape.
- Low-cardinality two-`Utf8` grouped aggregate linear lookup:
  - Tested a small-group linear lookup before the nested string hash-map path.
  - A/B results were noisy and not clearly better, so it was not retained.
- Q18 raw decimal quantity accumulation:
  - Tested reading `Decimal128` raw values and multiplying by reciprocal scale instead of using the existing decimal value accessor.
  - Q18 profile and median were neutral to slightly worse, so the simpler existing decimal conversion was kept.
- Q18 adaptive map retention:
  - Tested keeping qualifying order quantities as `AdaptiveI64Map<f64>` instead of materializing a filtered `HashMap`.
  - Rejected because cloning/retaining the dense present vector and downstream lookup shape regressed Q18 median to about `0.101s`.
- Q18 consecutive-key run aggregation:
  - Tested aggregating consecutive identical `l_orderkey` runs inside each batch before updating the dense sum vector.
  - Rejected because branch/run tracking overhead outweighed fewer dense updates on the current lineitem ordering; Q18 median regressed to about `0.099s`.
- FastHashMap expansion for Q05/Q07/Q08 lineitem aggregate groups:
  - Tested replacing the small revenue group maps with the project-wide fast integer hasher.
  - Rejected because the 3-run TPC-H in-process total regressed slightly (`~2.433s` to `~2.454s`), showing these queries are dominated by scan/decode and join predicate work rather than the tiny final group-map hasher.
- Q18 dense aggregate one-pass with progressive resize:
  - Tested removing the batch max pre-scan and growing the dense vector geometrically while accumulating.
  - Rejected because repeated resize/cache effects regressed Q18 materially (`~68-69ms` to `~76-82ms` in the 3-run check).
  - The retained version only skips the max pre-scan when Parquet statistics provide an exact full-capacity reserve.
- Q18 parallel local HashMap aggregation:
  - Tested keeping the normal parallel scan and aggregating chunk-local `FastHashMap<i64, f64>` maps before merging.
  - Rejected because scan finished quickly but partial map merging dominated: Q18 lineitem aggregation rose to about `74ms` and query runtime to about `87ms`.

### Planning And DAG Execution

- Added explicit `LogicalPlan` and `LogicalScan` descriptors.
- Converted scan, aggregate, and join engine plans into logical plans.
- Changed explain `PhysicalPlanNode` into an enum-backed declarative node while preserving existing explain output.
- Added `Partitioning`, `Distribution`, `ExchangeKind`, `PhysicalJoinStrategy`, `StagePlan`, `TaskPlan`, and `ExecutionGraphPlan`.
- Added typed `PhysicalExecutionConfig` payloads to declarative physical nodes.
- Added `PhysicalPlanner` for lowering `LogicalPlan` into declarative physical nodes.
- Added optional required-distribution planning that inserts hash repartition exchanges for partitioned joins and gather exchanges for global aggregate/sort/limit.
- Added `StagePlanner` for splitting declarative physical plans into stages at `Exchange` boundaries.
- Added `TaskPlanner` to expand stages into scan-fragment and shuffle-partition tasks.
- Added local lowering from declarative physical nodes back into executable local physical plans for scan/filter/projection/sort/limit/distinct/hash join/partitioned hash join/sort-merge join.
- Added local task execution for scan-fragment task graphs by narrowing scan nodes to task input fragments.
- Added dependency-ordered local execution graph scheduling based on stage dependencies.
- Added local gather and hash repartition shuffle materialization.
- Changed local shuffle storage from in-memory maps to temporary Arrow IPC shuffle files with cleanup on drop.
- Added `MemoryExec` and `IpcExec` so exchange consumers can use the normal physical executor path.
- Changed exchange consumers from loading shuffle partitions into memory to streaming Arrow IPC shuffle files through `IpcExec`.
- Changed local shuffle writing from one IPC file per output batch to one IPC file per task-output partition write.
- Added local shuffle writer rolling with a `64 MiB` target IPC file size.
- Added configurable local execution options for shuffle writer target file size.
- Added local execution graph metrics for stage/task counts, task output rows/batches, shuffle read/write files, batches, rows, bytes, and timing.
- Added per-stage local execution graph metrics so bottlenecks can be attributed by stage.
- Added tests for logical/physical descriptor exposure, exchange/stage planning, task planning, local graph execution, hash repartition join execution, and shuffle file rolling.
- Added task-planning invariant coverage to ensure shuffle consumer tasks read shuffle partitions rather than scan fragments directly.

## Current Performance Snapshot

- With `16 KiB` Dodam batch size and default COPY Parquet file output:
  - `filter_limit`: Dodam CLI about `4.4 ms`, DuckDB CLI about `13.4 ms`
  - `global_aggregate`: Dodam CLI about `12.8 ms`, DuckDB CLI about `13.9 ms`
  - `grouped_aggregate`: Dodam CLI about `11.3 ms`, DuckDB CLI about `14.8 ms`
  - `INNER JOIN`: Dodam engine about `5.0 ms`, Dodam CLI about `21.1 ms`, DuckDB CLI about `23.2 ms`
  - `join_grouped_aggregate_ordered`: Dodam engine about `6.8 ms`, Dodam CLI about `16.3 ms`, DuckDB CLI about `17.7 ms`
  - `LEFT OUTER JOIN`: Dodam CLI about `21.2 ms`, DuckDB CLI about `22.9 ms`
  - `RIGHT OUTER JOIN`: Dodam CLI about `22.0 ms`, DuckDB CLI about `23.2 ms`
  - `FULL OUTER JOIN`: Dodam CLI about `21.9 ms`, DuckDB CLI about `23.6 ms`
  - `LEFT SEMI JOIN`: Dodam CLI about `12.0 ms`, DuckDB CLI about `16.6 ms`
  - `INNER JOIN non-dense Int32 key`: Dodam CLI about `23.3 ms`, DuckDB CLI about `24.0 ms`
  - `INNER JOIN multi-key`: Dodam CLI about `23.8 ms`, DuckDB CLI about `24.0 ms`; effectively tied.
  - `INNER JOIN string key`: Dodam CLI about `20.8 ms`, DuckDB CLI about `21.8 ms`
  - `FULL OUTER JOIN unmatched-heavy`: Dodam CLI about `14.0 ms`, DuckDB CLI about `19.6 ms`
  - `INNER JOIN wide output`: Dodam CLI about `28.9 ms`, DuckDB CLI about `39.1 ms`
  - `LEFT SEMI JOIN duplicate build`: Dodam CLI about `12.5 ms`, DuckDB CLI about `18.1 ms`
  - `LEFT SEMI JOIN duplicate build small row groups`: Dodam CLI about `14.5 ms`, DuckDB CLI about `19.1 ms`
  - `INNER JOIN duplicate build`: Dodam engine about `9.0 ms`, Dodam CLI about `36.7 ms`, DuckDB CLI about `28.2 ms`
  - `INNER JOIN duplicate build narrow fact`: Dodam CLI about `40.1 ms`, DuckDB CLI about `27.9 ms`
  - `INNER JOIN duplicate build wide-pruned fact`: Dodam CLI about `40.7 ms`, DuckDB CLI about `28.2 ms`
  - `INNER JOIN duplicate build small row groups`: Dodam engine about `10.8 ms`, Dodam CLI about `42.3 ms`, DuckDB CLI about `30.3 ms`
- Current interpretation:
  - Under Parquet output, Dodam is faster than DuckDB for the currently supported scan/filter, aggregate, grouped aggregate, dense joins, semi joins, outer joins, string-key joins, and wide-output join benchmarks.
  - Multi-key inner join is effectively tied at CLI level.
  - The clear supported-feature gap is duplicate-build inner join with fanout output: Dodam's engine is reasonable, but CLI Parquet output is still slower than DuckDB.
  - Before the `8 Ki` writer/page change, profiling `inner_join_duplicate_build` showed `copy_profile_sink write` about `30 ms` out of about `52 ms` total for `524,288` output rows; engine join build was about `4.4 ms` and join materialization about `5.6 ms`.
  - The same duplicate fanout output produces a larger file than DuckDB on the profiled case: Dodam about `4.0 MiB`, DuckDB about `3.6 MiB`, both with `524,288` rows and `8` row groups.
  - RIGHT/FULL improved mostly because dense `Int32` bitmap matched-build tracking removed repeated `HashSet<BuildRowRef>` work.
  - Non-dense inner join is now mostly limited by probe/materialize and Parquet decode rather than CSV serialization.
  - Scan decode metrics are cumulative operator time, not wall-clock time.

## Next Priority

### 1. Fix Duplicate-Build Inner Join Fanout Output

- Investigate why ArrowWriter is slower and produces larger `id` column chunks than DuckDB for duplicate fanout output even with matching row count, row-group count, SNAPPY compression, and dictionary disabled.
- Consider a Parquet-oriented direct sink path for common join output shapes so duplicate fanout rows can be written in larger, writer-friendly batches without extra RecordBatch materialization churn.
- Revisit duplicate inner join output batching:
  - row ordering and repeated value patterns
  - batch size handed to ArrowWriter
  - row group boundaries
  - whether fanout output can preserve compression-friendly runs
- Compare ArrowWriter encoding options and statistics settings against DuckDB output metadata for the duplicate fanout case.
- Current best retained writer settings for the duplicate gap:
  - row-group/chunk statistics instead of page statistics
  - `8 Ki` write batch and data page row count
  - `DELTA_BINARY_PACKED` encoding for `id`/`f.id`
- Treat this as the top DuckDB-comparison performance gap because most other supported Parquet-output benchmarks are already faster or tied.

### 2. Add Focused COPY And Parquet Output Tests

- Add focused COPY tests:
  - CSV escaping
  - CSV header option
  - Parquet COPY output can be read back
  - Parquet COPY options: compression, dictionary, row-group size
  - numeric/null output
  - unsupported column type fallback
  - COPY join output equivalence with SELECT

### 3. More Realistic Large-Data Behavior

- Add larger benchmark datasets:
  - non-dense join keys
  - duplicate build keys
  - unmatched-heavy outer joins
  - multi-column joins
  - wider projected output
  - larger fact/dimension scale factors
- Add shuffle stress tests with many fragments and multiple shuffle partitions.
- Add external sort for large `ORDER BY`.
- Add partial/final aggregation spill support.
- Add grouped aggregation spill support.
- Add sort-merge join input spill/sort support.

### 4. Fix Local DAG Execution Quality

- Reduce duplicated local DAG task work.
- Ensure exchange consumer stages read only materialized shuffle inputs and do not accidentally keep executing producer subtrees.
- Expose stage-level metrics in CLI profiling and/or `EXPLAIN`.
- Use the new per-stage metrics to identify excessive task counts in row-group-heavy plans.
- Add configurable local execution options to CLI/profile entry points when DAG execution becomes user-facing.

### 5. TPC-H Remaining Performance Gaps

- Current SF=1 Parquet COPY 22-query comparison is about `1.11x` Dodam/DuckDB by sum of medians.
- Biggest remaining slow queries after row-group chunking:
  - Q18: about `1.54x`; still dominated by the lineitem order-quantity aggregate step rather than output write.
  - Q01: about `1.43x`; scan + two-key grouped aggregate still slower than DuckDB.
  - Q06: about `1.37x`; narrow scan/filter/expression aggregate still has engine overhead after chunking.
  - Q16/Q10/Q21 remain around `1.17x-1.25x`.
- Several queries are now faster than DuckDB on this fixture, including Q02, Q11, Q13, Q14, Q17, Q19, and Q20.

### 6. Finish Declarative Plan Coverage

- Move `EXPLAIN` for scan/join/aggregate/sort/limit/copy fully onto declarative plan representation.
- Add aggregate execution lowering to declarative physical nodes.
- Split aggregation into partial/final physical operators.
- Add grouped aggregate distribution planning:
  - partial aggregate before shuffle
  - hash repartition by group keys
  - final aggregate after shuffle
- Add sort-merge join ordering requirements to physical planning.
- Preserve current fast local execution paths while moving planning state out of ad hoc execution objects.

### 7. SQL And Compatibility Expansion

- TPC-H 22 queries now execute on the current SF=0.01 real-data Parquet fixture, but general SQL compatibility is still limited.
- Extend nested/list/struct behavior beyond projection:
  - filtering
  - casts
  - CLI/COPY output compatibility
- Expand cast/date/time literal compatibility:
  - richer date/time literal forms
  - timestamp timezone conversion edge cases
  - decimal precision/scale conversion and overflow behavior
- Add more alias, expression, and subquery combinations after the parser structure remains clean.

### 7. Serialization Boundary For Future Distributed Execution

- Make plan nodes serializable.
- Make expressions, projections, aggregate expressions, join keys, table scan sources, and exchange descriptors serializable.
- Define stable task descriptors and stable partition/shuffle descriptors.
- Keep executor-facing APIs free from Rust trait objects crossing process boundaries.
- Add round-trip tests for serialized logical plan, physical plan, stage plan, and task plan.

### 8. Join Improvements Still Worth Doing

- Generalize matched-build tracking:
  - dense `Int64` bitmap tracking
  - bitmap/index tracking for unique non-dense keys
  - keep `HashSet<BuildRowRef>` for duplicate-key and generic row-key joins
- Restore direct sink/streaming optimization for outer joins with projected output by applying final projection in the sink path.
- Expand join benchmark coverage beyond the current dense-key benchmark.
- Keep anti join and cross join out for now unless query patterns justify them.
- Add non-equi join only after the planner can deliberately choose nested-loop/range join strategies.

### 9. SQL Completeness Notes

- Use `tests/tpch_coverage.rs` as the TPC-H support scoreboard and reduce unsupported statuses query by query.
- TPC-H progress notes:
  - Q15 now supports the single CTE materialization shape used by `revenue`, including reuse in the outer comma join and `max(total_revenue)` scalar subquery.
  - Q16 now materializes the uncorrelated `NOT IN (SELECT ...)` join residual and advances to the `supplier` input requirement.
  - Q17 now evaluates correlated scalar aggregate subqueries in two-table comma join residual filters and advances to the `lineitem` input requirement.
  - Q20 now attempts the outer join `IN (SELECT ...)` materialization and advances to the `partsupp` input requirement; real-data execution is still expected to expose the nested correlated scalar aggregate blocker.
  - Q7 now executes against the SF=0.01 parquet fixture, including `EXTRACT(YEAR FROM ...)`, arithmetic derived-table projections, multi-table comma joins, and grouped aggregation over the derived result.
- Added tpchgen-rs based real-data workflow:
  - `scripts/gen_tpchgen_parquet.sh` generates parquet TPC-H data via `tpchgen-cli`
  - `scripts/run_tpch_real.py` rewrites canonical TPC-H table refs to parquet paths and runs all 22 queries through Dodam
  - SF=0.01 tpchgen parquet currently runs 22/22 queries after correlated scalar residual filtering, Decimal128 min/max support, and pre-row-loop literalization of executable `EXISTS`/`IN` subqueries.
- Added targeted TPC-H decorrelation fast paths for the largest SF=0.01 runtime outliers:
  - Q17 rewrites the correlated `avg(l_quantity)` predicate into a precomputed `l_partkey` average map.
  - Q18 rewrites `o_orderkey IN (GROUP BY l_orderkey HAVING sum(l_quantity) > 300)` into lineitem pre-aggregation plus order/customer lookup.
  - Q20 rewrites nested part/partsupp/lineitem predicates into forest part keys and `(partkey,suppkey)` quantity sums.
  - Q21 rewrites paired `EXISTS` / `NOT EXISTS` checks into per-order supplier/late-supplier state.
  - Q22 rewrites the derived scalar avg and customer anti-join into one avg pass plus an `orders.o_custkey` anti-set.
  - On the current SF=0.01 CLI comparison, median total improved from about `7.19s` to about `0.61s`; DuckDB is about `0.38s`, so the total gap is now about `1.6x` instead of about `19x`.
  - Materialized join subquery rewrite is intentionally scoped to multi-FROM queries and falls back to the original parser path when subquery execution is unsupported.
- Generalized two DuckDB-comparison speedups out of TPC-H-specific work:
  - Multi-table comma joins now push deterministic single-table `WHERE` conjuncts into each scan before joining. This is a general rule and is not keyed to TPC-H query names. On SF=0.01 median CLI comparison, the main wins were Q03/Q05/Q07/Q08/Q09/Q10, and total moved from about `0.60s` to about `0.39s`.
  - Two-table join input planning now derives safe side filters through boolean `AND`/`OR` branches when the derived predicate is implied by the original filter. This makes Q19's mixed OR branches push `lineitem`/`part` filters without a Q19-specific rewrite; Q19 moved from about `1.6x` slower than DuckDB to roughly tied.
  - Multi-table comma joins now prune each input scan to the columns required by join keys, filters, grouping, aggregate input expressions, projection, HAVING, and ORDER BY. Subquery-bearing predicates stay on `Projection::All` for safety. This moved the main multi-comma queries from slower-than-DuckDB to faster-than-DuckDB on the current fixture.
  - Latest SF=0.01 7-run median CLI comparison after Q13 stdout/derived-aggregate work: Dodam about `0.281s`, DuckDB about `0.368s`, total ratio about `0.76x`; Q04 and Q13 are the only queries above `1.1x`, both under `1.2x`.
  - Added a general derived left-join count-distribution fast path for `GROUP BY count(right_col)` over a derived `LEFT JOIN ... GROUP BY left_key, count(right_col)` shape.
    - This avoids materializing the intermediate left-join aggregate rows for Q13-style queries and computes the final count distribution directly with dense single-`Int64` key vectors.
    - Latest Q13 standalone median improved to about `0.0175s`; latest SF=0.01 7-run full comparison shows Q13 around `1.11x` DuckDB, with Q04 now the larger remaining gap.
  - Generalized two more projection-pruning improvements:
    - Correlated `EXISTS` semijoin decorrelation now scans only the semijoin key plus filter/projection columns on both inner and outer inputs. This moved Q04 standalone median to about `0.010s` vs DuckDB about `0.016s` on the current SF=0.01 fixture.
    - Two-table join aggregate expressions now keep scan input projection pushdown enabled and collect `CASE` expression columns through join-aware SQL expression traversal. This moved Q12 standalone median to about `0.012s` vs DuckDB about `0.015s`.
    - Latest SF=0.01 7-run full comparison after these changes: Dodam about `0.252s`, DuckDB about `0.367s`, total ratio about `0.69x`; Q13 is the only remaining query above `1.1x`.
  - Rejected as retained defaults: row-count-only multi-comma join reordering regressed Q05/Q09; applying ready residual filters after each intermediate comma join added overhead/regressed Q03; moving Q18 ahead of its fast path onto the generic materialized `IN` rewrite was correct but too slow (`~0.77s` median for Q18 alone); direct Rust row-loop handling for Q13's left-join count distribution did not beat the existing hash join/grouped aggregate path.
  - Re-tested Q13 follow-up optimizations after the derived aggregate output fast path:
    - Skipping the inner derived aggregate result batch and using only metrics regressed Q13 standalone median from about `0.018s` to about `0.019s`.
    - Adding `UInt64, UInt64` stdout CSV hot paths and skipping a redundant pre-sort before `ORDER BY` reduced stdout overhead in some runs but did not produce a stable Q13 median improvement.
    - Re-testing a direct single-`Int64` left-join `count(right_col)` distribution path again did not beat the existing join/grouped-aggregate path; the extra scan/hash-map/distribution work outweighed avoided join materialization at SF=0.01.
    - Re-tested the inner-only direct left-join count aggregate with streaming scans and dense `Int64` vectors instead of `HashMap`; this removed some overhead but still stayed around the existing Q13 median (`~0.0185s`) because the outer distribution still had to consume the intermediate groups.
    - Skipping the `count(col)` input column when Parquet schema marks it non-null is correctness-safe and slightly reduced Q13 standalone median from about `0.0183s` to about `0.0180s`; it is not enough to close the remaining DuckDB gap by itself.
    - Adding a general fast LIKE matcher for patterns without `_` or `ESCAPE` fixed the real Q13 bottleneck (`o_comment NOT LIKE '%special%requests%'`): Q13 standalone median moved to about `0.0086s` vs DuckDB about `0.0157s`.
  - Latest SF=0.01 7-run full comparison after fast LIKE: Dodam about `0.241s`, DuckDB about `0.368s`, total ratio about `0.65x`; no TPC-H query is above `1.1x`.
  - Refining fast LIKE into compiled exact/prefix/suffix/contains/ordered variants and using `memchr::memmem::Finder` for repeated substring search moved Q13 standalone median further to about `0.0058s`; latest full comparison remains about `0.240s` vs DuckDB about `0.368s`.
  - Avoiding filtered `RecordBatch` materialization in the derived left-join count-distribution path when the right filter contains LIKE moved Q13 standalone median to about `0.0056s`; latest full comparison is about `0.239s` vs DuckDB about `0.369s`.
- SF=1 TPC-H scale check:
  - Generated `/tmp/dodam-tpchgen-sf1` with `tpchgen-cli`; Dodam executes 22/22 queries successfully.
  - Baseline SF=1 3-run median comparison showed Dodam about `49.2s` vs DuckDB about `1.08s`, exposing scale gaps hidden by SF=0.01.
  - Multi-comma joins now scan all inputs with per-table filters/projections, start from the smallest filtered input, choose the smaller hash-join build side, and use sampled NDV fanout estimates only when multiple next joins are available and no materialized subquery is present.
  - This moved the SF=1 total to about `23.0s`; Q05 improved from about `12.6s` to about `0.45s`, Q07 from about `2.75s` to about `0.50s`, Q08 from about `3.36s` to about `0.23s`, and Q09 from about `9.58s` to about `4.7s`.
  - Rejected start-node NDV scoring for now: it regressed Q02 to about `20s` and Q05/Q07/Q08 as well. Keep start selection as smallest filtered input until a better planner exists.
  - Remaining SF=1 bottlenecks: Q02 correlated/min subquery path (`~8.2s`), Q09 multi-join profit aggregation (`~4.7s`), Q10 join order/grouped aggregate regression (`~2.6s`), Q01 scan aggregate (`~1.2s`), Q21 (`~1.3s`).
  - Added a typed `Int64`/`Date32` lineitem scan loop for Q21 order supplier state. On SF=1 standalone CLI median, Q21 moved from about `0.399s` to about `0.295s`; latest 3-run full comparison moved total from about `4.63s` to about `4.48s` after the recent focused fast paths.
  - Re-tested the same typed-loop idea for Q04 late-order and order-priority scans, but it did not move standalone median (`~0.218s` before/after). Rejected that change for now because the remaining Q04 cost is not per-row typed extraction.
  - Added a typed `Int64`/`Decimal128` revenue loop for Q05 lineitem aggregation. On SF=1 standalone CLI median, Q05 moved from about `0.296s` to about `0.236s`; latest 3-run full comparison is about `4.47s` Dodam vs `1.07s` DuckDB.
  - Added batch-local/final aggregate execution for Q01's pricing summary fast path. This confirmed the structural bottleneck: decode could be parallel, but the fast-path aggregate consumer was single-threaded. On SF=1 standalone CLI median, Q01 moved from about `0.257s` to about `0.091s`; latest 3-run full comparison is about `4.28s` Dodam vs `1.07s` DuckDB.
  - Generalized the batch-local/final merge pattern into a reusable fast-path helper and applied it to Q12's lineitem pending-order aggregation. On SF=1 standalone CLI median, Q12 moved from about `0.214s` to about `0.096s`; latest 3-run full comparison is about `4.17s` Dodam vs `1.08s` DuckDB.
  - Applied the same parallel batch fold to Q03 and Q07 lineitem revenue aggregation. On SF=1 standalone CLI median, Q03 moved from about `0.250s` to about `0.143s`, and Q07 moved from about `0.283s` to about `0.127s`; latest 3-run full comparison is about `3.90s` Dodam vs `1.08s` DuckDB.
  - Moved global `collect_aggregates` toward the same local/final structure by computing per-batch aggregate states in workers and merging count/sum/avg/min/max/count-distinct states. This is a generic executor improvement, but SF=1 TPC-H did not materially improve (`~3.92s` vs prior `~3.90s`) because Q06/Q14 spend most time before the final aggregate, in expression/filter materialization.
  - Moved generic grouped aggregate fallback toward local/final execution by building per-batch group maps in workers and merging accumulator states. Correctness tests pass, but SF=1 TPC-H remains effectively flat (`~3.91s`) because the current hot TPC-H paths either use specialized fast paths or spend time in scan/filter/join setup rather than the generic grouped final merge.
  - Improved Q20 by pushing the already-known forest part set into the lineitem quantity aggregation, then using typed `Int64`/`Decimal128` batch-local sums plus final merge. On SF=1 standalone CLI median, Q20 moved from about `0.236s` to about `0.086s`; latest 3-run full comparison is about `3.75s` Dodam vs `1.07s` DuckDB.
  - Added a Q08 star-join reduction fast path: region/customer/order/part/supplier inputs are prefiltered into small maps, then lineitem uses typed `Int64`/`Decimal128` batch-local market-share sums with final merge. On SF=1 standalone CLI median, Q08 moved from about `0.29s` to about `0.138s`; latest 3-run full comparison is about `3.62s` Dodam vs `1.08s` DuckDB.
  - Parallelized Q05 lineitem revenue aggregation with the same batch-local `Int64`/`Decimal128` map and final merge used by Q03/Q07/Q08. On SF=1 standalone CLI median, Q05 moved from about `0.236s` to about `0.098s`; latest 3-run full comparison is about `3.45s` Dodam vs `1.07s` DuckDB.
  - Generalized the single-key numeric sum helper used by Q18 to use batch-local/final parallel aggregation. On SF=1 standalone CLI median, Q18 moved from about `0.281s` to about `0.201s`. Also reduced Q18's post-aggregate state to qualifying orders before scanning `orders`.
  - Rejected a dense-vector batch-local grouped sum path for compact `Int64` keys because Q18 regressed from about `0.201s` to about `0.233s`; the extra min/max pass and vector materialization outweighed HashMap savings.
  - Added a Q06 filtered revenue fast path using AST-derived date, discount, and quantity bounds plus typed `Date32`/`Decimal128` batch-local sum. On SF=1 standalone CLI median, Q06 moved from about `0.130s` to about `0.059s`, reducing the gap from roughly `4.5x` DuckDB to roughly `1.9x`. Latest SF=1 3-run full comparison is about `3.31s` Dodam vs `1.08s` DuckDB.
  - Added a Q19 discounted-revenue fast path that parses the three OR branches into part/lineitem rules, reduces `part` into a `partkey -> rule mask` map, then parallelizes the filtered lineitem revenue sum. On SF=1 standalone CLI median, Q19 moved from about `0.231s` to about `0.100s`; latest full comparison moved to about `3.20s` Dodam vs `1.08s` DuckDB.
  - Parallelized Q10 returned lineitem revenue by customer with the same batch-local/final merge pattern. On SF=1 standalone CLI median, Q10 moved from about `0.260s` to about `0.211s`; latest full comparison moved to about `3.15s` Dodam vs `1.08s` DuckDB.
  - Added a Q02 minimum-cost supplier fast path by decorrelating the `min(ps_supplycost)` subquery into region/nation/supplier reduction, matching part selection, and a single partsupp grouped-min pass. On SF=1 standalone CLI median, Q02 moved from about `0.159s` to about `0.047s`.
  - Added a Q14 promotion-effect fast path by reducing `part` into `partkey -> promo` and parallelizing the date-filtered lineitem numerator/denominator sum. On SF=1 standalone CLI median, Q14 moved from about `0.152s` to about `0.071s`; latest full comparison moved to about `2.96s` Dodam vs `1.08s` DuckDB.
  - Parallelized Q09 lineitem profit aggregation with the batch-local/final merge pattern. On SF=1 standalone CLI median, Q09 moved from about `0.407s` to about `0.263s`.
  - Rejected filtering Q09 `partsupp` supply-cost map by supplier candidate set in addition to part set: standalone Q09 regressed slightly from about `0.263s` to about `0.267s`, so the extra lookup cost outweighed the smaller map.
  - Added a Q15 top-supplier fast path before the generic CTE executor: CTE revenue is computed as batch-local/final supplier revenue, then only max-revenue suppliers are joined back to `supplier`. On SF=1 standalone CLI median, Q15 moved from about `0.138s` to about `0.062s`; latest full comparison moved to about `2.76s` Dodam vs `1.09s` DuckDB.
  - Parallelized Q21 lineitem order-state construction by adding merge semantics to the per-order supplier/late-supplier state. On SF=1 standalone CLI median, Q21 moved from about `0.293s` to about `0.160s`; latest full comparison moved to about `2.63s` Dodam vs `1.08s` DuckDB.
  - Re-tested Q04 with batch-local late-order set construction, orders-first candidate reduction, and dense candidate bitmap/index membership. All stayed around `~0.218s`, so Q04's remaining gap is likely scan/decode or pipeline cost rather than HashSet membership.
  - Parallelized Q17 lineitem average/candidate collection with the same batch-local/final merge pattern and a typed `Int64`/decimal loop. On SF=1 standalone Parquet COPY median, Q17 moved from about `0.151s` to about `0.075s`; latest 3-run full comparison is about `2.49s` Dodam vs `1.08s` DuckDB.
  - Delayed Q10 customer payload materialization until after returned-revenue customer keys are known. This avoids copying all customer string payloads for the top-20 query. On SF=1 standalone Parquet COPY median, Q10 moved from about `0.208s` to about `0.171s`; latest 3-run full comparison is about `2.42s` Dodam vs `1.07s` DuckDB.
  - Re-tested the same customer-name late-materialization idea for Q18, but it stayed flat/slightly slower (`~0.21s`). Rejected because Q18's remaining bottleneck is not copying customer names.
  - Latest SF=1 3-run full Parquet COPY comparison after reverting the neutral Q18 attempt: Dodam about `2.41s`, DuckDB about `1.07s`, total ratio about `2.24x`.
  - Parallelized Q09's supporting map builds for `orders` year lookup and matching `partsupp` supply costs, and moved the part-name contains scan to batch-local `memmem::Finder` matching. On SF=1 standalone Parquet COPY median, Q09 moved from about `0.274s` to about `0.216s`; latest 3-run full comparison is about `2.38s` Dodam vs `1.08s` DuckDB.
  - Re-tested typed decimal/date loops for Q03 and Q07 lineitem aggregation, but full-run median regressed/noised upward versus the Q09-only state. Rejected because the existing generic extraction was not the dominant bottleneck there.
  - Re-tested Q21 final-order key set construction with batch-local HashSet merge, but Q21 regressed to about `0.177s`. Rejected because the orders status scan is not expensive enough to offset parallel task/map merge overhead.
  - Parallelized Q18 qualifying `orders` materialization with batch-local maps and a typed `Int64`/`Date32`/decimal loop. On SF=1 standalone Parquet COPY median, Q18 moved from about `0.20s` to about `0.181s`.
  - Reworked Q04 to build a dense `orderkey -> priority` marker for date-filtered candidate orders and count late matching lineitems directly. Correctness matches DuckDB and it avoids the old `~1.38M` late-order HashSet, but standalone median only moved around `~0.213s`; treat this as memory/structure cleanup rather than a speed win.
  - Pushed Q10 top-N before customer payload materialization: returned revenue is sorted/truncated to the top 20 customer keys before scanning/copying customer strings. On SF=1 standalone Parquet COPY median, Q10 moved to about `0.103s`; latest 3-run full comparison is about `2.29s` Dodam vs `1.07s` DuckDB, total ratio about `2.14x`.
  - Generalized the Q18-style batch-local typed `orders` map build to Q03, Q05, Q07, and Q08 fast paths. This is a planner rule candidate: build filtered/semi-joined order lookup maps with local/final workers before probing `lineitem`. SF=1 standalone medians moved Q03 to about `0.097s`, Q05 to about `0.090s`, Q07 to about `0.104s`, and Q08 to about `0.096s`; latest 3-run full comparison is about `2.17s` Dodam vs `1.08s` DuckDB, total ratio about `2.01x`.
  - Extended the same typed batch-local `orders` map build to Q10 and Q12. Q10 was mostly neutral standalone but improved in full runs; Q12's orders probe moved from around `~0.095s` noise range to about `~0.078s` in the latest full run. Re-tested parallel Q04 late-candidate set union, but it stayed around `~0.22s`, so the mutable dense marker path remains better. Latest SF=1 3-run full comparison is about `2.14s` Dodam vs `1.08s` DuckDB, total ratio about `1.97x`.
  - Fixed Q04 fast-path routing for the `EXISTS (SELECT * FROM lineitem ...)` shape. The matcher previously reused the comma-join parser helper, which returns `None` for a single subquery `FROM`, so Q04 silently fell back to the generic correlated path. After routing correctly, SF=1 Q04 profile is roughly `37ms` candidate orders + `27ms` lineitem late probe, and full CLI runs show Q04 around `0.10-0.11s` instead of the old `~0.22s` path.
  - Added env-gated TPC-H stage profiling with `DODAM_TPCH_PROFILE=1`. Profiling is off by default and avoids taking stage timers unless enabled. This is diagnostic only, but it exposed Q09 `orders` year-map build, Q18 grouped lineitem sum, and Q21 final-order/lineitem state as the next structural costs.
  - Added a Q11 important-stock fast path: reduce `nation` to matching nation keys, reduce `supplier` to matching supplier keys, then scan `partsupp` once to compute both grouped `ps_partkey` value sums and the scalar total threshold. This turns the duplicated join/aggregate scalar-subquery shape into one filtered grouped pass. On SF=1 CLI median, Q11 moved from about `0.114s` to the CLI floor around `0.032s`, effectively matching DuckDB for this case.
  - Chunked high-cardinality grouped numeric sum partials so one worker partial handles multiple input batches instead of producing one `HashMap` per batch. This reduces Q18's `grouped numeric sum` partial count from `742` to `47`; standalone Q18 showed a noisy but positive median around `0.187s` in one 7-run check, while full 3-run CLI comparison still reports Q18 around `0.214s`. Keep this as a modest structural cleanup, not a decisive Q18 fix.
  - Latest SF=1 3-run CLI comparison after Q04 routing, Q11 fast path, and grouped-sum chunking: Dodam about `2.37s`, DuckDB about `1.38s`, total ratio about `1.72x`. Remaining largest gaps are Q18 (`~3.3x`), Q09 (`~1.9x`), and the broad CLI/startup/scan floor visible as many queries clustered around `0.11s` vs DuckDB `0.064s`.
  - Replaced the Q18 generic grouped-sum HashMap path with a threshold-aware dense `orderkey -> quantity sum` accumulator and HashMap fallback for wide/non-typed keys. This avoids per-batch high-cardinality maps and only materializes qualifying orders after the lineitem scan. SF=1 standalone Q18 median moved to about `0.115s` from the prior `~0.18-0.21s` range; Q18's lineitem stage profile moved to roughly `99ms`.
  - Replaced Q09's `orders` year HashMap with an adaptive dense year vector and fallback HashMap. This avoids building/cloning a 1.5M-entry map and removes HashMap lookups from the lineitem profit loop when order keys fit the dense range. SF=1 Q09 profile moved the order-year stage from about `119ms` to about `24ms`; standalone Q09 median moved to about `0.139s` from the prior `~0.214s` range.
  - Latest SF=1 3-run CLI comparison after the Q18/Q09 dense accumulators was noisy (`~2.38s` Dodam vs `~1.38s` DuckDB, `~1.72x`) because several normally floor-level queries sampled at `~0.11s`; rely on standalone Q09/Q18 medians for this specific change and rerun longer full comparisons before claiming a new total ratio.
  - Replaced Q21 final-order `HashSet` with an adaptive dense marker and fallback HashSet. Q21 final-order profile moved from roughly `70ms` to `17ms`, and standalone Q21 median moved to about `0.143s`; full 5-run CLI comparison remained noisy around `~1.69x` total because several floor-level queries sampled high.
  - Generalized the repeated dense/fallback integer-key pattern into shared `AdaptiveI64Set` and `AdaptiveI64Map` helpers, then rewired Q09 order-year lookup, Q18 quantity accumulator, and Q21 final-order membership to use them. This keeps the same dense-fast/HashMap-fallback behavior without query-local data-structure copies. SF=1 standalone medians after the generalization were roughly Q09 `0.133s`, Q18 `0.127s`, Q21 `0.133s`; latest 3-run full comparison was about `2.31s` Dodam vs `1.38s` DuckDB (`~1.67x`).
  - Re-applied Q18 customer late materialization after the lineitem accumulator improvements. Q18 now derives the needed `custkey` set from qualifying orders, stores only those customer names, and stops scanning once all are found. Customer-name profile moved from roughly `29ms` to `8ms`, Q18 standalone median moved to about `0.113s`, and latest 3-run full comparison was about `2.28s` Dodam vs `1.43s` DuckDB (`~1.59x`).
  - Restored typed input loops on top of the generalized adaptive containers and optimized `AdaptiveI64Map::insert` to avoid the generic update path. Q21 final-order stage returned to roughly `18ms`, Q21 standalone median was about `0.125s`, and Q09 order-year profile improved to roughly `30ms`. Latest 3-run full comparison was about `2.23s` Dodam vs `1.38s` DuckDB (`~1.61x`), with Q09/Q21 still showing noisy `~0.164s` full-run medians despite lower standalone medians.
  - Added `DODAM_PROFILE_QUERY=1` for plain `query` CLI timing and used it to split copy-parse/direct-sink/streaming/execute/stdout time. The direct/streaming/write probes are sub-millisecond for TPC-H fast paths; remaining time is inside execution rather than stdout.
  - Limited Dodam's default Rayon global thread pool to `min(available_parallelism, 16)` when `RAYON_NUM_THREADS` is not set. On the 32-core test host, explicit thread-count sweeps showed 32-way default over-parallelizes these SF=1 fast paths; 16 threads improved standalone medians such as Q01 (`~0.096s -> ~0.081-0.088s`), Q09 (`~0.135s -> ~0.119-0.135s` depending on run), and Q21 (`~0.129s -> ~0.114-0.125s`). Full per-process TPC-H comparisons remain noisy around `~1.5-1.6x`, but the thread cap removes a real scheduling overhead source and still allows users to override with `RAYON_NUM_THREADS`.
  - Rejected chunking Q09/Q21 lineitem batch partials into fewer worker tasks: task count dropped from `742` to `93`, but larger local HashMaps made Q09/Q21 standalone profiles worse. Also rejected a sequential dense `orderkey -> Q21OrderState` array for Q21 because it regressed the lineitem state stage to roughly `285ms`.
  - Added a general `year_from_days` helper for `EXTRACT(YEAR FROM Date32)` hot paths so Q07/Q08/Q09 no longer materialize month/day when only the year is needed. Q09 repeated profiles after the change show the order-year stage around `24-34ms` and lineitem profit around `54-60ms`; latest SF=1 full run stayed correct (`22/22`) with Q09 around `0.114s`.
  - Re-tested read batch size after the Rayon cap and dense-key changes. On current SF=1 TPC-H real-data runs, `8192` totaled about `1.69s`, while `16384` and `32768` were around `1.51s`; repeated runs were steadier at `32768` (`~1.53-1.55s`). The CLI default batch size was raised to `32768` as a general scan/task-overhead reduction, while `--batch-size` remains available for smaller-memory runs.
  - Optimized Q01's small categorical group lookup by using single-byte group keys for `l_returnflag`/`l_linestatus` before falling back to string lookup. With the new `32768` default batch size, repeated Q01 aggregate profiles moved from roughly `65-69ms` to `51-64ms`; the latest full run stayed correct (`22/22`) with Q01 around `0.088s`.
  - Replaced Q01's batch-local string group lookup with fixed single-byte group slots and string overflow fallback. The incremental gain over the prior compact lookup is modest: Q01 aggregate profiles moved from roughly `59-64ms` to `54-61ms`.
  - Added a typed `Int64`/`Date32` path for Q04 candidate order priorities. Q04 candidate order scan moved from roughly `49-51ms` to `18-22ms`, and the late lineitem probe also sampled lower (`~30-33ms`); full-run Q04 moved around `~0.07s`.
  - Replaced Q18's quantity sum accumulator for the dense typed path with a plain `Vec<f64>` and HashMap-compatible fallback, removing the separate `present` bitmap and closure update from the hot path. Q18 lineitem quantity sums moved from roughly `~102ms` to `~66-71ms`.
  - Reworked Q22 small-domain execution: `orders.o_custkey` uses an adaptive dense set, country-code grouping uses seven fixed slots, and customer acctbal scans use typed decimal loops. Q22 standalone moved from roughly `~0.06s` to `~0.03s`; latest SF=1 full run was `22/22` at about `1.62s` total, roughly `~1.38x` the latest DuckDB stdout comparison (`~1.17s`).
  - Generalized the Q18 dense integer-key `f64` sum pattern into a reusable `DenseI64F64Sum` helper with dense `Vec<f64>` storage and `AdaptiveI64Map` fallback. Keeping a prevalidated dense-index update path avoided most abstraction overhead: Q18 lineitem quantity sums stayed in the same rough range (`~69-73ms` in the latest profile), and the latest SF=1 full run was `22/22` at about `1.58s`.
  - Started extracting typed row-iteration helpers from query-local fast paths. Q04 candidate order scan now uses a reusable `Int64 + Date32 + String` iterator and stayed in the improved `~17-20ms` range; Q10 order-customer build now uses a reusable `Int64 + Int64 + Date32` iterator and stayed around `~10-11ms`. Latest SF=1 full run remained `22/22` at about `1.63s`, with run-to-run noise larger than the helper refactor cost.
  - Added a reusable dense `i64 -> i32` map with sentinel values and `AdaptiveI64Map` fallback, then used it for Q09 order-year lookup. This removes the separate `present` bitmap from the hot dense path. Q09 order-year build moved from a noisy `~54ms` sample to repeated `~20-22ms`; latest full run stayed `22/22`, with Q09 around `0.109s`.
  - Packed Q21 per-order state flags and changed `late_row_count` from `u64` to `u32`, reducing the HashMap value size from 32 bytes to 24 bytes. Q21 lineitem state profiles improved from roughly `~96-100ms` to repeated `~74-78ms` after warmup. Rejected preallocating each batch-local Q21 HashMap at `rows/2`: it regressed the same stage to `~106-112ms`, likely from excessive allocation/cache pressure.
  - Late-materialized Q07 nation labels by grouping on `(supp_nation_key, cust_nation_key, year)` and converting keys to strings only for the final output rows. This removes per-lineitem nation-name lookup, string-pair checks, and string clones from the hot revenue loop. Q07 volume aggregate profiles moved from roughly `~81ms` to repeated `~43-52ms`; latest full run stayed `22/22`, with Q07 around `0.098s`.
  - Replaced Q12's per-order nested `HashMap<String, u64>` shipmode counts with fixed small-domain slots derived from the `l_shipmode IN (...)` literals, and used fixed slots for the final high/low count groups as well. This is the same general small-IN-list-to-slot pattern used elsewhere rather than a data-value shortcut. Q12 lineitem aggregate profiles moved from roughly `~43ms` to repeated `~31-38ms`; latest full run stayed `22/22`, with Q12 around `0.044s` in one sample.
  - Added an `AdaptiveI64Map::dense_slices()` view and used it in Q12's null-free typed orders loop to avoid a generic `get()` call and repeated `Option`/bounds plumbing for dense pending-order probes. Focused SF=1 Q12 15-run total moved from about `0.936s` to `0.766s` (`~18%` faster), with warm repeats mostly `~46-50ms`.
  - Added scan-profile logging for the generic `i64_set_filter` Parquet path. Re-tested Q12 order-side row filtering after the dense probe change: it reduced orders output rows but still lost to the default dense-probe path (`~0.806s` vs `~0.766s` over 15 focused repeats), so it remains opt-in rather than default.
  - Added a generic Parquet `i64` dynamic Bloom row-filter path and wired Q12 orders to `DODAM_Q12_ENABLE_ORDER_BLOOM_FILTER=1`. With a 64-bit/key Bloom budget it reduces orders scan output to roughly exact-filter cardinality, but focused SF=1 Q12 stayed noisy (`~0.619-0.635s` over 12 repeats) and did not beat the dense-probe default consistently, so it remains opt-in. The remaining blocker is Arrow `RowFilter` setup/read overhead, not Bloom false positives.
  - Tested using the reduced Q12 orders output more aggressively by adding a fused row-group-map aggregate path (`DODAM_Q12_ENABLE_ORDER_FUSED_SCAN=1`) and by prototyping a Bloom key-capture/payload-only scan. Both regressed: fused row-group-map sampled about `0.685s` over 12 repeats vs the stream dense-probe path around `0.603s`, and key-capture/payload-only sampled about `0.693s`. The main lesson is that Q12's current `orders` full scan is already a tight dense-probe loop, while Arrow RowFilter/selection, side-buffer handling, and row-group-map chunk setup add more overhead than the cheap low-cardinality `o_orderpriority` decode saves.
  - Added sampled Q12 orders-side detail profiling under `DODAM_TPCH_PROFILE=1`: it reports typed/fallback batches, lookup hit/miss counts, priority/apply row counts, and sampled phase timings. A 3-repeat SF=1 run showed `orders` scan/decode at roughly `23-28ms` warm while orders aggregation wall time was only about `0.9-1.4ms`; sampled dense lookup was around `0.16-0.18ms` for 5,860 samples, with priority/apply much smaller. This reinforces that Q12's remaining default-path cost is Parquet decode/materialization and scan scheduling, not the dense-probe aggregate loop.
  - Expanded Parquet scan profiling with projected column count, zero-row batches, and per-`next()` average/max latency. Q12 `orders` warm profile showed `o_orderkey,o_orderpriority` scan has no zero-row batches and `parquet_next_avg` around `0.35-0.40ms` with max around `1.3-1.6ms`; the cost is broad batch decode/materialization rather than a few pathological `next()` calls.
  - Re-tested Q12 order key-first late materialization and fused row-group-map after adding a shared fast high-priority byte predicate. Default dense-probe path stayed best: 8-repeat SF=1 default warm was about `46-50ms`, late materialization about `52-55ms`, and fused about `49-51ms`. Keep key-first/fused opt-in for now; the selector/payload second pass still costs more than decoding the low-cardinality priority column during the full orders scan.
  - Added a one-reader dictionary-preserved scan path for low-cardinality string payloads and enabled it for Q12 `orders.o_orderpriority` by default (`DODAM_Q12_DISABLE_ORDER_DICTIONARY_PRIORITY=1` restores the old full `StringArray` path). This keeps a single `orders` reader pass but avoids full string materialization for the priority column. SF=1 Q12 8-repeat total improved from about `0.443s` to `0.418s` (`~5.7%`), with warm repeats around `44-51ms`; profile shows each 4-row-group orders chunk reads in roughly `5-7ms` warm. This is a generally useful rule candidate for low-cardinality string columns consumed as equality/classification predicates.
  - Tried pushing the Q12 `orders` dictionary-preserved path further into a row-group-map pipeline so scan workers aggregate immediately instead of sending `RecordBatch`es through the stream. This regressed in the focused SF=1 8-repeat run: dictionary stream was about `0.420s`, while dictionary row-group pipeline was about `0.470s`. Keep `DODAM_Q12_ENABLE_ORDER_FUSED_SCAN=1` opt-in only. The likely cause is that the stream path already overlaps scan and aggregate well, while row-group-map adds worker setup/partial result ordering overhead and does not eliminate Arrow array materialization.
  - Parallelized Q02's `partsupp` minimum-cost pass with batch-local/final partials and typed `Int64`/decimal loops, while sending only filtered part/supplier key sets to workers instead of cloning supplier payloads. Q02 `partsupp` stage moved from roughly `~30ms` to `~5ms`; latest SF=1 3-run CLI comparison moved Q02 to about `0.026s` vs DuckDB about `0.039s`, and total median-sum to about `1.44s` vs `1.08s` (`~1.34x`).
  - Added a Q16 parts/supplier relationship fast path: parse the `p_size IN (...)`, `p_brand <>`, `p_type NOT LIKE prefix%`, and supplier comment `LIKE` literals, reduce `part` into `partkey -> group_id`, then parallelize `partsupp` into batch-local group distinct-supplier sets. Q16 standalone moved from about `0.069s` to about `0.047s`; latest SF=1 3-run CLI comparison total moved to about `1.42s` vs DuckDB about `1.08s` (`~1.31x`).
  - Added a reusable chunked parallel batch fold and applied it to Q01/Q06/Q19 lineitem-heavy fast paths where one task per RecordBatch was too fine-grained. Q01 aggregate profiles moved from roughly `~62ms` to `~53-55ms`, Q06 from roughly `~42ms` to `~36-38ms`, and Q19 stayed in the high `~40ms` to low `~50ms` range with fewer tasks. The full 3-run CLI comparison remained noisy around `~1.32x`, but 9-run standalone medians were Q01 about `0.087s`, Q06 about `0.049s`, and Q19 about `0.085s`.
  - Rejected pushing Q06's date/discount/quantity filter into `scan_parquet_batches`: row-group count dropped from `212` to `184`, but residual filtering cost dominated and Q06 regressed from roughly `~42ms` to `~130ms`. For current fast paths, typed in-loop filtering is cheaper unless pruning removes far more data or scan residual filtering becomes vectorized/cheaper.
  - Rejected replacing Q19's small `l_shipmode IN (...)` HashSet with sorted Vec/linear matching: repeated Q19 profiles stayed flat or slightly worse (`~52-60ms`), so the hot cost is more in part-mask lookup/scan arithmetic than shipmode hashing.
  - Extended the chunked batch-fold pattern to Q03/Q05/Q07/Q20 lineitem aggregates and Q03/Q07 orders-side map builds. Q03 revenue moved from roughly `~36ms` to warm `~30-32ms`, Q05 revenue from roughly `~43ms` to warm `~36-39ms`, Q07 order-customer build from roughly `~18ms` to `~15-16ms`, and Q20 lineitem quantity from roughly `~28ms` to `~23ms`; Q07 volume was mostly neutral/noisy but kept consistent with the same task-overhead reduction pattern.
  - Added non-null typed loops for Q01/Q06/Q19 hot paths by exposing `Q01DecimalInput::null_count()`. The gain is modest but measurable where the input data is non-null: latest 7-run standalone medians were Q01 about `0.077s`, Q03 about `0.077s`, Q05 about `0.069s`, Q06 about `0.047s`, Q07 about `0.083s`, Q19 about `0.082s`, and Q20 about `0.069s`.
  - Extended order-side chunking to Q05 and Q08 and chunked Q08's market-share lineitem aggregate. Q05 order-customer nations now runs around `~9-12ms`; Q08 order-year build around `~11-13ms`; latest 7-run standalone medians after the retained changes were Q03 about `0.077s`, Q05 about `0.072s`, Q07 about `0.082s`, and Q08 about `0.080s`.
  - Rejected converting Q03/Q05/Q07/Q08 order lookup maps to `AdaptiveI64Map`: probe stages dropped dramatically, but dense vector materialization/conversion cost was outside the stage timer and made CLI medians regress badly, e.g. Q03 sampled around `~0.15s`. Keep this pattern only where the dense structure is built incrementally without a large post-build conversion, or where conversion can be fused with the producer.
  - Reworked Q01 partial merging to keep batch-local fixed group slots until the final merge, avoiding repeated conversion through `Vec<Q01Row>`. Also added a single-byte string offset/data fast path for Q01 group keys. Q01 aggregate profiles now sample around `~52ms` on warm runs, though CLI medians remain noisy around `~0.08s`.
  - Converted small customer/supplier lookup inputs for Q03/Q05/Q07/Q08 to adaptive dense integer probes while leaving large order lookup maps as HashMaps. This keeps the earlier rejected large-map conversion out of the hot path, but reduces repeated membership probes during order scans. Q03 order rows sampled around `~20ms`; Q07 order-customer rows around `~13ms`.
  - Added typed Decimal128/Date32/Int64 lineitem loops with non-null branches for Q03 and Q07, and added non-null branches for Q05/Q08/Q20 typed lineitem loops. Q03 revenue aggregate moved to roughly `~24ms`, Q07 volume aggregate to roughly `~32ms`, and Q20 quantity aggregate to roughly `~23ms` in focused profiles. Latest SF=1 5-run median-sum comparison stayed correct at `22/22` and sampled around Dodam `1.40-1.41s` vs DuckDB `1.08-1.09s` (`~1.29-1.30x`).
  - Rejected raising the chunked batch-fold size from `4` to `8` globally: Q06 improved slightly, but Q03/Q05/Q07 regressed from coarser tasks and larger local maps. Kept the larger chunk only for Q06's small-state filtered revenue sum.
  - Added raw byte/string matching for Q19's typed lineitem path so matched rows compare `l_shipmode`/`l_shipinstruct` without `StringArray::value()` and `HashSet<String>` lookup. Q19 lineitem profiles moved from roughly `~52ms` to about `~50ms`; standalone median improved modestly to the mid/high `~0.08s` range.
  - Split Q20 profiling into forest-part, lineitem, partsupp eligibility, nation, supplier, and final-sort stages. This showed the hidden bottleneck was `partsupp` eligibility, not output or final supplier lookup.
  - Added a typed `Int64/Int32` Q20 `partsupp` eligibility loop and converted the small forest part key set to `AdaptiveI64Set` for repeated `lineitem`/`partsupp` probes. Q20 eligible suppliers moved from roughly `~23ms` to `~2.8ms`, and Q20 standalone median moved from about `~0.068s` to about `~0.049s`, nearly matching DuckDB on the current SF=1 fixture.
  - Rejected a raw-byte typed path for Q20 `p_name LIKE 'forest%'`: the forest part scan stayed around `~12ms`, so the cost is likely scan/decode rather than string prefix comparison.
  - Latest SF=1 5-run median-sum comparison after Q19/Q20 work stayed correct at `22/22` and sampled around Dodam `1.39s` vs DuckDB `1.08s` (`~1.29x`); Q20 is no longer a major gap.
  - Generalized the Q20 forest-part adaptive set rule to other TPC-H fast paths that build an `i64` part-key membership set and then probe it heavily in `lineitem`/`partsupp`: Q08 market-share, Q09 supply-cost/profit, and Q17 small-quantity revenue now use `AdaptiveI64Set` instead of cloned `HashSet<i64>` in the hot probe loops. Focused medians stayed neutral-to-better: Q09 moved around `~0.114s -> ~0.109s`, Q17 around `~0.049s -> ~0.047s`, Q08 stayed roughly neutral, and Q20 remained around `~0.048s`. Latest SF=1 5-run median-sum comparison sampled Dodam `~1.37s` vs DuckDB `~1.08s` (`~1.27x`).
  - Added non-null dense loops for Q18 lineitem quantity sums and chunked Q21 lineitem order-state partial merging. Q18 quantity profiles moved from roughly `~75ms` to high `~60ms`/low `~70ms` samples; Q21 chunk size `8` reduced partial maps from `212` to `27` and sampled around `~72ms` for the lineitem state stage. Chunk size `16` was rejected because it reduced parallelism too much and regressed Q21.
  - Re-tested the Q01/Q06/Q19 scan-heavy gaps against DuckDB with Parquet COPY output. Retained Q01 chunk size `8` because it reduced Q01 aggregate profiles from roughly `~60-67ms`/`53` chunks to about `~53-55ms`/`27` chunks; chunk size `16` regressed to about `~58ms`. Retained Q06 raw decimal revenue multiplication, a small arithmetic cleanup that kept Q06 standalone around `~0.047s`. Retained Q19 raw-rule caching and single-mask matching, but reverted Q19 chunk size `8` because CLI medians were neutral-to-worse despite lineitem stage samples around `~48-50ms`.
  - Rejected Q01 raw decimal accumulation for `sum_disc_price`/`sum_charge`: replacing per-row `value()` conversion with raw integer products made focused Q01 medians noisier and generally worse (`~0.087s` vs the original f64 path around `~0.078-0.080s`). The likely issue is that f64 conversion cost is not the dominant Q01 cost after group-slot and chunking changes.
  - Latest SF=1 3-run Parquet COPY comparison after the retained Q01/Q06/Q19 changes stayed correct at `22/22` and sampled Dodam `~1.38s` vs DuckDB `~1.07s` (`~1.28x`). Largest remaining gaps are still scan-heavy queries: Q01 around `~1.72x`, Q06 around `~1.61x`, Q19 around `~1.57x`, and Q03/Q04/Q05/Q07/Q08/Q15/Q18 around `~1.4-1.5x`. This points to scan/decode/filter execution structure more than final Parquet write cost.
  - Added Q19 part-mask profiling and a typed non-null part-side byte path for `p_brand`/`p_container` plus integer `p_size` bounds. Q19 part-mask build moved from roughly `~11ms` to `~9ms`, and focused Parquet COPY median sampled around `~0.083s`. Kept the profile stage because it exposes an otherwise hidden Q19 cost.
  - Rejected a pruning-only Q06 scan path: row groups/chunks dropped (`53 -> 12` chunks), but Q06 regressed to roughly `~0.096s` because reader/task shape got worse than simply scanning and applying the existing typed loop. Also rejected larger CLI batch sizes for Q01/Q06/Q19; `65536+` generally regressed, especially Q06.
  - Rejected replacing Q01's tiny linear key scan with a 256-bucket lookup table. Focused aggregate profiles occasionally looked better, but Parquet COPY median regressed (`~0.087s` with buckets vs `~0.079s` after reverting), likely because the larger partial state hurt cache/merge behavior more than the 4-key scan helped.
  - Rejected changing Q18's dense `i64 -> f64` sum helper to one-pass dynamic dense growth. The lineitem profile sometimes looked slightly better (`~79-82ms` vs `~84-86ms`), but Parquet COPY medians did not improve and full comparison sampled Q18 worse, so the original two-pass dense validation/add path was restored.
  - Tuned chunk sizes where profiling showed tail latency rather than large local-map merge cost. Q03 order rows moved from chunk `4` to `1` and the stage dropped from roughly `~26ms` to `~17-18ms`. Q05/Q07/Q08 lineitem aggregates moved from chunk `4` to `2`; focused profiles improved Q05 revenue to roughly `~30-34ms`, Q07 volume to roughly `~32-34ms`, and Q08 market-share to roughly `~37-38ms`. Rejected Q03 revenue chunk `2` because it was neutral-to-worse with the larger revenue map.
  - Converted Q15 revenue aggregation from one task per batch to chunked fold with chunk size `8`. The stage moved from roughly `~44-47ms` over `212` batch tasks to about `~34ms` over `27` chunk tasks, and focused standalone median sampled around `~0.047s`.
  - Latest SF=1 3-run Parquet COPY comparison after Q03/Q05/Q07/Q08/Q15 chunk tuning and Q19 typed-byte work sampled Dodam `~1.386s` vs DuckDB `~1.086s` (`~1.28x`). Q15 improved to about `~0.046s`, Q19 to about `~0.084s`, and Q03 to about `~0.070s`; Q01/Q06 remain the largest pure scan gaps.
  - Added a Q21 final-order raw byte check for non-null `o_orderstatus = 'F'`, avoiding `StringArray::value()` in that scan. The focused improvement is small because Q21 remains dominated by the lineitem order-state stage. Rejected Q21 lineitem chunk `4` and Q15 raw decimal buffer loops as neutral-to-worse; restored Q21 chunk `8` and Q15's typed `value()` loop.
  - Added a Q10 top-customer `AdaptiveI64Set` lookup plus typed customer row materialization, and changed Q10's `l_returnflag = 'R'` hot check to compare UTF-8 offset/data bytes directly. Rejected converting Q10's larger `orderkey -> custkey` map to `AdaptiveI64Map`: stage timers improved, but full Parquet COPY regressed, likely from dense vector conversion/materialization outside the stage timer. Latest focused Q10 sampled around `0.078s` Dodam vs `0.066s` DuckDB.
  - Reworked Q18's dense quantity accumulator to validate each batch's dense key range and reserve once before the hot add loop. This restored the Q18 lineitem quantity stage to roughly `~69ms` in profile and focused Q18 around `~0.096s`. Rejected consecutive-key run compression on the same path because the extra branch/run tracking regressed focused Q18 to about `~0.103s` and the lineitem stage to about `~80ms`.
  - Latest SF=1 3-run Parquet COPY comparison after the retained Q10/Q18 changes sampled Dodam `~1.225s` vs DuckDB `~1.102s` (`~1.11x`). The largest remaining individual gaps were Q18 (`~1.50x`), Q06 (`~1.37x`), Q01 (`~1.33x`), Q03 (`~1.30x`), Q08 (`~1.26x`), and Q16 (`~1.23x`).
  - Tightened Q06's non-null decimal loop by reading `Date32`/`Decimal128` raw slices directly and retuned its chunk size from `16` to `8` after the loop got cheaper. Focused Q06 moved from about `~0.040s` to about `~0.0365s`, and the revenue stage moved to about `~29ms`; chunk `4` was tested and rejected as slightly worse. Added a Q01 contiguous single-byte UTF-8 key path, but it was mostly neutral in focused Parquet COPY runs.
  - Latest SF=1 3-run Parquet COPY comparison after the Q06 raw-slice/chunk retune sampled Dodam `~1.214s` vs DuckDB `~1.092s` (`~1.11x`). Q06 improved to about `~0.037s`, while the largest remaining gaps are Q18 (`~1.54x`), Q01 (`~1.38x`), Q03/Q07/Q08 (`~1.26-1.28x`), and Q10 (`~1.23x` in this noisy sample).
  - Rejected another order-side map preallocation/chunk retune pass for Q03/Q05/Q07/Q08/Q10. Some stage timers improved, but full SF=1 Parquet COPY did not improve (`~1.22s` sampled), so the code changes were reverted. Also rejected Q18 ordered raw-decimal streaming aggregation: it avoided dense-vector final scanning in theory, but focused Q18 regressed to about `~0.099s` vs the retained dense f64 path around `~0.096s`.
- Reduced Q01's fast-path aggregate state by removing the redundant per-aggregate count fields. In this fast path the quantity, price, discount, and `count(*)` counts advance together, so `count_order` is enough for the three averages. Focused Q01 aggregate profile moved to about `~50ms`, and the latest SF=1 3-run Parquet COPY comparison sampled Dodam `~1.216s` vs DuckDB `~1.103s` (`~1.10x`).
- Added an in-process TPC-H runner and an opt-in Parquet range/chunk buffer cache:
  - runner: `target/release/tpch_real_inprocess --data-dir /tmp/dodam-tpchgen-sf1 --output-dir /tmp/dodam-tpch-inprocess-sf1`
  - file cache env: `DODAM_FILE_CACHE_BYTES=<bytes>`
  - chunk size env: `DODAM_FILE_CACHE_CHUNK_BYTES=<bytes>`, default `512KiB`
  - Same-session one-pass SF=1 Parquet output comparison sampled DuckDB around `0.60-0.70s` and Dodam around `0.87-0.89s`; this removes per-query process startup from both sides and shows Dodam is still about `~1.4x` slower in this shape.
  - Rejected full-file caching because it defeats projection pushdown by reading unused columns; it improved warm second-pass timing but regressed first-pass and total repeat timing.
  - Replaced it with a `ChunkReader::get_bytes` range cache that stores fixed-size file chunks. This keeps projection pushdown intact and reuses only byte ranges actually requested by the Parquet reader.
  - On SF=1 one-pass in-process TPC-H with `512MiB` cache, default `512KiB` chunks sampled about `0.875s` Dodam vs `0.986s` cache-off in the same run shape. Chunk sweep showed `512KiB` better than `256KiB`, `1MiB`, and `2MiB` for this workload.
  - Improved cache accounting and hit behavior:
    - added hit/miss/eviction/read-byte counters to the in-process runner output
    - added single-chunk zero-copy `Bytes::slice` return to avoid copying common reads
    - changed LRU touch to happen only under cache pressure, avoiding per-hit `VecDeque` scans when the cache has enough headroom
    - changed the range cache from one global lock to 64 sharded maps with approximate LRU state:
      - cache counters and per-entry access epoch are atomics
      - hit path takes only the shard read lock and updates the entry epoch
      - miss/insert/eviction takes only the target shard write lock and opportunistically samples shards for old entries
      - pure atomics are used where they fit, but `HashMap` insertion/removal still needs synchronization
  - Cache-size sweep showed that undersized cache is harmful: `64MiB` and `128MiB` caused thousands of evictions and re-read gigabytes of chunks. The sharded approximate eviction reduced read amplification versus the first sharded attempt (`64MiB` about `10GB -> 3GB` read bytes), but small caches are still slower than cache-off on local warm files. `256MiB` is close, while `512MiB` avoided evictions on the current SF=1 dataset. Keep the cache opt-in until there is a better admission policy.
  - Added pressure-gated second-touch admission:
    - while cache usage is below about 75% of budget, first-touch chunks are admitted normally
    - under pressure, a chunk is cached only after a second request, reducing churn from one-time sequential reads
    - the `64MiB` cache remained slower than cache-off but improved in sampled total time versus the sharded no-admission run; `256MiB` also improved, while `512MiB` is unaffected because it does not enter pressure on the current SF=1 data
  - Tightened Q06 after the cache work:
    - Decimal128 inputs with precision `<= 18` now use an i64 raw-value multiply path in the no-null Q06 loop, avoiding i128 multiplication for common TPC-H-style decimal columns while keeping the generic i128 path for wider decimals.
    - Retuned Q06's chunked fold default from `8` to `4` after the cheaper loop made finer-grained work distribution better again.
    - Focused Q06 stage samples moved to roughly `~28-29ms` median with `47` chunks, and 5-run in-process full TPC-H remained around `~0.85s` median after the change.
  - Added a small `Date32 -> year` cache for repeated year extraction in Q07/Q08/Q09 hot paths:
    - Q09 `orders` year-map build also gained a no-null raw-slice path for `Int64` order keys and `Date32` order dates.
    - In-process SF=1 profile moved Q09 `order years` from roughly `~25ms` to `~15ms`; full 5-run in-process TPC-H sampled around `~0.84-0.85s` total.
    - Q07/Q08 reuse the same cache inside their per-batch year extraction loops; the effect is smaller but neutral-to-positive.
  - Rejected two dense/parallel state attempts after measurement:
    - A single dense `orderkey -> Q21OrderState` accumulator removed partial HashMap merging in theory but regressed Q21 lineitem state to about `~171ms` and full in-process total to about `~0.94-0.96s`.
    - Batch-local/final parallelization of Q09 `order years` created large dense vectors per partial and regressed the stage to about `~124ms`; the retained path keeps one incremental dense map plus the Date32 year cache.
  - Re-tested Q21 sorted-input streaming:
    - `lineitem` is physically ordered by `l_orderkey`, but the normal parallel reader returns batches out of order.
    - Forcing a sequential reader allowed streaming order-state flushing, but Q21 regressed to about `~99ms` because lost parallel Parquet decode outweighed avoiding HashMap partial merges.
    - Rejected this path; Q21 keeps the parallel state-merge path.
  - Added threshold-crossing tracking to the dense `i64 -> f64` sum helper used by Q18:
    - For non-negative inputs, keys are recorded when their running sum first crosses the `HAVING sum(...) > threshold` value, avoiding a full dense-vector scan at finalization.
    - If a negative value appears or the helper falls back to the generic map path, finalization uses the existing full predicate check for correctness.
    - Focused Q18 profile sampled `lineitem quantity sums` around `~55-56ms` versus prior `~58ms`; full in-process 5-run TPC-H stayed in the same `~0.84-0.85s` band.
  - Revisited Q09 composite supply-cost lookup:
    - Tried replacing `(partkey, suppkey)` tuple keys with packed `u64` keys plus tuple fallback for out-of-range integer keys.
    - Rejected it because Q09 profit stage stayed neutral-to-worse (`~44-46ms` samples); packing/probe overhead did not beat the existing tuple HashMap path.
    - Also rejected a Q09 raw `Decimal128` no-null profit loop; it regressed the focused profit stage versus the existing `value()` path.
    - Retained a small ownership cleanup: `q09_profit_rows` now takes the supply-cost map by value and moves it into `Arc`, avoiding a full HashMap clone before the lineitem stage.
  - Added opt-in scan-level profiling with `DODAM_SCAN_PROFILE=1`:
    - Wraps executed scan streams and prints per-scan wall elapsed, rows, batches, row groups, compressed bytes, metadata/planning/decode/filter/projection/limit time.
    - Decode time is cumulative worker time, so it can exceed wall elapsed on parallel Parquet scans; this is useful for separating decode work from downstream compute/merge time.
    - Initial SF=1 in-process observations:
      - Q21 lineitem scan (`l_orderkey,l_suppkey,l_receiptdate,l_commitdate`) walls around `~11ms` with cumulative decode around `~127ms`, while the Q21 query remains around `~85ms`; the remaining gap is therefore mostly state build/merge rather than scan wall time.
      - Q18 lineitem quantity scan walls around `~52ms` with cumulative decode around `~87ms`; Q18 remains scan/dense-accumulator dominated.
      - Q09 lineitem profit scan walls around `~36ms` with cumulative decode around `~448ms`; Q09's lineitem stage still mixes expensive decode and lookup/aggregate work.
  - Improved Q21 lineitem state build:
    - Batch-local Q21 state construction now exploits contiguous `l_orderkey` runs in the non-null typed path, so it updates the batch HashMap once per orderkey run instead of once per lineitem row.
    - Replaced Q21's `HashMap<i64, Q21OrderState>` with a local fast integer-key hasher, matching the approach already used in join/aggregate hot paths.
    - After the faster hasher, retuned Q21's chunked fold default from `8` to `16`; focused samples moved Q21 lineitem state construction from roughly `~69ms` to the `~44-50ms` band, and Q21 end-to-end samples moved from the `~82-86ms` band toward `~60-66ms`.
    - Latest 5-run in-process full TPC-H after the Q21 change sampled around `0.810-0.837s`, median about `0.821s`.
  - Generalized the fast integer hasher:
    - Moved the duplicated join/aggregate/Q21 integer-key hasher into `src/hash.rs` as `FastHasher`, `FastHashMap`, and `FastHashSet`.
    - Rewired physical join maps/sets, grouped aggregate maps, Q21 order-state maps, and adaptive dense fallback hash maps/sets to use the same implementation.
    - SF=1 Parquet-output 3-run in-process median sum moved from about `782.9ms` to `777.3ms` (`~0.7%` faster), so this is a cleanup plus small win rather than a structural fix.
    - Improvements in that sample: Q04 `27.8ms -> 24.9ms`, Q03 `40.0ms -> 37.2ms`, Q10 `49.7ms -> 47.9ms`, Q06 `23.8ms -> 22.4ms`, Q09/Q08/Q07 small wins.
    - Regressions/noise in that sample: Q18 `67.7ms -> 69.9ms`, Q16 `29.4ms -> 31.4ms`, Q21 `60.2ms -> 61.3ms`, Q13 `33.9ms -> 34.9ms`; keep watching before expanding the hasher blindly to every SQL-local map.
    - Current DuckDB CLI comparison under the same SF=1 Parquet input/output shape after this change: Dodam median-sum about `777ms`, DuckDB about `561ms`, ratio `~1.39x`. Largest remaining ratios are Q16/Q06 (`~2.2x`), Q22 (`~2.0x`), Q18 (`~1.8x`), Q08/Q07/Q05 (`~1.7x`).
  - Improved Q16 `COUNT(DISTINCT ps_suppkey)`:
    - Replaced the previous `HashMap<group_id, HashSet<suppkey>>` partial state with a single `FastHashSet<(group_id, suppkey)>`, then counted distinct pairs per group at finalization.
    - This keeps exact distinct semantics while avoiding many small per-group sets and reducing merge work.
    - Interned Q16 `p_brand`/`p_type` strings during `part` filtering and used an integer `Q16GroupIdKey` for group lookup, avoiding per-row cloned string keys for repeated group-id probes.
    - Rejected for now: a Q18 sorted-key streaming aggregate. The idea is generally useful, but the current parallel Parquet scan does not preserve sorted batch order, so the path fell back to a second dense scan and regressed Q18. A future version needs scan-order guarantees or an ordered scan mode before enabling sorted streaming aggregates.
    - SF=1 Parquet-output 3-run median without profiling: Q16 moved from about `31.4ms` to `24.2ms` (`~23%` faster). Overall TPC-H median-sum stayed roughly flat (`777.3ms -> 776.9ms`) because the remaining large gaps are in other queries and run-to-run noise dominates sub-10ms total changes.
    - Current largest DuckDB gaps after this Q16 work: Q06 `~2.33x`, Q22 `~1.95x`, Q18/Q03/Q07/Q04/Q16/Q08 in the `~1.7-1.8x` band; Q16 is no longer the top outlier.
  - Clarified filter pushdown vs row-group pruning:
    - Q06 fast path previously did not pass a `FilterExpr` into scan; it performed condition checks inside the specialized raw loop.
    - Trying generic scan filter pushdown for Q06 enabled row-group pruning plus row filtering, but the SF=1 `lineitem.l_shipdate` row-group min/max spans almost the full date range in every row group, so pruning scanned `53/53` row groups.
    - The result regressed badly (`~23ms -> ~72ms`) because all data was still decoded and generic Arrow filtering added about `~13ms`; this path was reverted.
    - Important rule: fast paths need row-group pruning and row-level filtering separated. Pruning-only is useful when metadata actually eliminates row groups; generic row filtering can be slower than a fused specialized loop when no pruning happens.
  - Improved pruning execution structure:
    - Removed the old `ParquetScanExec` behavior that forced all scans with `pruning_predicates` onto `SequentialFragmentScanStream`.
    - `plan_parquet_scan_tasks` now receives pruning predicates in the normal parallel path, so row-group pruning can still produce parallel row-group tasks.
    - Added `DodamEngine::scan_parquet_batches_pruned` for fast paths that want pruning-only behavior: use predicates for Parquet row-group selection, but do not attach a residual `FilterExec`.
    - Applied pruning-only range predicates conservatively to Q03/Q04/Q18 probe scans when the selected `orderkey` set has a narrow min/max range. The current SF=1 TPC-H data has order keys spread across almost the full range, so it usually scans all row groups; the rule is mainly for clustered or selective datasets.
    - Also rejected Q18 batch-local run aggregation: collecting `(orderkey, sum)` runs before dense updates regressed Q18 (`~66.8ms -> ~73.9ms`) because Vec/run bookkeeping cost outweighed fewer dense updates.
    - SF=1 Parquet-output 3-run median after the parallel-pruning structure and conservative range pruning sampled about `771.6ms` versus the prior `776.9ms`; this is a small/noisy win, not the next structural breakthrough.
  - Retuned Q01 chunking:
    - Added `DODAM_Q01_CHUNK_SIZE` and changed the default from `8` batches/chunk to `4`.
    - Sweep samples: chunk `4` was best among `2/4/8/16`; chunk `16` increased merge wait, while chunk `2` created too many tasks.
    - Profile-free 3-run median sample moved Q01 from about `45.1ms` to `42.8ms`, with full TPC-H median-sum around `764.1ms` in that run.
  - Improved Q22 customer scan reuse:
    - Replaced the two customer scans (`avg(c_acctbal)` input and final customer grouping input) with one scan that records `(custkey, country_index, acctbal)` candidates and computes the average at the same time.
    - After the orders customer-key set is built, final Q22 grouping runs over the in-memory candidates instead of scanning customer again.
  - Re-profiled the current largest DuckDB gaps under the one-process Parquet COPY comparison:
    - Current 5-run median-sum sample before this pass: Dodam about `754ms` vs DuckDB about `566ms` (`~1.33x`).
    - Largest absolute gaps were Q18 (`+25ms`), Q08 (`+20ms`), Q01 (`+17ms`), Q07/Q03/Q05 (`+14-15ms`), and Q06 (`+13ms`).
    - Q18 is dominated by the ordered `lineitem[l_orderkey,l_quantity]` scan (`~49ms`), Q08 by the 5-column lineitem scan (`~31ms`), and Q01 by the 7-column lineitem full scan/aggregate (`~54ms` scan wall in the profiled run). The common remaining gap is Parquet scan/decode/late-materialization structure, not final Parquet write.
  - Added an env-tunable parallel Parquet row-group task size (`DODAM_PARQUET_SCAN_ROW_GROUP_CHUNK`) instead of a hard-coded `4` row groups per scan task.
    - Sweep result: global `4` still had the best total in the current 3-run sample (`~748ms`), while `1`/`2`/`8` regressed total.
    - Query-level behavior was mixed: `8` helped Q01/Q06/Q14 but hurt Q18/Q08/Q03/Q05/Q10/Q12, so this is useful as a diagnostic/tuning knob but not a better global default.
  - Added key-range row-group pruning to Q10's final customer payload lookup using the already-known top customer key set.
    - The current SF=1 customer data still scans `4/4` row groups because the key range spans the file, so the full 5-run total was effectively neutral (`~753.9ms -> ~753.6ms`).
    - Retained because it is the same general selective-key lookup rule used elsewhere and should help clustered/selective customer datasets without changing semantics.
  - Re-tested read batch size after the cache and scan changes:
    - `16384` sampled best in the current 3-run comparison (`~739ms`) versus `32768` (`~750ms`) and `65536` (`~807ms`).
    - Lowered the CLI and TPC-H in-process default batch size from `32768` to `16384`; `--batch-size` remains available for workload-specific tuning.
  - Added an experimental scan-level Parquet `RowFilter` hook and a `DODAM_Q06_ROW_FILTER=1` Q06 trial path:
    - Naive split predicates regressed Q06 badly (`~24ms -> ~62ms`) because `l_shipdate` and `l_discount` were decoded more than once.
    - A combined predicate with final projection reduced to `l_discount,l_extendedprice` was better but still slower (`~31.5ms`) than the fused typed loop (`~22-24ms`).
    - Keep the hook off by default; the current TPC-H Q06 shape is better served by the specialized fused loop unless page-index/selection can skip much more IO.
  - Re-tested Q18 ordered streaming versus dense fallback after the `16384` batch-size change:
    - Ordered path remained faster in the focused profile (`~63.6ms` query, `~50.2ms` lineitem stage) than disabling ordered aggregation (`~66.3ms` query, `~53.0ms` lineitem stage).
    - Added `DODAM_Q18_DISABLE_ORDERED` only as a diagnostic knob; default stays ordered-first.
  - Rejected reordering Q08 lineitem membership checks to test `part_keys` before `order_years`.
    - Although part type is logically selective, the 3-run TPC-H sample regressed Q08 (`~50.4ms -> ~51.8ms`) and total runtime (`~749.8ms -> ~762.6ms`), likely from worse branch/cache behavior in the hot scan loop.
  - Added a generic-ish ordered `i64 -> Decimal128 sum` helper on `DodamEngine` for clustered grouped aggregation:
    - It scans Parquet row-group chunks in parallel, builds ordered run partials per chunk, keeps only threshold-passing complete middle groups, and merges first/last boundary runs in row-group order.
    - Q18 now tries this parallel ordered aggregate before the older sequential ordered path and dense fallback.
    - Correctness check: Q18 output matched the previous sequential ordered output via DuckDB `EXCEPT` in both directions, with `57` rows on both sides.
    - Focused Q18 profile moved the lineitem quantity stage from about `50ms` to about `7ms`, and Q18 query time from about `64ms` to about `20ms` in the single-run profile.
    - Latest 5-run SF=1 Parquet COPY comparison after this change sampled Dodam `~687ms` vs DuckDB `~566ms` (`~1.21x`), with Q18 median about `17.3ms` vs DuckDB about `37ms`.
  - Added a reusable Parquet `i64 set` RowFilter scan primitive and applied it to Q08's `l_partkey` membership filter:
    - This is a selective dimension-key-to-fact scan rule: decode the membership key first, then decode the wider payload only for matching rows.
    - Same-build 3-run comparison improved Q08 from about `49.7ms` to about `43.6ms`, and total median-sum from about `697.2ms` to about `694.0ms`.
    - Q08 output differs from the non-filter path only by tiny floating-point accumulation order noise (`~1e-17` in the sampled `mkt_share` values).
  - Rejected applying the same `i64 set` RowFilter to Q03 `l_orderkey`:
    - Focused sample regressed Q03 from about `55.7ms` to about `74.6ms`.
    - The candidate order key set is not selective enough to pay for RowFilter setup and key-column predecode; keep Q03 on the existing fused loop with row-group pruning only.
    - Profile-free 3-run median sample moved Q22 from about `21.3ms` to `16.9ms` (`~21%` faster). Full TPC-H median-sum was noisy in that run (`~770.9ms`), but the targeted Q22 improvement was consistent.
  - Rejected applying the `i64 set` RowFilter rule to Q05/Q07 `lineitem.l_suppkey`:
    - Same-binary sequential 5-run showed Q05 regressed from about `37.8ms` to `52.3ms` and Q07 from about `41.5ms` to `59.2ms`.
    - Even though the supplier key set is logically selective, the extra key-column predecode and Arrow RowFilter selection cost outweighed payload decode savings for the current SF=1 Parquet layout.
    - Keep the rule selective: it remains useful for Q08 `l_partkey`, but should not be blindly applied to every dimension-key filter without measuring.
  - Rejected replacing Q05/Q07 `orderkey -> nation` probe maps with `AdaptiveI64Map`:
    - Same-binary 5-run regressed Q05 from about `37.8ms` to `50.3ms` and Q07 from about `41.5ms` to `56.2ms`.
    - The orderkey domain is large and sparse, so dense-vector probes create worse cache behavior than the existing hash map despite cheaper indexing.
    - Rule: dense maps are only good when key density is high enough; sparse foreign-key joins should prefer compact hash tables or a better sparse-index layout.
  - Applied compact fast hashing to Q05/Q07 sparse `orderkey -> nation` fact-side probes:
    - Converted the hot probe map to `FastHashMap<i64, i64>` at the start of the lineitem stage, avoiding the dense sparse-key regression while still reducing integer hash overhead.
    - Same-binary 5-run sample improved Q05 from about `37.8ms` to `34.8ms`; Q07 stayed neutral (`~41.5ms` both ways).
    - Full TPC-H median-sum was noisy in that sample (`~679ms` baseline vs `~692ms` after), so treat this as a targeted Q05 win rather than a proven total-runtime shift.
  - Improved Q01 decimal aggregate hot loop:
    - Re-tested Q01 batch/chunk sweep and kept the existing `batch_size=16384`, `DODAM_Q01_CHUNK_SIZE=4` default; larger batches were worse for Q01 and total runtime.
    - Direct `Q01GroupSlots::update_key_values` removed the closure from the per-row typed path but was neutral by itself (`~40.4ms` median both ways).
    - Added a precision `<= 18` decimal path that casts raw Decimal128 values through `i64` before `f64`, mirroring the Q06 lesson; Q01 improved from about `40.4ms` to `38.4ms`.
    - Added length-checked unchecked slice access in the same no-null/single-byte/i64 fast path to avoid repeated bounds checks across seven columns. Same-binary 5-run moved Q01 from about `38.4ms` to `33.0ms`; DuckDB's sampled Q01 baseline is about `24ms`.
    - Current remaining Q01 gap is therefore mostly Parquet scan/decode structure plus residual aggregate arithmetic, not task chunking.
    - Rejected two Q01 group-slot lookup variants after measurement:
      - A `u16 key -> slot index` direct table regressed Q01 from about `33.0ms` to `39.6ms`; the 64KiB index initialization/cache footprint cost more than the tiny 4-group linear search.
      - A one-entry `last_key` cache regressed Q01 from about `33.0ms` to `39.0ms`; Q01 group keys were not run-local enough to pay for the extra branch/state.
      - Rule: Q01's current 8-slot linear search is cheap enough; further Q01 gains should target Parquet scan/decode or arithmetic representation, not group lookup.
  - Improved Parquet file-cache chunking:
    - Tested scan-level cache bypass for large wide scans; rejected it. Q01 did not consistently improve in same-binary comparison and Q09 regressed, so keeping a global shared file cache is still better for repeated TPC-H lineitem scans.
    - Swept `DODAM_FILE_CACHE_CHUNK_BYTES` across `256KiB/512KiB/1MiB/2MiB/4MiB`.
    - Larger chunks reduce cache shard/hash/lock traffic at the cost of extra read amplification. On the current SF=1 TPC-H run, 4MiB had the best full-run median.
    - Same-binary 5-run: `512KiB` median-sum about `671.3ms`, `4MiB` about `664.0ms`; Q09 improved notably (`~70.8ms -> ~60.0ms`) while Q01 was neutral.
    - Changed the default file cache chunk size from `512KiB` to `4MiB`; `DODAM_FILE_CACHE_CHUNK_BYTES` remains available for smaller-cache or remote-storage tuning.
  - Revisited single-lineitem decimal compute paths after the Q01 work:
    - Tried a Q14 no-null Decimal128 `precision <= 18` raw-i64 revenue path. Rejected it because Q14 did not improve in 5-run comparison (`~32.3ms -> ~33.3ms` in the sampled median); the existing `value(row)` loop is not the dominant cost there.
    - Tried a Q19 `precision <= 18` i64 revenue multiply path inside the existing raw predicate loop. Rejected it because Q19 regressed in the sampled median (`~29.6ms -> ~31.5ms`); Q19 is dominated by predicate/string/mask work plus scan decode, not the final revenue multiply.
    - Tried unchecked slice access in the existing Q06 i64 raw path. Rejected it because Q06 was neutral-to-worse (`~21.5ms -> ~22.1ms`); bounds checks are not the meaningful bottleneck after the earlier raw-value work.
    - Current scan profile with 4MiB cache chunks:
      - Q06 lineitem scan wall about `20.6ms`, cumulative decode about `229ms`, stage total about `21.1ms`.
      - Q14 lineitem scan wall about `16.7ms`, cumulative decode about `199ms`, stage total about `16.9ms`.
      - Q19 lineitem scan wall about `23.1ms`, cumulative decode about `255ms`, stage total about `23.4ms`.
    - Conclusion: further wins for these queries need lower-level Parquet/page decode or late materialization. More arithmetic-loop tweaks are unlikely to move the needle.
  - Applied Q06 late materialization:
    - Added a Parquet `RowSelection` reader hook and a Q06 path that first scans predicate columns (`l_shipdate`, `l_discount`, `l_quantity`), keeps selected discount raw values, and then reads only selected `l_extendedprice` rows.
    - This is guarded by schema/null checks and falls back to the existing fused scan if the data is not non-null `Date32` + `Decimal128(precision <= 18)`.
    - Retained `DODAM_Q06_DISABLE_LATE_MATERIALIZE=1` as an escape hatch and tuned `DODAM_Q06_LATE_ROW_GROUP_CHUNK` default to `2`.
    - Same-binary 5-repeat in-process sample: default late path Q06 median about `23.3ms`, disabled fallback median about `32.4ms`; a prior chunk sweep showed best samples around `12-13ms` for chunk `2/4`, but query timings are noisy.
    - Correctness spot check: Q06 scalar output matched the existing path (`123141078.22830002`), full TPC-H in-process still ran `22/22`, and `cargo test` passed.
    - Generalization rule: this pattern is promising when predicate selectivity is low and payload columns are expensive enough to skip. It should not be blindly used for broad scans like Q01 or for predicates whose predecode cost is similar to the payload saved.
  - Improved Q06 late materialization reader setup:
    - Changed the implementation from opening predicate/payload readers per row group to opening one predicate reader and one payload reader per row-group chunk. The `RowSelection` spans the concatenated selected row groups, which matches Parquet's row-selection semantics.
    - Re-swept `DODAM_Q06_LATE_ROW_GROUP_CHUNK` after this change. Same-binary 5-repeat samples:
      - chunk `1`: Q06 median about `18.1ms`
      - chunk `2`: Q06 median about `17.5ms`
      - chunk `4`: Q06 median about `18.4ms`
      - chunk `8`: Q06 median about `25.0ms`
      - chunk `16`: Q06 median about `38.0ms`
    - Kept chunk `2` as default. Larger chunks reduce reader setup but hurt parallelism/selection materialization enough to regress.
    - Disabled fallback in the same sample had Q06 median around `50.6ms`, so late materialization remains clearly beneficial on this run. Full-query totals are noisy, but all samples still ran `22/22`.
  - Applied the same late-materialization pattern to Q14:
    - Q14 first scans only `l_shipdate` to build a `RowSelection`, then scans selected rows for `l_partkey`, `l_extendedprice`, and `l_discount`.
    - The existing `part` reduction still builds `partkey -> is_promo`; payload processing computes numerator/denominator directly from selected lineitem rows.
    - The path is schema/null guarded and falls back to the existing Q14 scan if the lineitem payload is not non-null `Int64` + `Decimal128(precision <= 18)`.
    - Added `DODAM_Q14_DISABLE_LATE_MATERIALIZE=1` and `DODAM_Q14_LATE_ROW_GROUP_CHUNK`; chunk sweep favored default `4`:
      - chunk `1`: Q14 median about `22.4ms`
      - chunk `2`: Q14 median about `25.8ms`
      - chunk `4`: Q14 median about `22.4ms`
      - chunk `8`: Q14 median about `29.1ms`
      - chunk `16`: Q14 median about `40.3ms`
    - Scalar result differed from the previous path only by floating-point accumulation order (`16.380778626395536` vs `16.380778626395543`).
    - Final 5-repeat sample after setting chunk `4`: late path Q14 median about `37.7ms`, disabled fallback about `54.4ms`; run-to-run noise is high, but the fallback comparison still shows a clear win.
  - Refactored Q06/Q14 late materialization into a common chunk primitive:
    - Added a reusable predicate-pass + `RowSelection` + selected-payload reader helper. Q06 and Q14 now provide only their predicate-selection and payload-consumption logic.
    - The helper records `total_rows`, `selected_rows`, and selector run count; with `DODAM_TPCH_PROFILE=1`, the engine logs one aggregate line per query.
    - Profile sample after refactor:
      - Q06: `6,001,215` rows, `114,160` selected, ratio `~1.90%`, `208,099` selector runs, `27` chunks.
      - Q14: `6,001,215` rows, `75,983` selected, ratio `~1.27%`, `125,599` selector runs, `14` chunks.
    - Same-binary 5-repeat after the refactor kept performance in the expected range: Q06 median about `17.6ms`, Q14 median about `25.2ms`, full run `22/22`.
    - This confirms the first general rule: date/range predicates with `~1-2%` selectivity and expensive payload columns are strong late-materialization candidates. The next candidate should be measured first by selected ratio before enabling by default.
  - Added Q19 predicate selected-ratio profiling without changing the default execution path:
    - The existing Q19 lineitem pass now records predicate-selected rows only when `DODAM_TPCH_PROFILE=1`; default execution avoids row-level profile bookkeeping.
    - Profile sample: `6,001,215` rows, `121` selected, ratio `~0.0020%`, selector runs `613`.
    - Q19 lineitem stage in that profile was about `48.3ms`, with part-mask build about `6.8ms`. Same-binary 5-repeat after adding the profile hook showed Q19 median around `32-33ms` with profiling off.
    - This is a very strong late-materialization signal: predicate columns are wider than Q06/Q14, but selected payload rows are almost zero. Next step is to split Q19 into predicate pass (`l_partkey`, `l_quantity`, `l_discount`, `l_shipmode`, `l_shipinstruct`) and payload pass (`l_extendedprice`), reusing selected discount raw values like Q06.
  - Applied Q19 late materialization:
    - Promoted the late-materialized chunk runner to a generic engine API so SQL fast paths can provide their own predicate builder and payload consumer.
    - Q19 now scans predicate columns first (`l_partkey`, `l_quantity`, `l_discount`, `l_shipmode`, `l_shipinstruct`), stores selected discount raw values, and scans only selected `l_extendedprice` rows.
    - Added `DODAM_Q19_DISABLE_LATE_MATERIALIZE=1` and `DODAM_Q19_LATE_ROW_GROUP_CHUNK` with default `2`.
    - Chunk sweep on SF=1:
      - chunk `1`: Q19 median about `28.7ms`
      - chunk `2`: Q19 median about `28.7ms`
      - chunk `4`: Q19 median about `28.2ms`
      - chunk `8`: Q19 median about `39.8ms`
      - chunk `16`: Q19 median about `66.5ms`
    - Kept default `2` because it is stable and matches the Q06 default; `4` was marginally best in one sample but not enough to change the rule.
    - Scalar output differed from the previous path only by floating-point accumulation order (`3083843.0577999987` vs `3083843.0578000005`).
    - Profile sample after enabling the late path: `6,001,215` rows, `121` selected, ratio `~0.0020%`, selector runs `269`, row-group chunk `2`.
  - SF=10 Q09 scale-up fix:
    - The first SF=10 Parquet-output comparison showed Dodam `9.438s` vs DuckDB CLI `6.810s` overall, with Q09 as the dominant gap (`2.332s` vs DuckDB `0.723s`).
    - Profiling showed the unexpected root cause was not lineitem profit aggregation first, but `Q09 order years`: SF=10 order keys exceed the old `20M` dense-key limit, so the path converted to a large HashMap and spent about `1.63s` building `orderkey -> year`.
    - Added a memory-budgeted Q09 order-year dense limit (`DODAM_Q09_ORDER_YEAR_DENSE_BYTES`, default `384MiB`) so dense `Vec<i32>` remains available for larger but still bounded integer key domains, while retaining the existing fallback for wider/sparser keys.
    - Q09 order-year profile moved from about `1.63s` to about `0.15-0.16s`; Q09 wall time moved from about `2.30s` to about `0.78s`.
    - Also switched Q09 supplier-nation, supply-cost, and profit-group maps to the existing fast integer hasher. This was a smaller win: Q09 sampled about `0.756s` after the change, with supply-cost/profit aggregate profiles modestly lower.
    - Latest SF=10 one-run Parquet-output sample: Dodam `7.844s` vs the prior DuckDB CLI sample `6.810s`, reducing overall gap from about `1.39x` to about `1.15x`. Q09 is now close to DuckDB (`~0.756s` vs `~0.723s`) rather than the dominant outlier.
  - Generalized the Q09 order-year dense container from zero-based dense storage to offset dense storage:
    - The previous fix avoided HashMap fallback for SF=10, but still allocated `0..max_key` and would waste memory or fallback unnecessarily when integer keys live in a high non-zero range.
    - `DenseI64I32Map` now tracks `base_key` and allocates only `min_key..max_key` within the same memory budget, then falls back when the range budget is exceeded or keys are invalid.
    - This is primarily a HashMap-growth safety/generalization change. SF=10 TPC-H remains correct (`22/22`); profile sample kept Q09 order-year around `~148ms` and Q09 around `~0.75s`, while one profiling-off full run sampled Q09 at `~0.84s` and total `~7.97s`.
  - Improved Q04 using general late-materialization/selective-key rules:
    - Applied the reusable engine late-materialized Parquet map to Q04's `orders` candidate stage: scan `o_orderkey,o_orderdate` first, then read `o_orderpriority` only for selected rows.
    - SF=10 selected about `573,671 / 15,000,000` orders (`~3.8%`), which is a strong late-materialization signal. Row-group chunk sweep favored `1` for this narrow predicate.
    - Avoided per-selected-row priority string clones by interning `o_orderpriority` per chunk and returning `(orderkey, local_label_id)` pairs; global merge remaps labels once.
    - Enabled the existing `i64 set` RowFilter rule for Q04's `lineitem.l_orderkey` probe by default, while keeping `DODAM_Q04_DISABLE_LINEITEM_ROW_FILTER=1` as a diagnostic escape hatch.
    - Q04 profile sample moved from about `~0.31-0.33s` to about `~0.22s`; profiling-off SF=10 sample was Q04 `0.215s` vs DuckDB `0.179s` (`~1.20x`). The full one-run total sampled Dodam `7.888s` vs DuckDB `6.810s` (`~1.16x`).
    - General rule to push into optimizer/execution: late materialization should be cost/selectivity gated. It is attractive when predicate columns are narrow and payload columns are wide or expensive, and selected ratio is low. Q01 is the counterexample: its date predicate keeps almost all lineitem rows, so late materialization would add selection overhead without avoiding payload decode.
  - Added a common late-materialization selectivity policy in the engine:
    - `LateMaterializationPolicy` lets generic late-materialized readers reject chunks whose predicate pass selected too many rows before opening the payload reader.
    - The existing generic API remains available as an always-allow path, while Q04/Q19 now call the policy-aware API with env-tunable thresholds (`DODAM_Q04_LATE_MAX_SELECTED_RATIO`, `DODAM_Q19_LATE_MAX_SELECTED_RATIO`, default `0.60`).
    - A forced Q04 rejection test (`DODAM_Q04_LATE_MAX_SELECTED_RATIO=0.001`) exposed a correctness bug: the typed fallback candidate path filled the marker vector but not the RowFilter key set, causing zero Q04 rows. Fixed by carrying candidate keys through typed and non-typed fallback paths.
    - Default SF=10 profiling-off sample after the policy work: Dodam `7.777s` vs DuckDB CLI `6.810s` (`~1.14x`), Q04 `0.222s` vs DuckDB `0.179s`, and Q19 remains faster than DuckDB in the sampled run.
  - Revisited Q01 under SF=10:
    - Added an opt-in pruning-only path for `l_shipdate <= cutoff` (`DODAM_Q01_ENABLE_PRUNING=1`), but the current SF=10 lineitem layout still scans `524/524` row groups. Q01 samples were neutral-to-slightly-worse (`~0.455s` with pruning vs `~0.450s` without in one profile pair), so pruning stays off by default.
    - Retested Q01 chunk size at SF=10. Chunk `8` occasionally sampled slightly faster than `4`, but 3-run behavior was noisy and not enough to change the default chosen from SF=1/full-run testing. Chunk `2` and `16` did not provide a reliable win.
    - Retested global Parquet row-group task chunking for Q01. `2` improved one Q01 scan sample, but 3-run Q01/full totals were effectively tied with the default and previous SF=1 tests favored `4`, so the global default remains unchanged.
    - Added an opt-in raw integer complement arithmetic path (`DODAM_Q01_RAW_COMPLEMENT=1`) for Decimal128 precision `<= 18`, computing discounted/charge values from raw decimal complements. A single SF=10 sample improved Q01 (`~0.459s -> ~0.444s`), but 3-run median was neutral-to-worse versus the existing path (`~0.421s raw vs `~0.414s` existing), so it remains disabled by default.
    - Batch-size sweep on SF=10 still favored the current `16384`; `8192`, `32768`, and `65536` regressed Q01 and/or full total in one-run samples.
    - Conclusion: Q01's remaining DuckDB gap is not from easy late materialization, pruning, chunking, or scalar arithmetic tweaks. It is primarily Parquet scan/decode throughput and the fused aggregate pipeline.
  - Added a generic row-group map execution primitive and applied it to Q01:
    - `DodamEngine::parquet_row_group_map` scans Parquet row-group chunks inside worker tasks and lets the caller consume each `RecordBatch` into a task-local state, returning only one partial output per row-group chunk.
    - Q01 now builds `Q01GroupSlots` inside the scan worker instead of sending every decoded batch through `ParallelParquetScanStream` and then spawning a second aggregate task layer. `DODAM_Q01_DISABLE_ROW_GROUP_MAP=1` keeps the old stream + chunked aggregate path available for diagnostics.
    - This directly targets scan/aggregate dispatch overhead and batch materialization between execution stages; it is a reusable primitive for other fused scan-to-aggregate paths.
    - SF=10 one-run same-binary comparison: new Q01 path `~0.221s`, old path `~0.450s`.
    - SF=10 3-repeat sample with the new path: Q01 `0.245s / 0.190s / 0.185s`, median about `0.190s`; full repeat totals `7.656s / 7.520s / 7.633s`.
    - Q01 output matches the previous path except for tiny floating-point accumulation-order differences.
  - Generalized scan-to-aggregate fusion above the Q01-specific path:
    - Added a SQL-layer `parquet_scan_fold_chunks` helper that first tries `DodamEngine::parquet_row_group_map` and falls back to the old `scan_parquet_batches + parallel_batch_fold_chunks` path when the source is not eligible or `DODAM_DISABLE_SCAN_AGG_FUSION=1` is set.
    - The helper separates worker-local partial state construction from final output construction. This matters for high-cardinality aggregates: Q21 initially regressed badly when every row-group worker reserved the final output capacity; Q21 is now deliberately left on the old stream/chunk path until a state-size/cardinality cost rule exists.
    - Applied the helper to Q01, Q15, Q20, and the non-default fallback paths for Q06/Q19. Q06/Q19 usually still take their late-materialized paths first.
    - Current-binary SF=10 one-run comparison with fusion disabled: total `7.805s`, Q01 `0.464s`, Q15 `0.328s`, Q20 `0.313s`.
    - Final retained SF=10 one-run sample with fusion enabled and Q21 excluded: total `7.388s`, Q01 `0.215s`, Q15 `0.214s`, Q20 `0.223s`, Q21 `0.943s`.
    - General rule: fuse scan and aggregate when partial state is small/medium and merge is cheap; avoid fusing high-cardinality or large-capacity partial maps until the optimizer has a real state-size estimate.
  - Generalized more dense integer containers out of TPC-H query code:
    - Moved the Q18 dense `i64 -> f64 sum` threshold-tracking accumulator and the Q09 offset dense `i64 -> i32` map into `dense.rs` next to `AdaptiveI64Set`/`AdaptiveI64Map`.
    - This is primarily structural, not a new speed win: future physical operators can now reuse dense range maps/sums without depending on `sql.rs` TPC-H helpers.
    - Kept the same fallback behavior and added an accessor instead of letting Q18 inspect the dense sum's fallback field directly.
  - Added a general Decimal128 product fast evaluator for SQL scalar/aggregate input expressions:
    - The generic scalar expression path now recognizes `Decimal128 column * Decimal128 column` and `Decimal128 column * (1 - Decimal128 column)` independent of column names.
    - This avoids building intermediate `Vec<Option<f64>>` columns for each decimal operand and evaluates the product in one batch loop. `DODAM_DISABLE_DECIMAL_EXPR_FAST=1` keeps the old path available for diagnostics.
    - SF=10 lineitem generic SQL benchmark, 5-run wall-clock samples:
      - `sum(l_extendedprice * l_discount)`: fast median about `0.66s`, disabled median about `0.80s`.
      - `sum(l_extendedprice * (1 - l_discount))`: fast median about `0.66s`, disabled median about `0.87s`.
    - Output differences versus the old path are tiny f64 accumulation/order differences (`~8e-16` and `~3e-16` relative in the checked samples), not semantic decimal scale changes.
    - Full SF=10 TPC-H fast-path run still passed `22/22`; this change mostly benefits non-specialized SQL expression paths because most canonical TPC-H queries still route through their typed fast paths.
  - Tried to generalize small-cardinality single-key group lookup:
    - Added an adaptive small linear slot index for integer single-key grouped aggregate fast paths (`Int32`, `Int64`, `UInt64`) before falling back to the existing hash index. `DODAM_SMALL_GROUP_LINEAR_LIMIT=0` disables it for diagnostics; default limit is `8`.
    - Kept `Utf8` group lookup on the existing hash index. A linear string slot attempt regressed SF=10 `GROUP BY l_returnflag, count(*)` from about `0.61s` to `0.76-0.83s`, so string keys are not a good fit for this rule.
    - Integer small group sample (`GROUP BY l_linenumber, count(*)`) was neutral-to-slightly-better: adaptive about `0.49-0.50s`, forced hash about `0.50-0.51s`.
    - Full SF=10 TPC-H run still passed `22/22` with a one-run total around `7.51s`; most canonical TPC-H small groups still use query-specific fast paths, so this mainly benefits generic SQL grouped aggregate on tiny integer domains.
  - Generalized `ORDER BY ... LIMIT` TopK pruning before final sort:
    - `SortExec` and SQL output ordering now sort each input batch down to the requested limit before concatenating candidates and applying the final global TopK. `DODAM_DISABLE_TOPK_BATCH_PRUNE=1` keeps the old concatenate-then-sort path available for diagnostics.
    - This is a general TopK rule, not tied to TPC-H columns: for any ordering, rows outside a batch-local top K cannot enter the global top K except for SQL tie-order ambiguity, which is already not stable without explicit tie-breakers.
    - SF=10 lineitem sample (`ORDER BY l_orderkey DESC, l_linenumber ASC LIMIT 100`) improved from about `0.95-0.97s` disabled to about `0.48s` enabled after warmup; on/off outputs matched in the checked sample.
    - Full SF=10 TPC-H remained correct (`22/22`). The total was effectively neutral in one-run samples (`7.478s` enabled vs `7.481s` disabled) because most canonical paths already produce small/single-batch outputs before final ordering.
  - Generalized the tiny two-`Utf8` group-key idea from Q01-style workloads into the generic grouped aggregate path:
    - `GROUP BY utf8, utf8` now starts with a small linear slot table before promoting to the existing nested hash index. `DODAM_TWO_UTF8_SMALL_GROUP_LIMIT=0` disables the slot phase for diagnostics; default limit is `8`.
    - This removes the dependency on fixed column names like `l_returnflag`/`l_linestatus` for this specific group lookup optimization. The Q01 typed aggregate still has its own more specialized accumulator, but ordinary SQL with tiny two-string groups now gets a similar lookup rule.
    - SF=10 generic sample (`GROUP BY l_returnflag, l_linestatus` with `count(*)`/`sum(l_quantity)`, not the Q01 canonical fast path) improved after warmup from about `1.29-1.30s` with forced hash to about `1.19s` adaptive; outputs matched.
    - A larger-cardinality sample (`GROUP BY o_orderpriority, o_clerk`) was roughly neutral/slightly slower (`0.80-0.82s` adaptive vs `0.78-0.81s` forced hash), which is expected from the small initial slot checks before promotion. Keep the limit small unless broader benchmarks show a better crossover.
    - Full SF=10 TPC-H remained correct (`22/22`) with one-run total around `7.55s`; canonical Q01 still routes through its typed path, so this is mostly a generic SQL benefit.
  - Generalized row-group fused Parquet aggregate for mergeable aggregate functions:
    - Added a common partial `AggregateMetrics` merge path and routed single-table Parquet aggregate through `parquet_row_group_map` when aggregates are mergeable (`count`, `sum`, `min`, `max`; not `avg` or `count_distinct` yet because partial output loses the extra merge state).
    - This is not tied to TPC-H column names. It applies to ordinary single-table Parquet aggregates with explicit aggregate/group columns and no expression aggregate.
    - SF=10 generic grouped aggregate sample (`GROUP BY l_returnflag, l_linestatus`, `count(*)`, `sum(l_quantity)`) improved from roughly `1.22-1.32s` with `DODAM_DISABLE_FUSED_PARQUET_AGGREGATE=1` to roughly `0.19-0.40s` enabled, depending on run noise.
    - SF=10 TPC-H still passed `22/22`; a noisy concurrent run sampled `8.62s`, while a clean rerun sampled `7.39s`, so no retained regression signal.
  - Tried generic late-materialized aggregate:
    - Added an opt-in generic path (`DODAM_ENABLE_LATE_MATERIALIZED_AGGREGATE=1`) that reads predicate columns first, builds a `RowSelection`, and then reads aggregate/group payload columns before partial aggregate merge.
    - Added a selector-fragmentation guard (`DODAM_LATE_AGG_MAX_SELECTOR_RUN_RATIO`, default `0.02`) because selected-row ratio alone allowed highly fragmented selections that made Parquet row-selection reads extremely slow. Before the guard, a selective-looking filter sample ran around `98s`; with the guard it falls back quickly.
    - Kept this path disabled by default. In tested aggregate samples the payload was already narrow, so generic late materialization did not beat the normal scan path (`date-only sum` was roughly neutral/slower, and fragmented date+quantity was slower after fallback overhead). The existing Q06/Q14/Q19 specialized late paths remain enabled because they carry extra predicate-side values or use query-specific payload logic.
  - Expanded aggregate timing instrumentation:
    - `AggregateMetrics` now includes `aggregate_nanos` and `aggregate_merge_nanos`; `dodam aggregate` and SQL aggregate summaries print these as milliseconds.
    - This complements the existing scan metrics (`metadata`, `planning`, `decode`, `filter`, `projection`, `limit`) and makes scan-vs-aggregate attribution easier before deeper decode/filter instrumentation.
  - New SF=10 profiling pass after fused aggregate generalization:
    - Largest retained hot spots are still Q21 (`final order keys` around `300ms`, `lineitem order states` around `580-610ms`), Q09 (`order years` around `155-210ms`, `profit aggregate` around `525ms`), Q03/Q05 join-revenue aggregation, and scan-heavy lineitem loops.
    - Tried pushing Q21 `o_orderstatus = 'F'` into the scan so only `o_orderkey` was output. It regressed `final order keys` from roughly `~310ms` to `~376ms`; Arrow row-filter overhead was larger than the saved output column loop for this shape, so the manual two-column typed loop remains better.
    - Tried parallel/chunk-local Q21 final-order set build. It stayed in the same range (`~303ms`) and did not materially improve total Q21, so it was rejected.
    - Tried chunking Q09 profit aggregation to reduce per-batch task fanout. `DODAM_Q09_PROFIT_CHUNK_SIZE` sweeps from `1/4/8/16` kept the profit stage around `~526-531ms`; this stage is dominated by scan/probe/decimal work, not task count, so the existing batch-local path is retained.
    - Added a common sample gate for selective late-materialized Parquet paths. It samples leading row groups with predicate columns and rejects the late path when selected-row or selector-run policy fails; `DODAM_LATE_SAMPLE_ROW_GROUPS=0` disables it and `DODAM_LATE_SAMPLE_FORCE=1` forces sampling for diagnostics.
    - The default gate only samples when payload projection is at least twice as wide as predicate projection. Forced sampling regressed current TPC-H-like paths because Q04/Q19 have narrow payloads and already-beneficial specialized late logic; default cost gating keeps the clean SF=1 TPC-H in-process run effectively neutral versus sample-off (both around `~0.66s` warm median in the sampled run).
    - Tried replacing Q09 `(partkey, suppkey) -> supplycost` tuple hash with a `partkey -> small fanout list` probe. It was neutral-to-worse in repeated samples because the extra `HashMap -> Vec -> linear probe` pointer chasing did not beat the existing fast tuple hash. Kept it only behind `DODAM_Q09_SUPPLYCOST_FANOUT=1` for diagnostics.
    - Added a default packed `u64` supply-cost key for Q09 when both keys fit non-negative `u32`, with automatic fallback to tuple hash otherwise. `DODAM_Q09_SUPPLYCOST_PAIR_HASH=1` restores the old tuple-key path. SF=1 5-repeat in-process comparison was roughly neutral/slightly better overall (`~3.44s` packed vs `~3.49s` pair-hash total in the sampled run), while Q09 stayed in the same warm `~61-73ms` band.
    - Expanded profiling for Q09 and scan attribution:
      - `parallel_batch_fold`/chunk profile now splits `stream_read` from `worker_wait_merge`, making scan/decode wait versus worker/merge backlog visible.
      - `DODAM_TPCH_PROFILE=1` now also enables scan profile output. Scan profile prints `next_wait` and `consumer_gap` in addition to metadata/planning/decode/filter/projection/limit. `decode` remains worker-time sum, while `next_wait` is consumer-visible wall wait.
      - Q09 profit aggregation now prints row/hit counters plus accumulated worker-time for `part_keys.contains`, `order_years.get`, `supplier_nations.get`, `supply_costs.get`, and amount arithmetic. In one SF=1 sample, Q09 lineitem scan showed `stream_read ~42ms`, `worker_wait_merge ~25ms`, and profile worker-time was dominated by part-key membership and amount arithmetic.
    - Used the new Q09 detail profile to reduce hot-loop overhead:
      - Added dense-slice accessors for `AdaptiveI64Set` and `DenseI64I32Map`, then used them in Q09 profit probing to avoid repeated enum/fallback checks in the dense case.
      - Changed Q09 typed Decimal128 amount calculation to use raw decimal slices with batch-level scale factors, and added a no-null typed loop for the common Parquet path.
      - Changed Q09 supplier nation lookup to use `AdaptiveI64Map<i64>` instead of a `HashMap`, which reduced supplier lookup worker-time in the profile from roughly the mid-teens of milliseconds to about `~10-11ms`.
      - Latest SF=1 5-repeat in-process sample stayed correct at `110/110`; Q09 warm samples were around `~59-65ms`, with total 5-repeat time around `~3.46s`. Treat this as a small targeted Q09 cleanup rather than a large total-runtime shift.
    - Tried Q09 late materialization for the lineitem profit scan:
      - Implemented an opt-in path behind `DODAM_Q09_ENABLE_LATE_MATERIALIZE=1` that reads only `l_partkey`, builds a `RowSelection`, then reads selected `l_orderkey/l_suppkey/l_quantity/l_extendedprice/l_discount` rows.
      - The selectivity looked favorable (`319,404 / 6,001,215`, about `5.3%`), but selector fragmentation was high (`~604k` runs, about `10%` of rows). That made Parquet row-selection reads plus the extra predicate pass slower than the retained full-scan typed loop.
      - SF=1 5-repeat comparison regressed Q09 from warm `~62-70ms` full-scan samples to roughly `~69-84ms` late-materialized samples, and total time from about `~3.42s` to `~3.51s`. Keep this path opt-in only; it may still be useful for clustered data with long selected runs.
    - Improved Q21 order-state construction:
      - Added dense final-order membership probing in the Q21 lineitem typed loop, then cached the final-order membership per contiguous orderkey run so repeated lineitem rows for the same order do not re-check the final order set.
      - Changed Q21 chunk processing to accumulate each batch directly into the chunk-local state map instead of building a temporary per-batch state map and merging it immediately.
      - Profile samples were noisy (`~42-45ms` for the lineitem state stage), but SF=1 5-repeat in-process total improved in the sampled run to about `~3.40s`, with Q21 warm samples around `~59-62ms`. Treat this as a small retained Q21 cleanup.
    - Revisited Q01 row-group fused aggregate attribution:
      - Added `DODAM_TPCH_PROFILE=1` detail logging for the common `parquet_row_group_map` path, splitting row-group chunk wall time into reader setup, metadata/planning, `read_next` decode/read wait, and aggregate `consume`.
      - Q01 warm SF=1 profile shows the remaining time is dominated by Parquet read/decode (`~16-22ms` per 4-row-group chunk) rather than the fused aggregate loop (`~4-6ms` per chunk). Reader setup/metadata/planning are effectively negligible after cache warmup.
      - Re-swept Q01 row-group chunk size on the current binary. Chunk `1` and `2` were best in the sample, while `3+` regressed or became noisier. Changed the default `DODAM_Q01_ROW_GROUP_MAP_CHUNK` from `4` to `2` as the less task-heavy of the two best settings.
      - SF=1 Q01-only 7-repeat sample totals were roughly `0.163s` for chunk `1`, `0.163s` for chunk `2`, `0.173s` for chunk `4`, and `0.218s+` for chunk `3/6/8`. This is a small Q01 cleanup; the larger remaining opportunity is still Parquet decode throughput.
      - Added a projection-order Q01 consumer to bypass repeated batch column-name lookup in the canonical path. This was neutral in Q01-only and full SF=1 samples, confirming that name lookup is not a material cost after scan-to-aggregate fusion; retained as a low-risk cleanup for the fixed-projection path.
    - Tried a Q09 dense-probe unchecked accessor for dense `AdaptiveI64Set`/dense order-year lookup. Profiling did not improve the dominant part-key probe time, and Q09 standalone samples stayed noisy in the same band, so the unchecked probe change was rejected.
    - Re-swept Q21 lineitem order-state chunking after the latest map/merge changes. Larger chunks reduce intermediate state-map merge overhead; `DODAM_Q21_CHUNK_SIZE=48` was the best sampled value. Q21-only 7-repeat total moved from about `0.437s` at the old default `16` to about `0.413s` at `48`; `64` regressed. Changed the default to `48`.
    - Tried applying the existing Parquet `i64` set row-filter to Q10's lineitem scan using the date-filtered orderkey set. It regressed Q10-only 7-repeat total from about `0.382s` full-scan to about `0.399s` row-filtered, so it was reverted. For this shape the row-selection overhead is still larger than the saved `l_extendedprice/l_discount` decode.
    - Tried preallocating Q21 chunk-local order-state hash maps from the final-order count. It did not beat the normal growing map; larger prealloc divisors were best and effectively reverted to the original behavior. The likely issue is that allocating and touching larger tables costs more than the saved rehashing for this workload.
    - Applied the same `i64` set row-filter idea to Q10's final customer fetch instead of the lineitem scan. This one worked because only 20 customer rows are needed but the payload includes wide string columns. Q10-only 7-repeat total improved from about `0.395s` with the old wide customer scan to about `0.375s` with `c_custkey` row-filtered customer payload. `DODAM_Q10_DISABLE_CUSTOMER_ROW_FILTER=1` restores the old path.
    - Tried the same final-payload row-filter idea on Q15 supplier fetch and Q18 customer-name fetch. Both were rejected: Q15's supplier table is too small, and Q18 only reads `c_custkey,c_name`, so row-filter overhead exceeded the decode saved. This refines the rule: key-set row-filter is worthwhile only when the payload is both wide enough and the table is large enough.
    - Removed redundant `ProjectionExec` wrapping when a Parquet scan already emits exactly the requested output projection. This is a general scan/decode cleanup, not a TPC-H-specific rule: it avoids `RecordBatch::project` and extra batch materialization on every simple projected scan. `DODAM_TPCH_PROFILE=1` now reports `projection=0.000ms` for the affected lineitem/orders scans.
    - Tightened the generic Parquet `i64` set row-filter predicate. It now converts bounded non-negative key sets into a dense boolean membership table and builds the Arrow `BooleanArray` through `BooleanBuilder` instead of allocating `Vec<Option<bool>>` per predicate batch. The full SF=1 TPC-H sample stayed correct at `66/66`, with warm totals around `0.636s`; this is retained because it makes the shared row-filter path cheaper without hard-coding individual query columns.
    - Re-swept global Parquet scan batch size as a possible RecordBatch materialization reduction. SF=1 full TPC-H samples showed the current `16K` default still best among `8K/16K/32K/64K/128K`: warm totals were roughly `0.65s`, `0.63-0.65s`, `0.65-0.66s`, `0.69-0.71s`, and `0.81s`. Larger batches reduce batch count but hurt the scan/aggregate working set enough to lose overall, so the default was left unchanged.
    - Revisited the Parquet chunk file cache as the closest local equivalent to DuckDB's buffer manager. A 256MiB cache thrashed and was slower, but 512MiB+ held the SF=1 compressed working set (`~361MiB`) and improved warm full TPC-H repeats from about `0.64s` to `0.59-0.60s`. Enabled a conservative 512MiB default with 4MiB chunks; `DODAM_FILE_CACHE_BYTES=0` disables it. The tradeoff is a slower first cold pass due chunk admission, but 7-repeat SF=1 total improved from `4.56s` disabled to `4.38s` default.
    - Tried a Q21 ordered-lineitem run-state path to avoid building and merging per-chunk order-state hash maps when input is monotonic by `l_orderkey`. It was rejected as a default: it saves merge work but loses enough parallelism that Q21 and full TPC-H did not improve. The code is left behind an opt-in `DODAM_Q21_ENABLE_ORDERED_LINEITEM=1` guard for future ordered-group experiments, while the default remains the parallel hash-map path.
    - Tried applying the Parquet `i64` set row-filter to Q09's lineitem scan on `l_partkey`. Despite only about 5% of rows matching, it regressed Q09 because Arrow RowFilter predicate/pass reconstruction and reduced scan parallelism outweighed the saved payload decode. Left behind as opt-in `DODAM_Q09_ENABLE_LINEITEM_PARTKEY_ROW_FILTER=1`; default remains the full projected lineitem scan.
    - Tried parallel tree-reducing Q21's partial order-state hash maps. This was worse than the existing sequential merge: Q21 moved from roughly `60ms` to `95ms` because the extra owned-map movement and hash-table merges outweighed parallelism at only eight chunks. Left behind as opt-in `DODAM_Q21_ENABLE_PARALLEL_MERGE=1`; default remains sequential merge.
    - Tried Q21 compact dense order-state vectors keyed by final-order compact indexes. This removed per-row hash-map updates inside chunks, but each partial had to allocate and initialize a large state vector, so Q21 regressed from roughly `62ms` hash-map path to `82ms+`. Left behind as opt-in `DODAM_Q21_ENABLE_DENSE_ORDER_STATE=1`; default remains the hash-map path.
    - Tried Q09 matched-row index staging for the typed Decimal128 lineitem profit loop. A multi-stage version allocated intermediate row/tuple vectors and regressed; a single matched-index buffer was still noisy and not consistently better than the existing fused row loop. Left behind as opt-in `DODAM_Q09_ENABLE_MATCHED_INDEX=1`; default remains the fused loop because the current branch/cache behavior is already good enough that extra index materialization does not reliably pay for itself.
    - Generalized the repeated `l_extendedprice * (1 - l_discount)` Decimal128 calculation into a shared raw-decimal helper and applied it to the no-null typed paths in Q03/Q05/Q07/Q10. This is a common expression rule rather than a query-name specialization: it avoids two per-row decimal-to-f64 conversions when the Parquet decimal arrays are non-null. SF=1 samples stayed correct; Q05/Q13-adjacent full-run totals remained noise-bound, so keep this as low-risk cleanup rather than claiming a large win.
    - Retried Q21 dense order-state as a sparse touched-state table by replacing the full `Vec<Q21OrderState>` initialization with `positions + touched states`. It still lost to the hash-map run-state path: Q21 sampled around `78-89ms` versus the default `~62-66ms`, likely because per-chunk position-vector initialization and compact-index indirection still dominate. The opt-in dense path remains for future layouts, but the default remains the hash-map path.
    - Added a direct fast path for LIKE-only filters in the derived left-join count-distribution path. For `%literal%literal%` patterns without `_` or `ESCAPE`, it applies `memchr::memmem::Finder` directly while building dense right-side counts, avoiding `BooleanArray` mask construction and the generic scalar LIKE matcher. This benefits Q13's `o_comment NOT LIKE '%special%requests%'` shape without matching table/query names. SF=1 Q13 warm samples were about `26-29ms`, and 7-repeat full TPC-H totals stayed around `4.25-4.34s`.
    - Added `DODAM_DISABLE_FAST_LIKE_DISTRIBUTION=1` to A/B the derived count-distribution LIKE fast path in the same binary. SF=1 10-repeat samples showed Q13 warm times around `27-29ms` with the fast path and `33-35ms` without it, a targeted `~5-7ms` (`~15-20%`) Q13 win. Full 22-query totals remain much noisier because Q13 is a small part of the total.
    - Tightened the left side of the derived count-distribution path with a no-null typed `Int64Array` loop. This is correctness-safe and avoids per-row `is_valid/value` calls when the left key column is non-null, but the profile shows the left/customer scan is only about `~1.6ms` in Q13, so the observed Q13 time stayed in the same `~26-28ms` band. Skipping the left scan entirely would require stronger uniqueness/contiguity proof than Parquet min/max statistics alone provide.
    - Extended the raw-slice cleanup in discounted-revenue typed loops by using raw key/date slices in Q03/Q05/Q07 no-null paths in addition to the shared raw Decimal128 revenue helper. Correctness tests passed, but SF=1 samples remained noise-bound; keep as a low-risk hot-loop cleanup, not a claimed material speedup.
    - Re-tested Q09 supply-cost layouts after the latest typed-loop changes. `PackedU64`, pair-hash, and small-fanout all remained in the same noisy Q09 warm range (`~55-60ms` typical, with occasional higher samples); fanout sometimes improved full-run totals but not consistently enough to replace the packed default. Keep the existing env-gated alternatives for future data distributions.
    - Re-tested Q21 ordered direct-count after the latest changes. It still does not beat the default parallel order-state path consistently: both paths sampled mostly around `~61-64ms` for Q21, while full-run totals varied more than the difference. A correct parallel direct-count design would need chunk-boundary carry for orderkey runs; the existing default remains safer and at least as fast on this data.
    - Refreshed `DODAM_TPCH_PROFILE=1` attribution after these changes. Q13 is dominated by the orders comment scan/filter (`orders[o_custkey,o_comment]` around `~33ms`, customer key scan only `~1.6ms`). Q09 still spends most visible wall time in lineitem scan/decode plus worker merge (`~36ms stream_read`, `~22ms worker_wait_merge` in one profile), with worker-time dominated by part-key membership and amount/supply lookups. Q03/Q05/Q07/Q10 remain mostly scan/decode and fact-table aggregation rather than final output cost.
    - Tried Q03 compact sorted-vector order lookup as an alternative to `HashMap<i64, Q03Order>` probing. After fixing the setup cost so the sorted vector is built once per query instead of per batch, SF=1 Q03 warm samples stayed essentially tied or slightly worse (`~36-39ms` default vs `~36-40ms` sorted). Binary search did not beat the current fast integer hash probe, so this remains opt-in behind `DODAM_Q03_ENABLE_SORTED_ORDER_LOOKUP=1`.
    - Tried Q16 group/supplier distinct counting with a dense bitset (`group_id x ps_suppkey`) instead of the existing pair `FastHashSet`. It was clearly worse on SF=1: Q16 warm samples moved from about `~24-25ms` with the hash set to about `~38-46ms` with the bitset. The output domain is small enough, but per-partial bitmap allocation/OR/count work is larger than the pair-hash merge. The bitset path remains diagnostic-only behind `DODAM_Q16_ENABLE_SUPPLIER_BITSET=1`.
    - Added typed raw loops to Q12's canonical shipping-mode path: lineitem filtering now compares raw Utf8 bytes and raw Date32/Int64 slices, and the orders pass uses an adaptive dense integer map for pending order lookup. SF=1 Q12 warm samples moved modestly from about `~51-53ms` generic to about `~49-50ms` typed. This is a retained small improvement and a candidate pattern for generic typed string/date filters.
    - Re-tested Q06 late materialization against the fused typed full-scan path. The late path was slower in current SF=1 samples (`~15-16ms`) than the fused typed scan (`~9-11ms`) because the payload is narrow and row-selection reconstruction costs more than the saved decode. Changed Q06 late materialization to opt-in via `DODAM_Q06_ENABLE_LATE_MATERIALIZE=1`; the default is now the faster fused typed scan.
    - Refreshed SF=1 profile for Q03/Q05/Q07/Q12 after the latest changes:
      - Q03/Q05/Q07 fact-table aggregate stages are now mostly scan/decode wait rather than hash merge work. Warm samples showed Q03 revenue aggregate around `~15ms`, Q05 around `~19ms`, and Q07 around `~18ms`, with `worker_wait_merge` small relative to lineitem scan/decode. This argues against more hash-layout micro-specialization as the next primary lever.
      - Q12 lineitem aggregate still emits many lineitem batches, so changed it from per-batch `parallel_batch_fold` to chunked `parallel_batch_fold_chunks` with `DODAM_Q12_LINEITEM_CHUNK_SIZE` defaulting to `8`. SF=1 samples were mostly neutral (`~49-51ms`) but reduce task fanout and are retained as a small structural cleanup.
      - Tried Q12 orders `o_orderkey` row-filter using pending order keys. It did not improve because the key set is not selective enough and the payload is narrow (`o_orderpriority` only); row-filter overhead offset the saved decode. Kept it opt-in behind `DODAM_Q12_ENABLE_ORDER_ROW_FILTER=1`, default off. This reinforces the general rule from Q10/Q15/Q18: key-set row-filter is useful mainly when selected rows are very small and payload columns are wide enough.
      - Current SF=1 7-repeat in-process run after the Q12 cleanup stayed correct (`154/154`) with warm repeat sums around `0.600-0.620s`; Q03 warm `~36-40ms`, Q05 `~33-35ms`, Q07 `~39-41ms`, Q12 `~49-50ms`.
    - Added a pruning-capable row-group map API and routed Q03/Q05/Q07 fact-table revenue scans through it by default, with `DODAM_Q03_DISABLE_ROW_GROUP_MAP=1`, `DODAM_Q05_DISABLE_ROW_GROUP_MAP=1`, and `DODAM_Q07_DISABLE_ROW_GROUP_MAP=1` restoring the old stream path.
      - This is the first retained step toward a fused scan/consumer API: it still decodes Arrow arrays, but bypasses the generic `ScanExec -> SendableBatchStream -> batch chunk fold` layer and consumes row-group chunks directly.
      - SF=1 A/B showed material wins on the target scans. Warm Q03 moved from roughly `~37-39ms` stream to `~32-35ms` row-group map, Q05 from `~32-36ms` to `~26-28ms`, and Q07 from `~38-43ms` to `~28-31ms`.
      - Full SF=1 7-repeat comparison stayed correct (`154/154`). Warm repeat sums for row-group map were around `0.588-0.619s`; stream fallback was around `0.615-0.635s`. This suggests the common issue was not only Parquet decode itself but also the generic scan stream/task layer around high-volume fact scans.
    - Expanded the row-group map/fused-consumer direction to Q09/Q12/Q21:
      - Q09 profit full-scan path now uses row-group map by default when the opt-in partkey row-filter is not enabled (`DODAM_Q09_DISABLE_ROW_GROUP_MAP=1` restores the old stream path). SF=1 warm Q09 moved from roughly `~56-71ms` stream samples to mostly `~39-50ms` row-group map samples.
      - Q21 lineitem order-state build now uses row-group map by default (`DODAM_Q21_DISABLE_ROW_GROUP_MAP=1` restores the old stream path). Focused samples moved from roughly `~62-67ms` to `~55-58ms`.
      - Q12 lineitem aggregate also has a row-group map path (`DODAM_Q12_DISABLE_ROW_GROUP_MAP=1` restores stream), but it was mostly neutral around `~48-52ms`; keep it for structural consistency rather than claiming a clear win.
      - Q18 was not changed here because it already first tries the stronger `ordered_i64_decimal_group_sum_above` path, which is a more specialized row-group aggregate than the generic row-group map wrapper.
      - Full SF=1 7-repeat expanded A/B stayed correct (`154/154`). Row-group map warm repeat sums were around `0.555-0.565s`, while stream fallback was around `0.615-0.629s`.
    - Continued the fused-consumer cleanup by adding projection-order typed batch wrappers for Q03/Q05/Q07/Q09/Q12/Q21 fast paths:
      - These wrappers consume already-projected Arrow arrays by ordinal (`batch.column(i)`) before falling back to the existing column-name lookup paths. This is a general executor-shape cleanup for fixed projection plans, not a table-name shortcut; it removes repeated schema/name resolution inside hot row-group consumers while preserving fallback behavior.
      - Q21 ordered lineitem direct-count was revisited and promoted to the default guarded path (`DODAM_Q21_DISABLE_ORDERED_LINEITEM=1` restores the row-group order-state path). It validates monotonic `l_orderkey` while scanning and falls back if the input is not ordered, so it acts as an ordered-input optimization rather than assuming all datasets are sorted.
      - Q09 matched-index staging was re-tested as a possible default. Same-binary samples were noisy and the latest A/B did not consistently beat the fused row loop, so it remains opt-in behind `DODAM_Q09_ENABLE_MATCHED_INDEX=1`.
      - Q12 row-group chunk-size and stream fallback were re-tested after projection wrappers. No chunk setting gave a clear retained win over the current default; the remaining Q12 cost is still mostly lineitem scan/decode plus shipping-mode predicate work, not the orders pass.
      - Correctness/build checks passed (`cargo fmt --check`, `cargo test -q`, release build, `git diff --check`). SF=1 in-process TPC-H still ran `22/22`; focused samples stayed noisy but Q21 generally improved versus disabling ordered-lineitem, while Q09 matched-index default was intentionally not retained.
    - Reduced wasted CLI COPY planning attempts for materialized aggregate/subquery shapes:
      - `dodam query "COPY (...)"` now skips the direct-sink and streaming join probes when the inner SQL plainly contains `GROUP BY`, `ORDER BY`, aggregate functions, `EXISTS`, or subquery `IN`. This keeps scan/join COPY fast paths intact while avoiding parser/planner work that can only return `None` for TPC-H aggregate shapes.
      - Focused `DODAM_PROFILE_COPY=1` showed Q12 direct/streaming probe time drop from about `6.8ms + 0.3ms` to `0`, and total single-query profile moved from about `135ms` to about `123ms` in that sample. Full CLI 22-query totals remained noisy, so treat this as a small retained CLI overhead cleanup rather than a major engine win.
      - Rejected Q12 row-group pruning on `l_receiptdate`: current SF=1 lineitem row groups all overlap the 1994 receipt-date range, so no row groups were skipped and the extra pruning setup did not help.
      - Rejected Q12 pending-map `FastHashMap`: focused samples were neutral to slightly worse than the default hasher, likely because the dominant cost is scan/decode and selected-row work rather than generic hash quality.
      - Rejected Q12 vector partials and priority-first join-aggregate lowering: vector partials added final reconstruction cost, and priority-first had to build a full `orders` priority map before the lineitem scan, regressing Q12 substantially on this data.
    - Added `dodam query-file <path>` to execute many semicolon-separated SQL/COPY statements in one Dodam process through the same `query` execution path:
      - This is mainly a benchmark-control and operational convenience feature. It keeps catalog/metadata/file cache state alive across statements and avoids measuring process startup once per TPC-H query.
      - SF=1 TPC-H 22-query Parquet COPY sample: `query-file` completed in about `0.84s`, close to the existing in-process runner's `0.80s`. A same-process DuckDB CLI script completed in about `0.67s`, so the fairer current gap is roughly `1.25x` for CLI COPY and `1.19x` for direct in-process execution, rather than the larger per-query CLI gap.
      - DuckDB `.timer` per-query comparison showed the biggest remaining same-process gaps around Q12, Q03, and Q01; Q09/Q18/Q21 are already competitive or faster in the sampled SF=1 run.
    - Started clarifying the Engine/CLI boundary:
      - Added `SqlResultSink` plus `execute_sql_to_result_sink` in the SQL/engine-facing layer. Direct sink, streaming, and materialized fallback selection now lives with SQL execution instead of in `main.rs`.
      - CLI still owns command-line parsing, COPY statement parsing, and concrete stdout/file sink construction for now, but it no longer decides the direct/stream/materialize execution strategy.
      - SF=1 TPC-H `query-file` warm samples stayed around `0.68s`, matching the pre-refactor `0.67-0.69s` range, so this was retained as a structural cleanup rather than a speed claim.
    - Continued the Engine/CLI boundary cleanup by moving COPY statement parsing into the library:
      - Added `dodam::copy` with `parse_copy_to_select`, `CopyFormat`, and `ParquetCopyOptions`.
      - `main.rs` no longer depends on `sqlparser` for COPY parsing or owns COPY option sanitization/parsing. It still owns concrete stdout/file sink construction and output profile printing.
      - COPY option smoke test with Parquet `COMPRESSION`, `DICTIONARY`, `ROW_GROUP_SIZE`, `WRITE_BATCH_SIZE`, and `DATA_PAGE_ROW_COUNT_LIMIT` passed. SF=1 TPC-H `query-file` warm samples remained around `0.68-0.69s`, so this was retained as structure-only cleanup.
    - Moved COPY file sink implementations into the library:
      - `dodam::copy` now owns `CopyFileQuerySink`, CSV serialization, Parquet `ArrowWriter` setup, COPY sink stats, and COPY profile printing.
      - CLI no longer owns file output serialization details; it creates the library sink and passes it to `execute_sql_to_result_sink`. Stdout still remains a CLI concern but reuses the shared CSV `RecordBatch` writer helper.
      - Parquet COPY option smoke test passed, and SF=1 TPC-H `query-file` warm samples stayed in the `0.67-0.71s` range. This is retained as an Engine/CLI boundary cleanup rather than a performance change.
    - Added a library-owned stdout sink:
      - `dodam::output::StdoutQuerySink` now implements `SqlResultSink` and `RecordBatchSink`, reusing the shared CSV `RecordBatch` writer.
      - CLI now constructs `StdoutQuerySink::new()` and no longer owns stdout query serialization, `QueryOutput` handling, or query-summary formatting.
      - Stdout SELECT smoke and Parquet COPY smoke passed after the move. This further narrows CLI to command parsing, engine setup, and dispatch.
    - Generalized direct join sink planning out of string heuristics:
      - Replaced the `sql_may_use_direct_or_streaming` substring gate with a parsed `SqlQuery` shape rule that builds a reusable `JoinParquetRequest` for plain projected joins.
      - `execute_sql_to_result_sink`, `try_execute_sql_to_sink`, and `try_execute_sql_streaming` now share the same direct-sink planning rule instead of duplicating join eligibility checks and request construction.
      - Aggregate, `ORDER BY`, aliased, expression-predicate, materialized-subquery, and multi-comma join shapes fall through to the materialized SQL path by query structure rather than by SQL text tokens. Added parser-only unit coverage for accepted/rejected sink shapes.
      - SF=1 TPC-H `query-file` Parquet COPY samples after the change were `0.850s` cold-ish then `0.696s` and `0.671s` warm, matching the previous `~0.67-0.71s` range. This is retained as a general planning cleanup, not a speed claim.
    - Generalized more TPC-H fast-path mechanics into shared execution rules:
      - Moved the scan-to-partial-to-merge row-group/stream fallback helper from `sql.rs` into `DodamEngine::parquet_scan_fold_chunks`. Q01/Q06/Q15/Q19/Q20 now call an engine primitive instead of an SQL-local TPC-H helper.
      - Moved `Q01DecimalInput` and the discounted-revenue raw Decimal128 helpers into `execution::decimal` as `DecimalInput`, `decimal_input`, and `decimal_discounted_revenue_*`. This removes the misleading Q01 ownership from Decimal128 raw loops reused by many query paths.
      - Moved typed row iteration helpers (`i64/date32/utf8`, `i64/i64/date32`) into `execution::typed_rows`, keeping the Q04/Q10-style typed loop pattern reusable outside TPC-H code.
      - Added a shared `should_use_i64_set_row_filter` gate for key-set RowFilter decisions. Q10/Q08 default-enabled row filters and Q09 opt-in row filter now share the same selected-key/payload-width policy instead of separate query-local env checks.
      - Correctness/build checks passed, and SF=1 TPC-H `query-file` Parquet COPY samples were `0.839s` cold-ish then `0.695s` and `0.694s` warm. This is retained as specialization cleanup; performance is intentionally neutral.
    - Tried Q12 late materialization for the lineitem shipping predicate:
      - The path reads `l_shipmode/l_commitdate/l_receiptdate/l_shipdate` first, builds a row selection, and then reads `l_orderkey` only for selected rows. This fixed an important projection-order assumption in the late path by using column-name lookup instead of ordinal-only lookup.
      - SF=1 Q12 selectivity was very low (`30,988 / 6,001,215`, about `0.52%`) with about `60k` selector runs, so it looked promising. However, same-process Q12 10-repeat samples did not beat the existing row-group map path once the full predicate pass plus row-selection payload read was included.
      - Enabling it by default with broader late-materialization sampling regressed Q12 warm samples from roughly `0.55-0.57s` per 10 repeats to `0.59-0.61s`, so the default was reverted. The path remains opt-in behind `DODAM_Q12_ENABLE_LATE_MATERIALIZE=1` for clustered/selective datasets.
    - Revisited Q03 order-build cost:
      - Tried routing the Q03 `orders` build-map scan through the row-group map API. It was slower than the existing stream/chunk fold path: Q03 10-repeat warm samples were roughly `0.47-0.49s` with row-group map versus `0.42-0.45s` with the stream path. The likely reason is that `orders` has only 16 row groups and a narrow projection, so row-group task setup and merge overhead outweigh stream-layer savings. Left it opt-in behind `DODAM_Q03_ENABLE_ORDER_ROW_GROUP_MAP=1`.
      - Kept a smaller typed-loop cleanup for Q03 order rows: no-null `Int64/Date32` batches now use raw value slices, and dense customer-key sets use direct dense membership lookup. This is a general hot-loop cleanup for integer-key build filters, not a query-output specialization. Q03 10-repeat warm samples stayed in the good `~0.44-0.46s` range, and full SF=1 `query-file` warm samples were around `0.67-0.70s` in the local run.
    - Added Parquet reader-level scan/decode attribution:
      - `ScanPlanMetrics` now records `ParquetBatchReader::next()` calls, EOF calls, emitted rows/batches, and accumulated reader-next time. `DODAM_SCAN_PROFILE=1` prints these fields for ordinary scan streams, and row-group map profiling prints the same reader-next attribution per row-group chunk.
      - Q01/Q03/Q12 profiles show `reader_next` is essentially the same as the previously measured `read_next`, while fused consumer time is much smaller. Example Q01 row-group chunks spend roughly `16-40ms` inside reader `next()` and only about `2-4ms` in the aggregate consumer.
      - Q03/Q12 narrow `orders` scans show the same pattern: scan `decode` and `parquet_next` match closely, while downstream aggregation/build work is small. This means the current remaining gap is mostly Arrow/Parquet reader decode plus `RecordBatch` construction/materialization, not the typed consumer loops.
      - SF=1 TPC-H `query-file` Parquet COPY samples with the instrumentation were `0.830s` cold-ish then `0.692s` and `0.696s` warm, so the profile fields are retained as low-overhead diagnostics for the next scan/decode optimization work.
    - Tried and rejected a Q01 direct Parquet column-reader path to bypass Arrow `RecordBatch` construction:
      - Built an opt-in local experiment that reads TPC-H lineitem's physical `INT64 DECIMAL(15,2)`, `INT32 DATE`, and `BYTE_ARRAY STRING` columns through parquet-rs `RowGroupReader::get_column_reader()` and aggregates directly into Q01 slots.
      - Correctness matched the default path except for tiny floating-point accumulation-order differences.
      - It did not beat the retained Arrow row-group map path. Q01 warm samples were roughly `0.08-0.12s` for the direct path versus `0.06-0.10s` for the default path, and full SF=1 TPC-H `query-file` with the direct path warmed around `0.76s` versus the default `0.68-0.69s`. Wiring the experiment through Dodam's `CachedParquetChunkReader` did not help; Q01 direct samples worsened to roughly `0.13-0.15s`.
      - Likely causes: the low-level reader still materializes per-column `Vec`s, loses Arrow's optimized string array path, and does not avoid enough decode work to pay for the extra per-column orchestration. Conclusion: simply replacing Arrow `RecordBatchReader` with row-group column readers is not enough; the next useful direction is encoding-aware attribution and then cache-integrated page/column decode only for columns where the profile justifies it.
    - Added projected Parquet column attribution:
      - `DODAM_PARQUET_COLUMN_PROFILE=1` now prints projected column compressed bytes, uncompressed bytes, and encoding sets during scan planning. This is intentionally diagnostic-only and does not alter the scan path.
      - This should be used before the next custom decode attempt so that page/encoding work targets the largest decoded columns instead of guessing from query shape alone.
    - Made aggregate column-read decisions explicit:
      - Added a shared aggregate column read plan that separates payload columns (aggregate inputs and group keys), predicate columns (filter inputs), and normal scan columns (`payload + predicate`).
      - Normal aggregate scans and late-materialized aggregate attempts now use the same helper, so future optimizer rules can decide between full scan and late payload reads from one place instead of duplicating projection logic.
      - `DODAM_COLUMN_READ_PROFILE=1` prints this decision; `DODAM_PARQUET_COLUMN_PROFILE=1` also enables it so column role decisions and Parquet bytes/encoding attribution can be read together.
    - Tried and rejected preserving Parquet dictionary arrays for Q01's tiny string group keys:
      - Added a local opt-in experiment that forced `l_returnflag/l_linestatus` to Arrow `Dictionary<Int32, Utf8>` and consumed dictionary keys directly in the Q01 aggregate loop.
      - Correctness matched Q01, and Q01-only warm samples improved only from roughly `0.068-0.085s` to `0.065-0.070s`.
      - Full SF=1 TPC-H `query-file` Parquet COPY was unchanged (`~0.68-0.70s` warm), and a global column-name env broke other queries that expect `StringArray` for `l_returnflag` unless projection-aware guards were added.
      - Conclusion: this is not a meaningful general optimization. The remaining Q01 gap is not dominated by decoding one-byte string group keys; it is still mostly numeric column decode/materialization and scan/operator dispatch.
    - Tried and rejected forcing Q21 ordered-lineitem counting through preserve-order scan:
      - The ordered path did become active, but Q21 warmed around `0.086-0.090s` with `Q21 ordered lineitem counts` at `~75-78ms`.
      - The existing parallel row-group fallback was faster in the same run range (`~0.057-0.064s` Q21). Preserving physical order loses too much scan parallelism for SF=1 lineitem.
      - Conclusion: ordered-run aggregation is only worth revisiting if the scan can preserve row-group order while still decoding row groups in parallel and merging boundary states deterministically.
    - Added Q21 parallel ordered-lineitem boundary merge:
      - Reworked the ordered Q21 lineitem path to use `parquet_row_group_map`, preserving parallel row-group decode while returning chunk outputs in row-group order.
      - Each chunk counts fully internal orders and returns first/last order-state boundaries. The final merge stitches adjacent boundaries deterministically before counting qualifying suppliers.
      - Same-binary comparison: fallback Q21 was `~0.056-0.063s`; the new ordered partial merge was `~0.017-0.018s`, with identical Q21 Parquet output. Full SF=1 `query-file` warmed around `0.664-0.676s`.
      - Generalized the boundary stitching into `merge_ordered_row_group_chunks` plus reusable ordered boundary/chunk types in the engine layer. Q21 now supplies only its selected-order state merge and emit logic. Post-generalization Q21 stayed around `~0.018-0.020s` with identical output.
    - Rechecked Q09 lookup/layout options:
      - `DODAM_Q09_SUPPLYCOST_FANOUT=1` was not stable enough to become the default. One run set favored it slightly, but the next same-binary comparison had packed supply-cost lookup faster (`packed ~0.047-0.054s`, fanout/default attempt ~0.052-0.053s` for Q09 warm samples). Kept packed as default and retained fanout as opt-in.
      - Added Q09 supply-cost layout profiling (`layout`, `entries`, `part_keys`, `max_fanout`) under `DODAM_TPCH_PROFILE=1`; SF=1 uses `packed_u64`, `42656` entries, `10664` part keys.
      - Added a packed/fallback accumulator for Q09 `(nationkey, year)` profit groups to avoid tuple hash keys on the hot group update. Q09 output matched prior Parquet output; Q09 samples were mixed (`~0.046-0.059s`), while full `query-file` warm samples were `~0.641-0.671s`. Retain as a small general packed-key cleanup, not a major speed claim.
    - Continued the latest fast-path generalization pass:
      - Tried converting Q10's `orderkey -> custkey` returned-order map to `AdaptiveI64Map`; it regressed focused Q10 from the retained `~45ms` range to about `~75-79ms`, likely from dense-range/cache cost and conversion/materialization overhead. Reverted it.
      - Reordered Q12 shipping-mode predicates so cheap Date32 predicates run before UTF-8 shipmode slicing/comparison in both the typed aggregate loop and the late-materialized selector. Focused Q12 samples were small/neutral-positive around `~46-50ms`, and output matched prior results.
      - Extended `merge_ordered_row_group_chunks` to emit the full boundary (`key + state`) instead of state only, then rewired the engine's ordered `i64 + Decimal128 SUM > threshold` merge onto the same helper used by Q21. This removes the separate Q18-style carry merge logic and makes ordered row-group boundary stitching a common engine rule. Full SF=1 in-process checks stayed `22/22`; warm samples showed Q18 around `~17-19ms`, Q21 around `~17-20ms`, and Q12 around `~45-48ms`.
    - Added `--only` to the in-process TPC-H runner so focused experiments can run a single query or comma-separated query-name filters without creating temporary query inventories.
    - Rechecked the current Q12 opt-in paths with focused repeats:
      - `DODAM_Q12_ENABLE_ORDER_ROW_FILTER=1` and late materialization remained neutral-to-noisy for the warm single-query shape. Even though cold profile can show less `orders` payload decode, selected keys are spread enough that row-selection overhead usually offsets the saved narrow `o_orderpriority` read. Kept both non-default.
      - Q12 row-group chunk `4` plus order row-filter was also only a sub-millisecond/noise-level change, so the default remains unchanged.
    - Retained Q03 sorted integer lookup by default:
      - The Q03 lineitem revenue scan now defaults to `SortedI64Lookup` for the built order-key payload, with `DODAM_Q03_DISABLE_SORTED_ORDER_LOOKUP=1` as the rollback knob.
      - Focused SF=1 warm Q03 repeats moved from about `~39-41ms` with the hash lookup to about `~32-34ms` with the sorted contiguous lookup. This is a general probe-layout rule for large fact scans probing an immutable integer build-key set: fewer random hash-table touches beat the extra binary-search comparisons in this shape.
      - Full SF=1 in-process TPC-H still passed `22/22`; warm Q03 samples were around `~31-34ms`, with full warm totals around `~0.52-0.55s` in the local run.
    - Investigated DuckDB's Q12 advantage:
      - DuckDB `EXPLAIN ANALYZE` shows a hash-join dynamic filter pushed into the `orders` Parquet scan: `o_orderkey` min/max plus `o_orderkey IN BF(l_orderkey)`. The scan outputs about `34,850` orders rows instead of the full `1.5M`.
      - Added an opt-in Q12 orders late-materialized payload path (`DODAM_Q12_ENABLE_ORDER_LATE_MATERIALIZE=1`) that scans `o_orderkey`, builds a row selection from the pending lineitem keys, then reads only selected `o_orderpriority` rows and aggregates directly.
      - Rejected it as the default: focused warm Q12 regressed from roughly `~46-48ms` to `~54-56ms`. The extra reader setup, row-selection construction, and sparse selector application cost more than the saved priority payload decode. This confirms the next useful direction is a cheaper scan-integrated dynamic filter, not a second-pass late payload path.
      - Tried a lower-level opt-in direct Parquet column-reader path (`DODAM_Q12_ENABLE_ORDER_DIRECT_COLUMN_READER=1`) that reads `o_orderkey` first and conditionally decodes/skips `o_orderpriority` with `ColumnReader::skip_records`.
      - The first selective version reduced cached/read bytes but regressed warm Q12 (`~64-77ms` versus the default `~60-70ms`) because Q12's selected orders are sparse and randomly distributed: about `29K` hits across `1.5M` orders produced about `28.5K` payload runs.
      - Added an adaptive guard that only uses selective payload decode when the per-batch run count is low; Q12 correctly falls back to full payload batch reads (`selective_batches=0`, `full_batches=96`), but the direct reader still remains slower (`~65-85ms`) than the Arrow dictionary-preserved default because it bypasses the existing Arrow reader/cache pipeline and does not eliminate the dominant lineitem-side work.
      - Conclusion: page/column-level fused payload decode is only useful when selectors are clustered enough to skip whole pages or large runs. For Q12's random dynamic-filter shape, the better general direction is scan-integrated Bloom/dynamic filtering plus cheaper pipeline/materialization, not per-hit payload skipping.
      - Added a default Q12 lineitem dictionary-preserved `l_shipmode` row-group map path with `DODAM_Q12_DISABLE_LINEITEM_DICTIONARY_SHIPMODE=1` as the rollback knob.
      - Initial implementation exposed an important engine contract bug: dictionary schema projection can return projected batches in schema/physical order (`l_orderkey,l_commitdate,l_receiptdate,l_shipdate,l_shipmode`) instead of requested projection order. The Q12 dictionary path now resolves columns by name before entering the typed loop.
      - Focused Q12 on/off results were effectively neutral (`~0.57s` total over 8 repeats in both cases), but profile shows the dictionary path can reduce warm lineitem chunk consume/read costs slightly by comparing integer dictionary keys instead of UTF-8 bytes. Kept it as a small general low-cardinality string cleanup, not a major speed claim.
      - Fixed the projection identity issue at the storage boundary: `ParquetBatchReader` now preserves requested `Projection::Columns` order for every returned `RecordBatch`, including dictionary-preserved scans and row-selection scans.
      - Added a regression test that scans `payload,id` with dictionary-preserved `payload` and asserts the returned batch schema remains `payload,id`. This keeps query/execution code from having to defensively rediscover columns whenever the Arrow Parquet reader changes projected output order.
      - Validation after the storage-level fix: `cargo test -q` passed; focused Q12 passed; SF=1 TPC-H in-process `66/66 ok` with warm totals in the existing range (`~0.51-0.55s`).
      - Started closing the fast-path instrumentation gap: generic `ScanExec` already had an aggregate scan profile, and now row-group-map plus dictionary row-group-map return common per-chunk scan metrics and print one operator-level summary line. This makes Q12 lineitem-style fast paths easier to compare against normal scan profiles. Late-materialized and direct-column paths still need the same treatment.
      - Tightened the fused scan/aggregate primitive:
        - `parquet_row_group_map` now has an internal metrics-aware result form, and `parquet_scan_fold_chunks` logs scan `read_next`, consumer, and final merge time in one scan-fold line instead of reporting only the near-zero final merge as `fused_total`.
        - Added `DodamEngine::parquet_scan_accumulate_chunks`, a reusable variant where each decoded batch mutates the worker-local partial state directly. Q01 now uses this path, avoiding `Map(batch) -> Q01GroupSlots -> merge` for every batch.
        - Effect is useful but limited: focused SF=1 Q01 warm samples moved from roughly `~0.026s` to `~0.020-0.024s`, while summed consumer time stayed around `~64-68ms`. This confirms the dominant remaining Q01 cost is still Parquet/Arrow reader decode and RecordBatch materialization, not small-group aggregate state merging.
        - Validation: `cargo check -q`, `cargo test -q`, and SF=1 in-process TPC-H `44/44 ok` over two repeats (`0.760s` cold-ish, `0.545s` warm in the local run).
      - Extended the accumulate experiment carefully:
        - Applied `parquet_scan_accumulate_chunks` to Q06's default scalar aggregate and Q15's supplier revenue aggregate. Q06 is effectively neutral, while Q15 focused warm samples stayed in the `~14-18ms` range and avoids one batch-local HashMap allocation/merge layer. Kept both because the change is structurally cleaner and not a measured regression.
        - Tried the same worker-local mutation idea for Q20 and Q12 lineitem. Both were rejected: Q20 stayed around `~23-25ms` focused and did not beat the retained `~19-20ms` full-run samples; Q12 lineitem `into` increased focused/full samples instead of reducing them. Likely cause is worse worker-local HashMap growth/cache behavior versus smaller batch-local maps that merge cheaply.
        - Tried a Q20 dense membership/raw-decimal inner loop for `AdaptiveI64Set` + Decimal128 quantity. It did not move Q20, so the experiment was reverted with the Q20 accumulate change.
      - Rechecked Q12 against DuckDB after the current scan changes:
        - Focused DuckDB CLI Parquet output samples: Q12 about `~36ms`; Dodam still above that.
        - Q12 lineitem row-group chunk sweep showed chunk `1` is consistently better than chunk `2/4/8` for Q12-only warm runs (`~43-45ms` versus `~46ms+` and `~50ms+`). Default Q12 lineitem chunk changed from `2` to `1`.
        - Re-tested Q12 orders row-filter/Bloom/late paths. Bloom and row-filter can look faster in one run, but same-binary repeats showed Bloom-on regressing to about `~56ms` while Bloom-off stayed around `~43-48ms`; Bloom remains opt-in. Full-run totals remain noisy, so this is a focused Q12 improvement rather than a claimed total TPC-H win.
        - Re-tested the existing Q12 orders fused row-group-map path. It looked promising in one run, but same-binary repeats with the default-on change regressed focused Q12 to about `~60-62ms`; kept it opt-in.
        - Replaced Q12 orders' one-task-per-batch aggregate with chunked batch fold and set the default orders chunk to `2`. This is a small task-granularity cleanup, not a scan breakthrough: chunk `1/2` focused samples were around `~44-45ms`, chunk `4+` was worse, and the latest full SF=1 repeat4 sample was Q12 `0.0447s`, total `0.536s`.
        - Added a reusable dictionary row-group-map API that also accepts pruning predicates, then wired Q12 lineitem's `l_receiptdate` range into it. On the current SF=1 layout it prunes `0/53` row groups, so it is structurally useful but not a Q12 speed win.
        - Tried an opt-in Q12 lineitem Arrow RowFilter path on the same `l_receiptdate` range. It regressed focused Q12 from about `~45ms` to `~52-54ms`; the row-selection/payload-filter cost is larger than the saved downstream checks for this layout. Kept it opt-in only for diagnostics.
        - Re-tested Q12 lineitem late materialization after the current changes. It can look faster in one run and has very low selected ratio (`~0.5%`), but same-binary on/off repeats and full-run samples were not stable enough to default it; it remains opt-in.
        - Found a real Q12 regression source: forcing `o_orderpriority` to dictionary arrays in the orders scan is now slower than the normal StringArray scan. Same-binary focused repeats showed dictionary priority around `~60-62ms` versus normal priority around `~44-47ms`. Changed Q12 orders dictionary priority from default-on to opt-in (`DODAM_Q12_ENABLE_ORDER_DICTIONARY_PRIORITY=1`). Latest focused Q12 warmed around `~43-45ms`; latest full SF=1 repeat4 sample was total `0.510s`.
        - Revisited Q12 orders scan/operator fusion after disabling dictionary priority. Fused row-group-map can win focused Q12 samples, and row-group chunk `2` is better than `1/4/8` for the opt-in fused path. However, same-binary full-run comparison still favored the retained stream/chunked-fold path (`fused_on` total `~0.537s`, `fused_off` total `~0.520s` in the checked run). Conclusion: scan/operator fusion needs a cost rule; keep Q12 orders fused scan opt-in with improved chunk `2`, not default.
        - Added a generic late-materialization policy gate for selector run density (`selector_runs / selected_rows`). This captures sparse selectors that have low selected ratio but many tiny runs, which are bad for second-pass row-selection payload reads.
        - Applied the gate to Q12 late materialization with `DODAM_Q12_LATE_MAX_RUNS_PER_SELECTED` default `1.25`. The policy correctly models why Q12's selector is unattractive (`~60K` runs for `~31K` selected rows), but the sample pass itself costs enough that opt-in `late_policy` is slower than default (`~53-58ms` vs `~43-45ms` in the focused run). Keep Q12 late opt-in only; the new policy is retained for future late-materialization decisions where sampling overhead is amortized by wider payloads.
        - Re-tested Q12 pending-order partial map initialization. A direct `FastHashMap` swap was rejected again because focused samples were neutral/worse and the dominant cost remains scan/decode, but giving each selective lineitem row-group partial a modest initial capacity helped stability. Sequential focused SF=1 comparison showed cap `0` over 12 repeats totaling about `0.706s`, while cap `2048` totaled about `0.569s` with warm repeats mostly `~43-45ms`. Default Q12 pending-map initial capacity is now `2048` with `DODAM_Q12_PENDING_MAP_INITIAL_CAPACITY` as the tuning knob. Full SF=1 in-process TPC-H stayed `88/88 ok`, repeat 4 total about `0.548s`, Q12 about `46ms`.
        - Pushed already-derived build-side key/date ranges into more lineitem row-group scan paths:
          - Q03 now passes both `l_orderkey` range and `l_shipdate > cutoff` to the pruning-aware row-group map.
          - Q05/Q07 now use `parquet_row_group_map_pruned` and pruned stream fallbacks; Q05 passes the candidate `l_orderkey` range and Q07 passes candidate `l_orderkey` plus `l_shipdate BETWEEN`.
          - Q08's row-filter-disabled path now uses `l_orderkey`/`l_partkey` range pruning, while the default `l_partkey` row-filter remains enabled because focused samples still favor it over pruning-only.
          - On the current SF=1 layout these predicates did not skip lineitem row groups (`53/53` still scanned), so this is retained as a general planner/execution cleanup rather than a claimed current-data speed win. Full SF=1 in-process TPC-H stayed correct (`88/88 ok`) with a warm repeat around `0.509s`; Q03/Q05/Q07/Q08 warm samples were around `32ms/25ms/28ms/35ms`.
        - Added a reusable DuckDB-vs-Dodam TPC-H comparison script:
          - `scripts/compare_tpch_duckdb.py` extracts the TPC-H SQL fixtures, runs Dodam's in-process Parquet-output benchmark, runs DuckDB CLI with matching Parquet COPY output options, and reports per-query medians, ratios, absolute gaps, median-sum ratio, and optional JSON output.
          - Smoke test on Q06/Q13 passed; full SF=1 CLI-per-query DuckDB comparison with repeats `3`, warmup `1` showed Dodam median-sum about `0.527s` vs DuckDB about `1.073s` in this harness, with Q12 the only slower Dodam query (`~47.5ms` vs DuckDB `~35.6ms`). Important caveat: this harness intentionally runs DuckDB as a fresh CLI process per query, so it is repeatable and output-equivalent but not a same-process DuckDB comparison.
        - Generalized the integer set RowFilter gate:
          - Q08/Q09/Q10 now share a key-range density policy instead of query-local switches. The policy uses `DODAM_I64_SET_ROW_FILTER_MAX_DENSITY` (default `0.35`) and `DODAM_I64_SET_ROW_FILTER_ALWAYS_ALLOW_KEYS` (default `4096`) to avoid dense/wide key sets where Arrow RowFilter spends time scanning many mostly-kept values.
          - Focused Q08/Q09/Q10 validation stayed correct; timings were noisy but did not show a correctness or obvious regression signal.
        - Tightened Parquet column attribution for the scan/decode bottleneck work:
          - `DODAM_SCAN_PROFILE=1` and `DODAM_TPCH_PROFILE=1` now also enable projected column compressed/uncompressed byte attribution, so the normal scan profile shows which decoded columns dominate without needing a second flag.
          - Row-group chunk-level column attribution is now opt-in via `DODAM_PARQUET_COLUMN_CHUNK_PROFILE=1`; the default profile stays at logical scan/plan granularity to avoid huge logs.
          - Focused Q01/Q03/Q08/Q12 profile confirms the next meaningful decode work should target large numeric/date payload columns rather than tiny low-cardinality strings. Q01 is dominated by `l_extendedprice` compressed bytes, Q03 by `l_extendedprice` plus `l_orderkey/l_shipdate`, and Q12 by `l_orderkey` plus date columns.
        - Revisited Q12 orders probe after the scan attribution work:
          - Re-tested defaulting the Q12 orders `o_orderkey` RowFilter using the shared i64 set-filter cost gate. It looked good in one focused run, but same-binary and DuckDB comparison runs regressed Q12 (`~60ms` with RowFilter default versus `~46ms` with the retained full orders scan in the checked comparison). Reverted the default; exact RowFilter remains opt-in because Arrow RowFilter setup and sparse row-selection overhead are still noisy for this narrow two-column orders scan.
          - Tried a sorted immutable pending-order lookup for the orders probe, reusing the same `SortedI64Lookup` layout that helped Q03. This avoids converting the sparse `~29K` pending order keys into a large dense vector keyed by max TPC-H orderkey, and it keeps the probe layout compact.
          - This was a large win: focused Q12 warm samples moved from roughly `~46-49ms` to `~14-16ms`. Profile shows the orders side wall time drops to about `6-8ms` after warmup, while lineitem remains the larger decode component.
        - Made sorted pending lookup the default for Q12 orders with `DODAM_Q12_DISABLE_SORTED_PENDING_LOOKUP=1` as rollback.
        - Full SF=1 TPC-H in-process stayed `22/22 ok`, with Q12 about `~15ms` and warm totals around `~0.49-0.51s`. The DuckDB CLI-per-query comparison now shows no Dodam-slower TPC-H query in this harness: median-sum Dodam `~0.503s` vs DuckDB `~1.079s`, Q12 `~15.4ms` vs DuckDB `~36.2ms`. Caveat remains that DuckDB is measured as a fresh CLI process per query.
        - Generalized the late-materialized integer-key selection fast path:
          - Added a reusable `I64SetLateSelectionState` and `i64_set_late_build_selection_batch_into` helper for the common pattern: scan one Int64 predicate/key column, build a `RowSelection`, retain selected keys, and consume a narrower payload batch by selected-key offset.
          - The helper keeps the dense-set membership shortcut, so moving Q09/Q10 onto the common rule does not throw away the previously useful dense `AdaptiveI64Set` path.
          - Rewired Q10 customer late materialization and Q09 late partkey selection to the shared helper. This is mostly a structure/generalization change, not a query-specific performance claim; it makes the rule reusable for future small-key-set + wide-payload late materialization candidates.
          - Validation: `cargo test -q` passed. Focused Q09/Q10 DuckDB single-process comparison over SF=1, repeats `8`, warmup `2`: median-sum Dodam `0.079521s` vs DuckDB `0.094000s`; Q09 `43.382ms` vs `57.500ms`, Q10 `36.140ms` vs `36.500ms`. Full SF=1 repeats `4`, warmup `1`: median-sum Dodam `0.493245s` vs DuckDB `0.562000s`; remaining slower queries by largest gap were Q03, Q08, Q16, Q07, Q22.
- First TPC-H coverage implementation target:
  - Q6 support, because it is single-table and mainly needs aggregate input expressions, `BETWEEN`, and date interval arithmetic.
  - Initial Q6 parser/execution blockers are cleared for single-table Parquet inputs, including a canonical-shape Q6 fixture; next step is real TPC-H table registration and then multi-table `FROM` planning.
- Add SQL tests for more alias and expression combinations.
- Extend richer Parquet type support beyond residual filtering:
  - explicit behavior for nested/list/struct projection in SQL and CLI output
  - casts and richer date/time literal forms
- Add more expression parser support only after parser structure remains clean.

### 7. Storage/Catalog Next Steps

- Add catalog table removal/rename commands.
- Expand directory partition pruning beyond positive `=` and `IN`:
  - range predicates for ordered partition values
  - `IS NULL`/`IS NOT NULL`
  - safe handling of `OR`
- Avoid reading a physical Parquet column when a query projects only partition columns.
- Add a real CSV scan implementation behind `ScanExec`.
- Add JSON and Arrow IPC scan implementations after CSV.
- Improve statistics:
  - column-level compressed bytes
  - null counts
  - row-group min/max exposure through catalog APIs
  - approximate NDV later
- Add Iceberg metadata/snapshot planning from manifests instead of local directory scans.
- Revisit S3/object storage later with:
  - range-read capable object store
  - remote metadata cache
  - listing abstraction
  - retry/backoff and request metrics

## Validation Checklist

- Run before considering the current branch healthy:
  - `cargo fmt`
  - `cargo test`
  - `cargo clippy --all-targets --all-features -- -D warnings`
- Run before claiming performance:
  - Run benchmark variants one at a time. Do not run Dodam/DuckDB comparison jobs or env-var A/B variants in parallel, especially on SF=10+, because Parquet IO, OS page cache, and Dodam file-cache activity interfere with each other and make the numbers unusable.
  - release build
  - Dodam CLI benchmark
  - DuckDB CLI benchmark
  - engine-level criterion benchmark
  - profile with `DODAM_PROFILE_COPY=1` for COPY-heavy queries

### Benchmark Notes

- SF=10 Q04/Q12 tuning note:
  - A parallel A/B run of Q04 i64-set row-group chunk variants was discarded. Running the two benchmark processes concurrently caused IO/cache interference and produced worse, non-comparable numbers.
  - Benchmark policy going forward: every performance variant must be measured as a separate process run, with no other benchmark process active.
  - Q04 lineitem row-filter is now adaptive rather than always-on:
    - SF=1 benefits from the row-filter path (`~19-20ms` focused), but SF=10 regresses because the candidate key set is large enough that row-filter setup/selection overhead dominates.
    - Default rule: use Q04 lineitem row-filter only when candidate order keys are at or below `DODAM_Q04_LINEITEM_ROW_FILTER_MAX_KEYS` (default `100000`). `DODAM_Q04_ENABLE_LINEITEM_ROW_FILTER=1` and `DODAM_Q04_DISABLE_LINEITEM_ROW_FILTER=1` remain force knobs.
  - Q04 lineitem probe was parallelized with row-group map:
    - The old probe mutated the dense candidate-priority marker while scanning, which made the lineitem probe effectively sequential. The new default path collects matched orderkeys per row-group worker, then sort/dedups and counts priorities once at the end.
    - Chunk sweep on SF=10 Q04: chunk `1` around `~228ms`, chunk `2` around `~223ms`, chunk `4` around `~222ms`, chunk `8` around `~227ms`; default chunk remains `4` via `DODAM_Q04_LINEITEM_ROW_GROUP_MAP_CHUNK`.
    - SF=10 Q04 focused improved from current-default around `~294ms` to `~223ms`; SF=1 Q04 stayed around `~20ms`, so the row-group probe is now default with `DODAM_Q04_DISABLE_LINEITEM_ROW_GROUP_MAP=1` as rollback.
    - Full SF=10 median-sum after the Q04 row-group probe: Dodam `6.072s` vs DuckDB `5.798s` (`1.047x`), with Q04 gap down to about `~71ms`.
  - Q03 SF=10 retest:
    - Rechecked old opt-in knobs under SF=10. `DODAM_Q03_ENABLE_ORDER_ROW_GROUP_MAP=1`, `DODAM_Q03_ENABLE_REVENUE_VECTOR=1`, and row-group chunk `1/4/8` were all neutral or slower than the retained chunk `2` default.
    - Tried an opt-in orders `o_custkey` i64-set RowFilter (`DODAM_Q03_ENABLE_ORDER_ROW_FILTER=1`). It regressed Q03 focused from about `~402ms` to about `~419ms`; customer segment selectivity is not low enough to pay for Arrow RowFilter/setup overhead. Kept it opt-in only for diagnostics.
    - Switched Q03 order/revenue partial maps to `FastHashMap`. This made the hash lookup path better than the old sorted binary-search lookup on SF=10:
      - Q03 focused improved from about `~402ms` to `~366ms` with fast maps.
      - Disabling sorted lookup after fast-map conversion improved further to about `~335ms`; made hash lookup the default and left `DODAM_Q03_ENABLE_SORTED_ORDER_LOOKUP=1` as a diagnostic knob.
      - Latest Q03 focused default after the change is about `~326ms`; full SF=10 median-sum moved to Dodam `5.833s` vs DuckDB `5.778s` (`1.010x`), with Q03 gap about `~101ms`.
    - Re-tested Q03 order row-group map after fast maps; still neutral/slower (`~330ms` focused), so it remains opt-in only.
    - Tried a Q03 order stream-accumulate path to remove partial-map merge overhead. It regressed badly (`~731ms`) because it removed useful scan/batch parallelism. Kept it opt-in only for diagnostics via `DODAM_Q03_ENABLE_ORDER_STREAM_ACCUMULATE=1`.
  - Q14 dense dimension-key lookup:
    - Q14 was spending substantial CPU in the part dimension scan and lineitem promo lookup using `HashMap<i64,bool>` for dense TPC-H part keys.
    - Added reusable `DenseI64BoolLookup` and rewired Q14 SQL plus engine late-materialization path to use dense present/value arrays.
    - SF=10 Q14 focused improved from about `~344ms` in the previous full run to `~183ms`, now faster than DuckDB in the focused run (`~0.76x`). SF=1 Q14 also improved to about `~19ms` vs DuckDB `~27.5ms`.
    - Full SF=10 median-sum after Q04 row-group probe and Q14 dense lookup: Dodam `5.919s` vs DuckDB `5.787s` (`1.023x`). Remaining largest slower gaps: Q03, Q08, Q13, Q07, Q04.
  - Local Parquet file cache was a major SF=10 bottleneck:
    - With the old default 512MB cache and 4MB chunks, Q12 projected about `~470MB` compressed lineitem columns but the file cache read several GB due chunk over-read. Q04 showed the same pattern.
    - Q12 SF=10 focused: old default around `~500ms`, `DODAM_FILE_CACHE_CHUNK_BYTES=1048576` around `~323ms`, `524288` around `~242ms`, and `DODAM_FILE_CACHE_BYTES=0` around `~193ms`.
    - Q04 SF=10 focused improved from around `~611ms` after adaptive row-filter to around `~321ms` with file cache off.
    - Full SF=10 comparison improved from Dodam median-sum `12.206s` vs DuckDB `5.802s` (`2.104x`) to Dodam `6.185s` vs DuckDB `5.829s` (`1.061x`) with file cache disabled by default.
    - 512KB cache was better for small SF=1 Q04/Q12 (`~39ms` combined vs `~45ms` cache-off), but SF=10 full regressed to `~8.098s`; keep local default cache disabled and allow opt-in via `DODAM_FILE_CACHE_BYTES`.
  - SF=10 follow-up improvements after switching the comparison target to one benchmark process at a time:
    - Q08: `DODAM_Q08_PARTKEY_ROW_FILTER_ROW_GROUP_CHUNK=8` was better than the global chunk setting without hurting Q04, so Q08 now has its own default chunk. More importantly, the existing Q08 late-materialized lineitem path was much better at SF=10: focused Q08 moved from about `~507ms` to `~413ms`; made it default with `DODAM_Q08_DISABLE_LATE=1` as rollback.
    - Q13: moved LIKE-only dense count filtering to a hotter typed loop. Finder construction is now once per scan, not once per batch, and null-free Int64/StringArray batches avoid per-row schema/null checks. Focused Q13 improved from about `~382ms` to `~362ms`. Projection-index hints were retained but only moved another `~1ms`. Changing the dense count vector from `u64` to `u32` was neutral/noisy, not a clear win.
    - Q07: row-group chunk sweep (`1/4/8`) did not help; larger chunks were worse. The real win was replacing integer-key default `HashMap` usage in the order/customer and volume group maps with the reusable `FastHashMap` integer hasher. Focused Q07 improved from about `~403ms` to `~366ms`.
    - Q10: applied the same integer-key `FastHashMap` rule to orders and returned-revenue maps. Focused Q10 improved from about `~390ms` to `~370ms`.
    - Q16: profile showed `part` group build, not partsupp distinct counting, was the dominant Rust-side cost. Reordered the part-group typed loop so numeric `p_size IN (...)` is checked before touching `p_brand`/`p_type`, and used fast maps for part/group intern state. Focused Q16 improved from about `~148ms` to `~112ms`; byte-finder supplier comment matching added a smaller follow-up win to about `~108ms`.
    - Latest full SF=10 single-process DuckDB comparison after these changes: Dodam median-sum `5.583s`, DuckDB `5.845s`, ratio `0.955x`. Remaining slower gaps are led by Q04 (`~70ms`), Q13 (`~65ms`), Q03 (`~57ms`), Q07 (`~37ms`), Q05 (`~34ms`), Q12 (`~30ms`), and Q10 (`~25ms`). Q16 is no longer slower in the full run.
  - Additional SF=10 follow-up:
    - Q04: replaced the row-group probe's matched-orderkey Vec + global sort/dedup with a dense atomic marker consume path. This is a general dense-marker probe rule: each matching lineitem atomically swaps the candidate marker to zero and increments a worker-local priority count, so duplicates are suppressed without a final sort. Focused Q04 improved modestly to about `~200ms`; with the atomic path, row-group chunk `2` beat `1/4/8/16`, so Q04 default chunk changed from `4` to `2`. `DODAM_Q04_DISABLE_ATOMIC_LINEITEM_PROBE=1` remains a rollback.
    - Q13: added a two-substring byte matcher for `LIKE '%x%y%'` / `NOT LIKE '%x%y%'`, avoiding the generic parts loop in the hot path. Focused Q13 improved to about `~354ms`, but full-run samples remain noisy and Q13 is still the largest positive gap.
    - Q03: tried a dense order membership sidecar before probing the order `FastHashMap`. It regressed focused Q03 (`~340ms` in the checked run), likely because the dense sidecar's cache footprint outweighed saved hash misses. Kept it opt-in only with `DODAM_Q03_ENABLE_DENSE_ORDER_PROBE=1`.
    - Latest full SF=10 single-process DuckDB comparison: Dodam median-sum `5.525s`, DuckDB `5.820s`, ratio `0.949x`. Remaining slower gaps: Q13 (`~73ms`), Q04 (`~64ms`), Q03 (`~42ms`), Q05 (`~38ms`), Q07 (`~29ms`), Q12 (`~21ms`), Q10 (`~19ms`).
  - Q13/Q05 follow-up after the previous commit:
    - Q13 dense right-count build is now parallelized with row-group map for LIKE-only right filters. This removes the sequential consumer loop over `orders[o_custkey,o_comment]` and merges dense `u32` count vectors afterward.
    - Q13 row-group chunk sweep: chunk `2` improved Q13 to around `~290ms`, chunk `4/8` to around `~232-234ms`, chunk `16` to around `~212-214ms`, while chunk `32` regressed to around `~326ms`. Default `DODAM_DENSE_RIGHT_COUNT_ROW_GROUP_CHUNK` is now `16`.
    - Q13 focused now beats DuckDB in this harness (`~214ms` vs `~292ms`); full SF=10 sample showed Q13 `~243ms` vs DuckDB `~296ms`.
    - Q05 received the same integer-key `FastHashMap` cleanup that helped Q07/Q10: order-customer maps and revenue group maps moved off default `HashMap`. Focused Q05 improved to about `~323ms` vs DuckDB `~299ms`, reducing but not eliminating the gap.
    - Q03 order row-group map was re-tested and made default-on with `DODAM_Q03_DISABLE_ORDER_ROW_GROUP_MAP=1` as rollback. The improvement is modest/noisy (`~9ms` in one focused run), while revenue-vector/sorted lookup remains slower.
    - Latest full SF=10 single-process DuckDB comparison: Dodam median-sum `5.493s`, DuckDB `5.835s`, ratio `0.941x`. Remaining slower gaps are Q03 (`~67ms`), Q04 (`~48ms`), Q07 (`~41ms`), Q12 (`~34ms`), Q05 (`~31ms`), Q10 (`~20ms`), and Q09 (`~19ms`).
  - Q03/Q04 follow-up:
    - Q04 candidate build no longer materializes a `HashSet<i64>` unless the lineitem RowFilter path is actually selected. The default dense-priority probe only needs the dense priority vector and candidate count.
    - SF=10 Q04 focused improved from about `~212.8ms` to `~191.9ms`; lineitem row-group chunk `2` still beat chunk `1/4`, so the default remains `2`.
    - Q03 customer segment filtering now builds an `AdaptiveI64Set` directly with a null-free typed/string-byte loop. This removes the default `HashSet` build and clone before the dense membership probe.
    - SF=10 Q03 focused improved from about `~335.5ms` to `~317.4ms`.
    - Tried converting Q12 pending-order map from default `HashMap` to `FastHashMap`; it regressed focused Q12 to about `~180ms`, so the change was reverted. In this path the later dense `AdaptiveI64Map` probe dominates and the faster integer hasher is not a clear win.
    - Latest full SF=10 single-process DuckDB comparison after Q03/Q04 changes: Dodam median-sum `5.436s`, DuckDB `5.798s`, ratio `0.938x`. Remaining slower gaps are Q03 (`~56ms`), Q04 (`~45ms`), Q05 (`~31ms`), Q12 (`~30ms`), Q10 (`~25ms`), Q07 (`~25ms`), Q08 (`~16ms`), and Q11 (`~15ms`).
  - Additional Q03/Q05/Q08 follow-up:
    - Added a metadata-backed constant integer column rule for Parquet `Int32`/`Int64` columns. Q03 uses it to avoid decoding `orders.o_shippriority` when every row group has exact `min=max` and the column is non-nullable, or has explicit zero null count. This is a general constant-column projection rule with `DODAM_Q03_DISABLE_CONSTANT_SHIPPRIORITY=1` as rollback for the Q03 path.
    - Q03 orders projection dropped from `[o_orderkey,o_custkey,o_orderdate,o_shippriority]` to `[o_orderkey,o_custkey,o_orderdate]`; focused Q03 improved to about `~299ms`, and full-run Q03 sample improved to about `~284ms`.
    - Tried changing Q04 atomic consume from unconditional `swap(0)` to `load` plus `compare_exchange`; focused Q04 was neutral/slightly worse (`~193ms`), so the change was reverted.
    - Q05/Q08 customer/supplier nation dimension builds now use a shared typed integer-key path with `AdaptiveI64Set` for the small nation filter and `FastHashMap` for the output map.
    - SF=10 focused Q05 improved from about `~341ms` to `~287ms`, now faster than DuckDB in this harness. Q08 also benefited from the shared customer dimension path, improving to about `~374ms`, also faster than DuckDB in focused comparison.
    - Latest full SF=10 single-process DuckDB comparison: Dodam median-sum `5.263s`, DuckDB `5.770s`, ratio `0.912x`. Remaining slower gaps are Q04 (`~52ms`), Q12 (`~34ms`), Q03 (`~26ms`), Q10 (`~24ms`), Q09 (`~22ms`), Q07 (`~16ms`), Q11 (`~13ms`), and Q16 (`~4ms`).
  - Q10/Q12/Q04 follow-up:
    - Added a pruned late-materialization engine helper. Existing late-materialization calls still use empty pruning predicates; Q04 candidate scan now passes `o_orderdate` range predicates. On the current SF=10 data this is neutral because all orders row groups overlap the date range, but it is a general correctness-preserving capability for clustered datasets.
    - Q10 returned revenue now uses late materialization: first scan `l_orderkey,l_returnflag`, select rows with `l_returnflag='R'` whose orderkey is in the Q10 order-customer map, then decode `l_extendedprice,l_discount` only for selected rows.
    - SF=10 focused Q10 improved from about `~354ms` to `~256ms`, now faster than DuckDB in this harness. Full-run Q10 sample improved to about `~243ms` vs DuckDB `~328ms`.
    - Re-tested Q12 sorted pending lookup and lineitem late-materialization toggles. Disabling sorted lookup regressed Q12 to about `~190ms`; enabling lineitem late materialization regressed to about `~275ms`. Keep the current default dictionary row-group map plus sorted pending lookup path.
    - Latest full SF=10 single-process DuckDB comparison: Dodam median-sum `5.146s`, DuckDB `5.777s`, ratio `0.891x`. Remaining slower gaps are Q04 (`~49ms`), Q12 (`~28ms`), Q03 (`~20ms`), Q17 (`~17ms`), Q07 (`~12ms`), Q11 (`~11ms`), and Q16 (`~7ms`).
  - Q04/Q12 follow-up:
    - Added an opt-in Q04 candidate row-group map path and then made it default after A/B. It reads `orders[o_orderkey,o_orderdate,o_orderpriority]` in parallel instead of using late materialization for the tiny `o_orderpriority` payload. This lowers candidate build time in profile from about `~90ms` to about `~76-78ms`. `DODAM_Q04_DISABLE_CANDIDATE_ROW_GROUP_MAP=1` is the rollback knob.
    - Q04 candidate row-group chunk sweep: chunk `2` was best in the focused run (`~189ms` total); chunk `4` was similar/noisy (`~192ms`), chunk `8` was worse (`~196ms`). Default `DODAM_Q04_CANDIDATE_ROW_GROUP_MAP_CHUNK` is now `2`.
    - Re-tested Q04 sequential mutable lineitem probe by disabling row-group map; it regressed to about `~290ms`, so the parallel atomic probe remains the default.
    - Re-tested Q12 dictionary row-group chunk under the current code. Chunk `2` improved focused Q12 to about `~174ms`, while chunk `4` regressed to about `~188ms`; default `DODAM_Q12_ROW_GROUP_MAP_CHUNK` is now `2`.
    - Latest full SF=10 single-process DuckDB comparison: Dodam median-sum `5.058s`, DuckDB `5.811s`, ratio `0.871x`. Remaining slower gaps are Q12 (`~25ms`), Q04 (`~24ms`), Q03 (`~13ms`), Q11 (`~9ms`), Q07 (`~9ms`), and Q16 (`~5ms`).
  - Q11 follow-up:
    - Converted Q11 supplier membership from default `HashSet<i64>` to `AdaptiveI64Set`, and Q11 grouped part values from default `HashMap` to the reusable integer `FastHashMap`. The partsupp typed loop now uses Decimal128 raw values directly instead of `DecimalInput::value(row)`.
    - Added a Q11 late-materialized partsupp path: scan/select only `ps_suppkey`, then decode `ps_partkey,ps_supplycost,ps_availqty` for matching supplier rows. On SF=10 the selected ratio is about `323,920 / 8,000,000` (`~4.05%`), so it avoids most payload decode despite random selector runs.
    - Focused SF=10 Q11 after release rebuild: default late path `~44.25ms` vs late disabled `~66.50ms`; DuckDB same-process sample was around `~29ms`.
    - Q11 late row-group chunk sweep: chunk `1` was best (`~40.34ms`), chunk `2` default sample was `~44.25ms`, chunk `4` was `~45.16ms`, and chunk `8` was `~50.85ms`. Default `DODAM_Q11_LATE_ROW_GROUP_CHUNK` is now `1`.
    - After setting chunk `1` as the default and rebuilding release, focused SF=10 Q11 sampled at `~41.70ms` vs DuckDB `~29.50ms`.
    - Latest full SF=10 single-process DuckDB comparison after Q11 late default: Dodam median-sum `5.129s`, DuckDB `5.817s`, ratio `0.882x`. Q11 sampled faster than DuckDB in the full run (`~37.8ms` vs `~40.5ms`). Remaining slower gaps are Q04 (`~37.7ms`), Q09 (`~26.5ms`), Q03 (`~17.8ms`), Q12 (`~16.9ms`), Q07 (`~5.4ms`), Q16 (`~4.7ms`), and Q17 (`~1.3ms`).
    - Important failure/limit: Q11's selected rows are sparse and produce many selector runs (`~621K` runs in one profile), so this is still not a low-level page-skip win. The retained improvement comes from avoiding most payload column materialization; a deeper fused Parquet decoder would be needed to close the remaining gap to DuckDB.
  - Q04 follow-up:
    - Reordered the atomic lineitem probe hot loop to check the dense candidate marker before reading/comparing `l_commitdate,l_receiptdate`. This is a general sparse-marker probe rule: when the candidate marker is selective, defer payload/date checks until after membership. Profile showed lineitem consume sum dropping sharply, but wall-clock improvement is modest because Parquet decode/read remains dominant.
    - Added a dictionary-preserved candidate priority path for `orders.o_orderpriority` using the existing dictionary row-group-map API. This is a general low-cardinality string payload rule candidate, not a hard-coded priority-value shortcut. Focused SF=10 Q04 improved modestly: dictionary on `~169.8ms`, dictionary off `~174.3ms` in one same-binary check. `DODAM_Q04_DISABLE_CANDIDATE_DICTIONARY_PRIORITY=1` is the rollback knob.
    - Re-tested Q04 lineitem chunk after marker-first. Chunk `1` had one fast sample (`~160.0ms`) but was not stable on rerun (`~171-177ms`). Chunk `2` remained more stable (`~172.2ms` in the checked same-binary rerun), so `DODAM_Q04_LINEITEM_ROW_GROUP_MAP_CHUNK` default remains `2`.
    - Candidate chunk stayed best at `2`; candidate chunk `1`/`4` with lineitem chunk `1` regressed to about `~170.7ms`/`~172.8ms` in the checked runs.
    - Latest full SF=10 single-process DuckDB comparison after Q04 marker-first and dictionary candidate: Dodam median-sum `5.161s`, DuckDB `5.792s`, ratio `0.891x`. Remaining slower gaps are Q12 (`~27.0ms`), Q04 (`~24.6ms`), Q03 (`~19.4ms`), Q09 (`~12.3ms`), Q07 (`~9.9ms`), Q16 (`~8.2ms`), and Q17 (`~2.8ms`).
  - Q12 follow-up:
    - Re-profiled SF=10 Q12. The lineitem dictionary row-group map is still the dominant scan/decode cost, while the orders side was around `~44ms` with the sorted stream aggregate.
    - Re-tested lineitem knobs under the current code: `DODAM_Q12_ROW_GROUP_MAP_CHUNK=1` regressed focused Q12 to about `~184.5ms`, chunk `4` to about `~174.4ms`, and dictionary shipmode off to about `~175.1ms`; default chunk `2` and dictionary shipmode remain correct.
    - Re-tested orders fused row-group-map now that other paths changed. Fused without dictionary was not enough (`~168-170ms` focused), but fused plus dictionary-preserved `o_orderpriority` was consistently better (`~162.7ms`, then `~152.2ms` on rerun) than the sorted stream fallback (`~163.2ms` in the same comparison set).
    - Changed Q12 orders to use fused row-group-map by default, before the sorted stream fallback, and made fused `o_orderpriority` dictionary-preserved by default. Rollbacks: `DODAM_Q12_DISABLE_ORDER_FUSED_SCAN=1` and `DODAM_Q12_DISABLE_ORDER_FUSED_DICTIONARY_PRIORITY=1`.
    - Focused SF=10 Q12 default after the change sampled at `~153.6ms` vs DuckDB `~121.5ms`; disabling fused sampled at `~163.2ms`, and disabling fused dictionary sampled at `~169.7ms`.
    - Latest full SF=10 single-process DuckDB comparison after the Q12 orders fused default: Dodam median-sum `5.024s`, DuckDB `5.815s`, ratio `0.864x`. Q12 gap dropped to about `~8.9ms`. Remaining slower gaps are Q03 (`~19.0ms`), Q04 (`~13.2ms`), Q07 (`~11.9ms`), Q12 (`~8.9ms`), Q17 (`~7.1ms`), and Q16 (`~3.6ms`).
  - Q03 follow-up:
    - Re-tested existing Q03 knobs under the current SF=10 code. Sorted lookup remained worse (`~323ms` focused), vector+sorted was also worse (`~325ms`), dense order probe regressed (`~303ms`), and row-group chunk `4` was worse than default. Chunk `1` was noisy/slightly faster on Dodam time in one run but did not justify a default change.
    - Added Q03 lineitem revenue late materialization. It scans `l_orderkey,l_shipdate` first, then reads `l_extendedprice,l_discount` only for selected rows. This is a general selective-predicate/payload split, not a query-value shortcut.
    - Profile showed very low selected ratio (`302,114 / 59,986,052`, about `0.50%`) but many selector runs (`~301K`), so the win is real but bounded by sparse row-selection overhead.
    - Focused SF=10 Q03 late opt-in sampled at `~275.7ms`, then `~271.6ms`, versus default around `~282.8ms`. Late chunk `1` and `4` regressed to about `~280ms`, so chunk `2` remains the default.
    - Q03 revenue late materialization is now default with `DODAM_Q03_DISABLE_REVENUE_LATE_MATERIALIZE=1` as rollback.
    - Latest full SF=10 single-process DuckDB comparison after Q03 late default: Dodam median-sum `5.083s`, DuckDB `5.768s`, ratio `0.881x`. Q03 gap dropped to about `~9.5ms`. Remaining slower gaps are Q04 (`~24.0ms`), Q09 (`~15.5ms`), Q12 (`~11.0ms`), Q03 (`~9.5ms`), Q17 (`~9.2ms`), Q16 (`~7.6ms`), Q07 (`~7.2ms`), and Q15 (`~1.5ms`).
  - Q03/Q04/Q07 follow-up:
    - Re-tested Q03 order row-group-map chunking under the current SF=10 binary. Chunk `16` sampled slightly better than the old chunk `2` in focused runs (`~274.7ms` vs `~277.8ms`), while chunk `32/64` regressed badly. Default `DODAM_Q03_ORDER_ROW_GROUP_MAP_CHUNK` is now `16`, but the win is small/noisy.
    - Re-tested Q04 candidate and lineitem choices. Candidate chunk `4` beat the old chunk `2` in focused runs, so `DODAM_Q04_CANDIDATE_ROW_GROUP_MAP_CHUNK` now defaults to `4`.
    - Q04 lineitem late materialization was a failed default: selected ratio was only about `3.83%`, but the row-selection/second-reader overhead made it slower than the atomic row-group probe. Focused with late default was around `~156-166ms`; candidate chunk `4` plus late disabled sampled around `~148ms`. Q04 lineitem late is now opt-in only via `DODAM_Q04_ENABLE_LINEITEM_LATE_MATERIALIZE=1`.
    - Added Q07 lineitem late materialization as a general predicate/payload split: scan `l_orderkey,l_suppkey,l_shipdate` first, then decode `l_extendedprice,l_discount` only for rows whose supplier nation, order customer nation, and date range match. This is the same reusable late-materialization rule, not a hard-coded TPC-H result shortcut.
    - Q07 focused SF=10 improved from late disabled `~346.2ms` to default late `~311.2ms`, now roughly DuckDB parity/faster in the same harness (`~314.0ms`). Rollback: `DODAM_Q07_DISABLE_LATE_MATERIALIZE=1`.
    - Latest full SF=10 single-process DuckDB comparison after Q07 late and Q03/Q04 default tuning: Dodam median-sum `5.027s`, DuckDB `5.783s`, ratio `0.869x`. Q07 is now faster than DuckDB in the full run (`~295.3ms` vs `~323.5ms`). Remaining slower gaps are Q03 (`~24.4ms`), Q04 (`~8.6ms`), Q16 (`~4.7ms`), Q02 (`~2.7ms`), Q19 (`~1.8ms`), and Q12 (`~0.6ms`).
  - Additional Q03/Q04 follow-up:
    - Tried changing Q03 late revenue accumulation from per-chunk `FastHashMap` updates to append `(orderkey,revenue)` pairs followed by global sort/reduce. This failed: focused Q03 regressed from about `~275.2ms` to `~278.8ms`, so the experiment was reverted.
    - Re-tested Q03 order build alternatives. The current row-group map path remained best (`~276.3ms` focused); disabling row-group map regressed to about `~288.0ms`, and sequential stream accumulate regressed badly to about `~358.3ms`.
    - Re-tested Q04 late candidate after the candidate chunk change. The current dictionary row-group candidate path remained better (`~149.2ms` focused); forcing late candidate regressed to about `~164.3ms`.
    - Added Q03 late selector order carry-through: while selecting lineitem rows, carry `Q03Order { orderdate, shippriority }` from the already-probed order map into the payload phase. This removes the final second lookup into the orders map when constructing output rows. It is a general late-materialization payload-carry rule, not a query-value shortcut.
    - Focused SF=10 Q03 improved modestly from about `~277.0ms` to about `~273.9-274.2ms`. Rollback: `DODAM_Q03_DISABLE_REVENUE_LATE_CARRY_ORDER=1`.
    - Latest full SF=10 single-process DuckDB comparison after Q03 carry-order: Dodam median-sum `5.091s`, DuckDB `5.812s`, ratio `0.876x`. The total is still faster than DuckDB, but this sample had visible per-query noise: Q03 gap was about `~19.2ms`, Q04 about `~27.2ms`, and Q09 about `~15.2ms`.
  - Q03 order-build follow-up:
    - Committed the previous late-materialization batch before this round as `c17cfdf`.
    - Q03 profile showed `orders` row-group-map consume sum around `~235ms` before order-build cleanup. Changed the Q03 orders row-group-map consumer to insert directly into the task-local accumulator instead of building a temporary per-batch `FastHashMap` and merging it. Profile consume sum dropped to about `~214ms`, though focused wall-clock movement was noisy.
    - Added capacity-aware final merge for Q03 order partial maps. This was only a small/noisy focused improvement.
    - Added task-local initial capacity for Q03 order row-group maps. Capacity sweep on SF=10 focused Q03: `0` sampled Dodam `~263.2ms`, `65536` `~261.1ms`, `131072` `~258.4ms`, `262144` `~260.1ms`; default is now `DODAM_Q03_ORDER_ROW_GROUP_MAP_INITIAL_CAPACITY=131072`.
    - Tried a dense bitset membership probe inside Q03 late selector to avoid order-map misses. It failed badly (`~296.4ms` focused vs default `~271.4ms`) because the large random bitset access outweighed hash miss savings, so the experiment was removed.
    - Latest full SF=10 single-process DuckDB comparison after Q03 order-map accumulator/capacity tuning: Dodam median-sum `5.161s`, DuckDB `5.830s`, ratio `0.885x`. Q03 is now effectively at DuckDB parity in the full run (`~261.5ms` vs `~261.0ms`, gap `~0.5ms`). Remaining slower gaps in that sample are Q09 (`~22.5ms`), Q17 (`~12.0ms`), Q04 (`~11.9ms`), and Q12 (`~11.7ms`).
  - Q09/Q04 follow-up:
    - Re-tested Q09's existing late materialization and matched-index paths on SF=10. Late materialization is still a clear failure (`~821.9ms` focused, and worse with matched-index) because the selected rows are too sparse and row-selection/second-reader overhead dominates.
    - Q09 matched-index is a clear win. It first collects matching row indexes from dense part-key membership, then performs order/supplier/supply/amount work only for matched rows. Focused Q09 moved from about `~606.4ms` default to `~596.5-598.6ms`, while DuckDB was around `~616ms`; made it default with rollback `DODAM_Q09_DISABLE_MATCHED_INDEX=1`.
    - Q09 profile with matched-index showed lineitem consume sum dropping from about `~4.97s` to about `~2.00s` across row-group tasks, and Q09 focused remained faster than DuckDB in the same-process harness.
    - Latest full SF=10 single-process DuckDB comparison after Q09 matched-index default: Dodam median-sum `5.028s`, DuckDB `5.803s`, ratio `0.867x`. Remaining slower queries in that sample were only Q04 (`~12.4ms`) and Q12 (`~3.0ms`).
    - Re-tested Q04 chunking under the current binary. Candidate chunk `2` beat `4/8` in focused runs (`~147.2ms` vs `~151.5ms`/`~156.7ms`), so `DODAM_Q04_CANDIDATE_ROW_GROUP_MAP_CHUNK` default was restored to `2`. Lineitem chunk `2` remains best among `1/2/4`.
    - Tried lower-level direct Parquet column readers to bypass Arrow `RecordBatch` materialization:
      - Q04 lineitem direct reader scanned `l_orderkey` first and selectively read `l_commitdate/l_receiptdate`; it regressed badly (`~192.0ms` vs default `~147.9ms`) because sparse skip/read runs cost more than the Arrow row-group-map path.
      - Q12 lineitem direct full-column reader decoded `l_orderkey,l_shipmode,l_commitdate,l_receiptdate,l_shipdate` directly and accumulated pending orders without RecordBatch creation; it also regressed badly (`~260.5ms` vs default `~148.4ms`).
    - Conclusion: with the current `parquet` crate low-level `ColumnReader` API, direct row/record readers are not the structural fix. The Arrow reader path is still faster because it uses optimized batch/page decode. Closing Q04/Q12 further likely requires a fused Arrow/Parquet decoder or DataFusion/arrow-level vector pipeline integration, not ad-hoc per-column `read_records` loops.
    - Applied a structural Q17 lineitem aggregation change: process stream batches in chunks and reuse one chunk-local partial state instead of allocating/merging one `HashMap`/candidate vector per `RecordBatch`. This is a general batch-dispatch/partial-state reduction pattern for small build-set scan aggregates.
      - SF=10 focused Q17, individual runs: chunk `1` `~306.8ms`, `4` `~302.1ms`, `8` `~299.7ms`, `16` `~305.0ms`, `32` `~294.2ms`, `64` `~289.7ms`, `128` `~297.6ms`; DuckDB in the same harness was `~313-317ms`.
      - Default `DODAM_Q17_LINEITEM_CHUNK_SIZE` is now `64`. This reduced dispatch/merge overhead without query-value specialization and made focused Q17 faster than DuckDB in the current SF=10 single-process comparison (`~0.920x`).
    - Re-profiled Q04 under the same-binary SF=10 harness. The lineitem stage is dominated by Arrow/Parquet `next()`/decode, not row consume (`row_group_map_summary` lineitem read-next sum around `~1000ms` across parallel tasks, consume around `~47ms`, wall stage around `~123ms` with profiling enabled).
      - Re-tested lineitem row-group chunk individually: chunk `1` `~149.0ms`, `2` `~151.1ms`, `4` `~150.2ms`, `8` `~155.7ms`; changed default `DODAM_Q04_LINEITEM_ROW_GROUP_MAP_CHUNK` to `1`.
      - Tried general predicate ordering in Q04 atomic probe by checking `l_commitdate < l_receiptdate` before random candidate-marker load. It did not improve (`~151.3ms` focused) because the date predicate does not filter enough on this data; reverted to marker-first ordering.
    - Applied a structural Q09 order-year build change: move `orders[o_orderkey,o_orderdate]` from one sequential stream consumer into the existing row-group-map pipeline, using compact chunk-local `(orderkey, year)` partials and building the final dense `orderkey -> year` map once. This is a general dense-key map-build parallelization rule with a bounded final dense range, not a query-value shortcut. Rollback: `DODAM_Q09_DISABLE_ORDER_YEAR_ROW_GROUP_MAP=1`.
      - Q09 profile before this showed `orders` scan `next_wait` around `~2ms` but `consumer_gap` around `~204ms`, so the bottleneck was the single dense-map fill loop rather than Parquet decode.
      - SF=10 focused Q09 individual runs: new row-group order-year path `~577.6ms`; old sequential path with rollback env `~606.2ms`; DuckDB in the same harness was around `~615ms`.
      - Chunk sweep: `DODAM_Q09_ORDER_YEAR_ROW_GROUP_MAP_CHUNK=2` `~591.6ms`, `4` `~577.6ms`, `8` `~577.0ms`, `16` `~594.8ms`; default is now `8`.
      - Latest full SF=10 single-process DuckDB comparison after the Q09 order-year row-group build: Dodam median-sum `5.043s`, DuckDB `5.812s`, ratio `0.868x`. Q09 is now faster than DuckDB in that full sample (`~580.3ms` vs `~641.5ms`). Remaining slower queries are Q12 (`~12.3ms`), Q16 (`~9.0ms`), Q04 (`~6.0ms`), Q17 (`~1.4ms`), and Q11 (`~0.1ms`).
    - Q12/Q16 follow-up:
      - Tried changing Q12 pending orders from a `HashMap<i64, Q12PendingOrder>` to `AdaptiveI64Map<Q12PendingOrder>` so lineitem would build the same dense representation used by the orders probe. This failed badly and was interrupted: each row-group partial grew a large dense key range, causing memory/merge cost explosion. Reverted. Lesson: dense maps are good for the final shared lookup, but not for many parallel sparse partials unless the dense storage is sharded or built only once.
      - Re-tested Q12 lineitem row-group chunk: `1` `~157.9ms`, `4` `~154.5ms`, both worse than the current default area. Re-tested Q12 orders fused row-group chunk: `1` `~150.5ms`, `4` `~146.4ms`, `8` `~152.6ms`, sorted fallback with fused disabled `~160.8ms`. Changed `DODAM_Q12_ORDER_FUSED_ROW_GROUP_CHUNK` default from `2` to `4`; later same-binary reruns were noisy (`4` `~151.0ms`, `2` `~151.8ms`) but not a regression.
      - Tried Q16 part group row-group parallelization with chunk-local string/group interning and final global remap. It was not stable: one focused run sampled `~92.0ms` vs sequential rollback `~102.3ms`, but repeat default was `~103.5ms` while rollback was `~99.1ms`, and profiling showed extra partial merge/string remap cost. Reverted.
      - Tried Q16 supplier bitset distinct path (`DODAM_Q16_ENABLE_SUPPLIER_BITSET=1`, packed distinct disabled); it regressed badly (`~128.0ms`), so packed distinct remains default.
      - Latest full SF=10 single-process DuckDB comparison after retaining Q09 order-year row-group and Q12 orders chunk `4`: Dodam median-sum `5.070s`, DuckDB `5.855s`, ratio `0.866x`. Remaining slower queries are Q12 (`~14.8ms`), Q16 (`~11.3ms`), Q04 (`~8.8ms`), and Q07 (`~3.9ms`).
    - Q16 dense/dictionary follow-up:
      - Added a dense direct-probe path inside Q16 packed distinct supplier counting. When `part_to_group` and bad supplier membership are both dense, the typed null-free `partsupp` loop now reads dense slices directly instead of calling `AdaptiveI64Map::get()` and `AdaptiveI64Set::contains()` for every row. Focused SF=10 Q16 improved to about `~86.4ms`; chunk `4/16` regressed, so supplier chunk default remains `8`.
      - Added dictionary-preserved scan for Q16 `part[p_brand,p_type]`, with dictionary-key to interned group-id lookup per batch. This avoids materializing/comparing repeated brand/type strings in the part-group stage. Rollback: `DODAM_Q16_DISABLE_PART_DICTIONARY_STRINGS=1`.
      - Focused SF=10 Q16 improved to about `~78.6ms`; disabling the dictionary part path in the same binary regressed to about `~101.7ms`.
      - Tried precomputing Q12 order-priority dictionary high/low flags per batch. It did not improve (`~151.7ms` focused) because the extra per-batch Vec allocation outweighed the tiny saved byte comparisons, so it was reverted.
      - Latest full SF=10 single-process DuckDB comparison after Q16 dense/dictionary work: Dodam median-sum `5.015s`, DuckDB `5.828s`, ratio `0.860x`. Q16 is effectively parity in that sample (`~79.4ms` vs `~78.5ms`). Remaining slower queries are Q12 (`~17.3ms`), Q04 (`~9.6ms`), Q17 (`~2.0ms`), and Q16 (`~0.9ms`).
    - Scan/vector pipeline follow-up:
      - Applied a small stage-1 RecordBatch-after-cost reduction to Q12 lineitem: the projected hot path now uses fixed projected column positions for `l_orderkey,l_shipmode,l_commitdate,l_receiptdate,l_shipdate` instead of repeated schema/name lookup in the dictionary path. Focused Q12 sampled around `~149.6ms`, modest/noisy but retained as a general projected-column rule.
      - Tried stage-2 predicate ordering in Q12 dictionary lineitem by checking shipmode dictionary key before date predicates. It regressed (`~152.8ms` focused), so date-first ordering was restored.
      - Latest full SF=10 comparison after the retained Q12 direct-column and Q16 dictionary/dense changes sampled Dodam `4.950s` vs DuckDB `5.810s` (`0.852x`). Remaining slower queries in that sample were Q04 (`~30.5ms`), Q12 (`~2.6ms`), and Q03 (`~2.1ms`).
      - Re-tested Q04 row-filter forcing under the current binary (`DODAM_Q04_ENABLE_LINEITEM_ROW_FILTER=1`); it regressed to about `~156.7ms` focused. Q04 remains dominated by Parquet/Arrow lineitem decode, and the current full-decode atomic marker path still beats Arrow RowFilter/late materialization for this key distribution.
      - Stage-3 fused row-group consumers are already the retained default for Q04/Q12/Q16/Q09. Stage-4 lower-level Parquet `ColumnReader`/direct decoder experiments remain rejected for default use because earlier Q04/Q12 direct column readers were slower than Arrow's optimized batch reader; a true win likely needs a fused page/decoder layer rather than ad-hoc `read_records` loops.
      - Started the real vector pipeline refactor with a shared `BatchView` typed-column boundary, a `BatchConsumer` handoff, and a reusable `SelectionVector` skeleton. Q12 projected lineitem now consumes a `BatchView` through a consumer object, and Q16 projected part-group hot paths use fixed projection positions through `BatchView` instead of doing local `RecordBatch`/schema/name plumbing. This is stage 1 only: it removes duplicated glue and establishes the common vector handoff, but it does not yet bypass Arrow `RecordBatch` materialization or implement a fused Parquet page decoder.
      - Validation after the stage-1 vector boundary: `cargo test -q` passed. SF=10 focused Q12 sampled Dodam `~146.4ms` vs DuckDB `~126.0ms`; focused Q16 sampled Dodam `~94.1ms` vs DuckDB `~64.0ms`, both noisy and not a claimed speed win. Full SF=10 single-process comparison sampled Dodam `5.044s`, DuckDB `5.819s`, ratio `0.867x`; slower queries were Q04 (`~19.4ms` gap), Q12 (`~14.6ms`), Q16 (`~5.0ms`), Q17 (`~4.3ms`), and Q11 (`~0.9ms`).
      - Extended the vector boundary into the engine row-group-map layer with `parquet_row_group_map_pruned_view` and `parquet_row_group_map_dictionary_columns_pruned_view`. Q12 dictionary lineitem and Q04 atomic lineitem now receive `BatchView` directly from the row-group worker instead of moving `RecordBatch` through a query-local closure first. This is a structural scan-consumer split, not a page-decoder bypass.
      - Validation after row-group-map view consumers: `cargo test -q` passed. Focused SF=10 Q04 sampled Dodam `~158.6ms` vs DuckDB `~124.0ms`; Q12 sampled Dodam `~148.0ms` vs DuckDB `~125.0ms`, so this is not a claimed speed win. Full SF=10 single-process comparison sampled Dodam `5.052s`, DuckDB `5.869s`, ratio `0.861x`; slower queries were Q04 (`~22.0ms` gap), Q12 (`~15.6ms`), Q17 (`~4.1ms`), and Q16 (`~1.8ms`).
      - Added lazy projection-order caching inside `ParquetBatchReader`: the first output batch computes whether projection reorder is needed, and later batches skip schema/name matching or use cached column indices. This is a general scan overhead reduction, but Q04/Q12 remain dominated by Parquet/Arrow decode rather than per-batch schema glue.
      - Re-profiled Q04 directly. The lineitem row-group-map summary showed read/next dominating (`read_next` sum around `~998ms`, consume around `~51ms` across parallel row-group workers), confirming that row consume/view dispatch is no longer the main cost.
      - Re-tested Q04 lineitem row-group chunk under the current view path: chunk `1` was clearly worse in a focused sweep, while chunk `2` was best among `1/2/4/8`; default `DODAM_Q04_LINEITEM_ROW_GROUP_MAP_CHUNK` is back to `2`.
      - Validation after projection cache and Q04 chunk retune: `cargo test -q` passed. Focused Q04 remained noisy/slower (`~163.4ms` vs DuckDB `~125.0ms`), focused Q12 sampled `~145.4ms` vs DuckDB `~128.0ms`. Full SF=10 single-process comparison sampled Dodam `5.045s`, DuckDB `5.871s`, ratio `0.859x`; slower queries were Q04 (`~21.0ms`), Q12 (`~11.0ms`), Q16 (`~8.2ms`), and Q17 (`~8.0ms`).
      - Continued moving hot paths onto the scan-consumer vector boundary:
        - Added a non-pruned `parquet_row_group_map_view` wrapper.
        - Moved Q16 `partsupp[ps_partkey,ps_suppkey]` supplier-count aggregation from stream/batch chunk folding to row-group-map `BatchView` consumers for packed distinct, bitset, and hash-set fallback modes.
        - Moved Q21 ordered lineitem row-group aggregation to `BatchView` consumers.
      - Q16 view path was noisy in focused runs: one 7-run sample regressed to `~101.0ms`, but a same-binary chunk sweep showed chunk `8` around `~76.8ms` and chunk `16` around `~78.0ms`; default chunk `8` was retained. Q21 focused stayed faster than DuckDB (`~534.4ms` vs `~644.0ms`).
      - Full SF=10 single-process comparison after Q16/Q21 view consumers sampled Dodam `5.061s`, DuckDB `5.909s`, ratio `0.856x`. Q16 was faster than DuckDB in that sample (`~81.3ms` vs `~85.5ms`). Remaining slower queries were Q12 (`~12.5ms` gap), Q04 (`~9.8ms`), Q17 (`~1.9ms`), and Q11 (`~0.4ms`).
      - Moved Q17 lineitem revenue aggregation from stream chunk folding to `parquet_row_group_map_view`. The existing typed 3-column loop now consumes `BatchView` inside row-group workers and merges row-group partials directly. Focused SF=10 Q17 improved from the previous slower/noisy area to about `~203ms` vs DuckDB `~320ms`.
      - Tried moving Q12 fused orders aggregation to dictionary/non-dictionary `BatchView` row-group-map consumers. It regressed focused Q12 to about `~157ms` vs DuckDB `~124ms`, so the Q12 orders fused path was reverted to the previous `RecordBatch` row-group-map consumer. After revert, focused Q12 returned to about `~147ms` vs DuckDB `~126ms`.
      - Full SF=10 single-process comparison after keeping Q17 view and reverting Q12 orders view sampled Dodam `4.912s`, DuckDB `5.955s`, ratio `0.825x`. Remaining slower queries were Q04 (`~23.5ms` gap) and Q12 (`~7.3ms`); Q17 was faster than DuckDB in that sample (`~226ms` vs `~334ms`).
      - Direction update: even though Q12 fused orders view consumer can be slower today, keep moving hot paths toward the vectorized scan-consumer shape so later vector/page-level work has a consistent execution boundary. Q12 fused orders now defaults to `BatchView` row-group-map consumers again instead of the older `RecordBatch` consumer.
      - Validation after making Q12 fused orders view-default: `cargo test -q` passed. Focused Q12 sampled Dodam `~150.7ms` vs DuckDB `~124.0ms`. Full SF=10 single-process comparison sampled Dodam `4.906s`, DuckDB `5.819s`, ratio `0.843x`; remaining slower queries were Q12 (`~14.2ms` gap), Q04 (`~13.3ms`), and Q03 (`~5.1ms`).
      - Extended the vector scan-consumer shape further:
        - Q03 revenue and Q04 candidate/fallback lineitem row-group-map consumers now use `BatchView`.
        - Q09 order-year build and Q09 profit row-group-map now use `BatchView`.
        - Q05/Q07 lineitem aggregate row-group-map paths and Q21 fallback order-state build now use `BatchView`.
        - Existing typed loops are retained behind projection-position wrappers; fallback still clones the underlying `RecordBatch`, so semantics are unchanged while the hot projected path avoids repeated name/schema plumbing.
      - Validation after this expansion: `cargo test -q` passed and release build passed. Focused SF=10 affected-query comparison (`q03,q04,q05,q07,q09,q21`, individual benchmark) sampled Dodam `2.197s`, DuckDB `2.308s`, ratio `0.952x`; slower samples were Q04 (`~19.2ms`), Q05 (`~6.5ms`), Q03 (`~1.8ms`). Full SF=10 single-process comparison sampled Dodam `4.916s`, DuckDB `5.842s`, ratio `0.842x`; remaining slower queries were Q12 (`~19.1ms`), Q04 (`~14.4ms`), and Q03 (`~2.3ms`). Q05/Q07/Q09/Q21 were faster than DuckDB in the full run.
      - Completed the remaining row-group-map call-site conversion in `src/sql.rs`: Q12 fallback lineitem, Q03 opt-in vector+sorted, Q04 non-atomic fallback, and fast-like dense-count row-group map now also use `BatchView` consumers. A direct search for the old `parquet_row_group_map(...)`/`parquet_row_group_map_pruned(...)`/`parquet_row_group_map_dictionary_columns(...)` call forms in `src/sql.rs` returns no matches.
      - Validation after completing those call sites: `cargo test -q` passed and release build passed. Focused SF=10 (`q03,q04,q12,q13`, individual benchmark) sampled Dodam `0.774s`, DuckDB `0.795s`, ratio `0.973x`; Q13 was faster, while Q12/Q04/Q03 remained slower in that focused sample. Full SF=10 single-process comparison sampled Dodam `4.859s`, DuckDB `5.835s`, ratio `0.833x`; remaining slower queries were Q04 (`~9.8ms`), Q12 (`~9.6ms`), and Q15 (`~1.6ms`, likely noise-level).
      - Added stream and scan-aggregate vector boundaries:
        - New `parallel_batch_fold_view_chunks` helper processes stream chunks as `BatchView` consumers instead of forcing query code to own `Vec<RecordBatch>`.
        - Q06 row-filter fallback and Q08 order-year stream aggregation now use this helper.
        - New engine-level `parquet_scan_accumulate_chunks_view` plus stream fallback moves Q01/Q06/Q15 scan-aggregate consumers to `BatchView`.
      - Validation after stream/scan-aggregate view expansion: `cargo test -q` passed and release build passed. Focused SF=10 Q06/Q08/Q12 comparison sampled Dodam `0.644s`, DuckDB `0.650s`, ratio `0.991x`; Q06/Q08 were faster and Q12 remained slower. Focused Q01/Q06/Q15 sampled Dodam `0.478s`, DuckDB `0.533s`, ratio `0.897x`, with no slower queries. Full SF=10 single-process comparison sampled Dodam `4.826s`, DuckDB `5.847s`, ratio `0.825x`; remaining slower queries were Q04 (`~10.0ms`), Q12 (`~6.7ms`), and Q03 (`~0.2ms`, noise-level).
      - Applied the next 1-4 vector pipeline pass:
        - Late materialization: added engine-level `late_materialized_parquet_map_pruned_with_policy_view` plus view-based sample/chunk execution; moved Q03 revenue late/carry and Q12 lineitem late callbacks to `BatchView`.
        - Stream fold: moved Q12 row-filter lineitem fallback and Q12 orders sorted/default stream fallback to `parallel_batch_fold_view_chunks`.
        - Simple scan build: moved Q15 supplier output scan to fixed-projection `BatchView`.
        - BatchView API: added `num_rows`, `i64`, and `date32` typed accessors and applied them to several hot wrappers.
      - Validation after this pass: `cargo test -q` passed and release build passed. Focused SF=10 Q03/Q12/Q15 sampled Dodam `0.606s`, DuckDB `0.588s`, ratio `1.032x`; Q15 was faster while Q03/Q12 were slower in that focused sample. Full SF=10 single-process comparison sampled Dodam `4.839s`, DuckDB `5.838s`, ratio `0.829x`; only Q04 was slower (`~6.5ms`), while Q12 was effectively parity/faster in that full run (`~0.8ms` faster).
      - Continued the 1-4 vector pipeline pass:
        - Late materialization: moved Q10 customer late and Q10 returned-revenue late to the engine `BatchView` API; added a reusable `i64_set_late_build_selection_view` helper.
        - Stream fold: moved Q10 order-customer build and returned-revenue fallback to `parallel_batch_fold_view_chunks`.
        - Simple scan build: moved Q08 `part_keys` and `supplier_is_brazil` dimension scans to fixed-projection `BatchView`.
        - BatchView API: added `required_i64`, `required_utf8`, and `required_date32` accessors and used them in Q08/Q10 view paths.
      - Validation after Q10/Q08 view expansion: `cargo test -q` passed and release build passed. Focused SF=10 Q08/Q10 sampled Dodam `0.606s`, DuckDB `0.720s`, ratio `0.841x`, with no slower queries. Full SF=10 single-process comparison sampled Dodam `4.891s`, DuckDB `5.848s`, ratio `0.836x`; remaining slower queries were Q12 (`~14.5ms`) and Q04 (`~11.5ms`).
      - Continued with Q12/Q04/Q02 vector cleanup:
        - Late materialization: moved Q12 order late and Q04 candidate late to `BatchView` callbacks.
        - Stream/fallback scan: moved Q04 candidate fallback scan and Q04 lineitem fallback count loop through `BatchView` helpers.
        - Simple dimension build: moved Q02 region-key and nation-name scans to fixed-projection `BatchView`.
        - Typed layout: added small `Q12OrderPriorityView`, `Q04CandidatePredicateView`, and `Q04CandidatePayloadView` layout structs for late-materialized payload/predicate callbacks.
      - Validation: `cargo test -q` passed and release build passed. Focused SF=10 Q02/Q04/Q12 sampled Dodam `0.386s`, DuckDB `0.330s`, ratio `1.171x` with all three slower in that focused run. Full SF=10 single-process comparison sampled Dodam `4.869s`, DuckDB `5.771s`, ratio `0.844x`; remaining slower queries were Q04 (`~28.7ms`), Q12 (`~12.0ms`), and Q03 (`~0.7ms`, noise-level). Q04's gap worsened in this sample, so the next performance pass should profile Q04 candidate/lineitem stages before further broad conversion.
      - Q04 follow-up after the vector cleanup:
        - Applied the general "local predicate before random marker lookup" rule to Q04's atomic lineitem probe, checking `l_commitdate < l_receiptdate` before dense atomic marker load/swap.
        - Validation: `cargo test -q` and release build passed. SF=10 Q04 standalone stayed roughly neutral/noisy (`~0.163s` Dodam vs `~0.127s` DuckDB in one 7-repeat sample), while profile lineitem probe stabilized around `~113ms` after warmup.
        - Re-tested alternatives and kept them disabled: Q04 non-atomic collect/dedup lineitem probe regressed (`~0.177s`), large i64 set row-filter was much worse (`~0.283s`) due per-chunk filter setup, larger lineitem row-group chunks were neutral/worse, disabling late candidates was neutral/worse, and Q12 row-filter/direct-column-reader/sorted-order alternatives were worse or noisy on SF=10.
        - Full SF=10 single-process comparison after the Q04 predicate-order change sampled Dodam `4.866s`, DuckDB `5.821s`, ratio `0.836x`; remaining slower queries were Q04 (`~17.3ms`), Q12 (`~11.5ms`), and Q03 (`~0.3ms`, noise-level).
      - Vector pipeline generalization pass:
        - Predicate-first probe: applied the same local-predicate-before-random-probe rule to Q03's dense customer probe and Q08's part-key/order-year probe order.
        - Dictionary string path: moved Q12-named dictionary string helpers into `vector` as generic `DictionaryStringValues`, `dictionary_i32_string_values`, and `dictionary_string_key_for_value`, now shared by Q04/Q12/Q16.
        - Row-group scan-map API: added `parquet_row_group_map_scan_view` so dictionary-preserved columns and row-group pruning are selected through one view entrypoint. Q04 candidate and Q12 lineitem paths now use it.
        - Fused view layout: added `Q12LineitemStringView` so Q12 lineitem string-mode batches can avoid repeated projected-column downcasts on the RecordBatch path.
        - BatchView API: added `required_dictionary_i32` to continue moving fixed-projection code away from ad hoc downcasts.
        - Validation: `cargo test -q` and release build passed. Focused SF=10 Q03/Q04/Q08/Q12/Q16 sampled Dodam `1.010s`, DuckDB `0.992s`, ratio `1.018x`; full SF=10 sampled Dodam `4.882s`, DuckDB `5.809s`, ratio `0.840x`. Remaining slower queries in that full run were Q04 (`~13.6ms`), Q12 (`~11.9ms`), Q16 (`~7.7ms`), and Q03 (`~1.6ms`). Q16 should be rechecked before assuming the dictionary helper move is neutral.
      - Q16 recheck after the dictionary helper move:
        - Compared the previous commit binary against the current binary and confirmed the Q16 focused gap was pre-existing/noisy rather than caused by the helper generalization. Previous commit sampled Dodam `~78.5ms` vs DuckDB `~62.5ms`; current sampled around `~77.3ms` vs DuckDB `~65.5ms`.
        - Re-tested Q16 supplier-count row-group chunking. Chunk `4` was slightly better in a serial focused sweep than the old default area, so `DODAM_Q16_SUPPLIER_COUNT_CHUNK_SIZE` default changed from `8` to `4`. This is a small grain-size retune, not a structural win.
        - Validation: `cargo test -q` and release build passed. Focused SF=10 Q16 sampled Dodam `~75.1ms` vs DuckDB `~63.0ms`. Full SF=10 single-process comparison sampled Dodam `4.985s`, DuckDB `5.831s`, ratio `0.855x`; remaining slower queries were Q12 (`~20.1ms`), Q04 (`~9.0ms`), and Q16 (`~5.5ms`).
      - Q12/Q04 retest:
        - Q12 lineitem remains best on the one-pass full-decode path. Re-tested lineitem late materialization (`~234.6ms`) and Arrow RowFilter (`~253.7ms`), both much worse than default focused samples around `~147-169ms`. Q12 orders direct/late/fused-on/off samples were noisy; no default change.
        - Q04 profiling still points at lineitem scan/decode. Q04 lineitem late materialization has attractive selectivity (`~3.8%`) but very fragmented selectors (`~1.1M` runs on SF=10). Same-binary focused A/B was effectively tied: late default candidate `~151.9ms`, late disabled `~151.8ms`, so the opt-in default was not changed.
      - Vector pipeline common primitive pass:
        - Added a shared dictionary `Int32` match-flag helper and moved Q12 lineitem shipmode and orders priority dictionary paths to use dictionary-key flags instead of query-local dictionary value scans in the row loop.
        - Added a reusable Q06 null-free Decimal128/i64 revenue vector loop and moved Q14 late materialization callbacks from `RecordBatch` to `BatchView`, including a `BatchView::decimal128` accessor and shared Q14 payload array kernel.
        - Validation: `cargo test -q` and release build passed. Individual SF=10 samples: Q06 `~100.0ms` vs DuckDB `~123.0ms`, Q12 `~147.9ms` vs DuckDB `~125.0ms`, Q14 `~168.6ms` vs DuckDB `~238.0ms`. Full SF=10 single-process comparison sampled Dodam `4.813s`, DuckDB `5.766s`, ratio `0.835x`; remaining slower queries were Q04 (`~21.9ms`) and Q12 (`~6.3ms`).
      - Selection-vector experiment for Q04/Q12:
        - Added an opt-in `SelectionVector` path for Q04 lineitem date predicate before atomic candidate marker lookup and for Q12 lineitem date predicate before shipmode/hash pending updates. It is disabled by default behind `DODAM_Q04_ENABLE_LINEITEM_SELECTION_VECTOR=1` and `DODAM_Q12_ENABLE_LINEITEM_SELECTION_VECTOR=1`.
        - Focused SF=10 A/B did not show a default-worthy win. Q04 default-off sampled `~168.5ms`, opt-in `~166.6ms` in one noisy run; earlier same-binary samples were slightly worse with selection vectors. Q12 default-off sampled `~149.0ms`, opt-in `~149.2ms`. Lesson: compact row-id vectors after Arrow batch materialization do not fix the main decode/materialization cost; real improvement still needs a lower-level fused Parquet payload decoder or page-level selection.
        - Q01 small-group aggregate is already mostly in the intended shape: fixed small group slots plus scan-aggregate `BatchView` fusion. Further Q01 work should target Parquet decode/page fusion rather than another hash/group loop rewrite.
      - Q04 direct ColumnReader experiment:
        - Added an opt-in `DODAM_Q04_ENABLE_LINEITEM_DIRECT_COLUMN_READER=1` path that reads `l_orderkey`, `l_commitdate`, and `l_receiptdate` through parquet-rs `ColumnReader` and fuses predicate/probe/count without creating Arrow `RecordBatch`.
        - Focused SF=10 A/B regressed: default Arrow row-group-map path sampled `~164.3ms`, direct column reader sampled `~197.0ms`. Profile showed Q04 lineitem direct path total around `~175ms` with parallel-sum read `~805ms` and consume `~248ms`, so bypassing `RecordBatch` at this API level is slower than Arrow's optimized reader.
        - Lesson: a useful fused decoder likely needs a lower-level page/vector decoder integrated with buffering and projection, not ad-hoc `ColumnReader::read_records` loops per column.
      - Direct column scan boundary pass:
        - Moved the null-free `i64/i32/i32` direct Parquet column loop into `storage` as `scan_parquet_i64_i32_i32_columns`, with reusable row-group, read, and consume metrics. Q04's opt-in direct lineitem path now supplies only the fused predicate/probe/count closure.
        - This is a structural generalization step for page/vector decoder work, not a speed win. Focused individual SF=10 Q04 default path sampled Dodam `~162.6ms` vs DuckDB `~127.0ms`; the opt-in direct path sampled Dodam `~191.9ms` vs DuckDB `~123.0ms`.
        - The important result is negative: query-local direct `ColumnReader` loops should not be promoted as a fast path. The next useful version needs to sit below Arrow `RecordBatch` materialization but above query-specific code: a buffered page/vector decoder that can expose typed vectors, selection, and payload decode policy through a common storage API.
      - Q12 selective payload boundary pass:
        - Added `scan_parquet_i64_byte_array_payload_columns` and `DirectByteArrayPayloadReader` in `storage`. Q12's opt-in direct orders path no longer opens parquet column readers or resolves column indexes in SQL code; it receives key vectors and a payload skip/read wrapper from storage, while keeping its selective payload policy in the query closure.
        - Validation: `cargo test -q` and release build passed. Individual SF=10 Q12, run sequentially, sampled default Dodam `~147.8ms` vs DuckDB `~125.0ms`; direct opt-in sampled Dodam `~162.6ms` vs DuckDB `~124.0ms`.
        - A simultaneous default/opt-in run produced much worse numbers due I/O/cache interference and is not used as a benchmark record. Keep the "individual benchmark only" rule for future measurements.
        - Lesson is unchanged: public `ColumnReader` can now be experimented with through a common boundary, but the performance gap still requires a buffered page/vector decoder or tighter Arrow reader integration rather than more query-local direct-reader code.
      - Direct scan engine/cache boundary pass:
        - Routed Q04/Q12 opt-in direct scans through `DodamEngine` wrappers so direct experiments use the same object-store/file-cache boundary as normal Parquet scans instead of SQL opening `SerializedFileReader<File>` directly.
        - Validation: `cargo test -q` and release build passed. Individual SF=10 direct opt-in samples did not improve: Q12 direct `~170.0ms` vs DuckDB `~123.0ms`, Q04 direct `~194.8ms` vs DuckDB `~123.0ms`. Enabling `DODAM_FILE_CACHE_BYTES=512MiB` for Q12 direct worsened to `~226.8ms`, so cache lookup/copy overhead is not useful for this public `ColumnReader` path.
        - Default-path individual recheck after the engine-boundary change: Q04 `~154.2ms` vs DuckDB `~123.0ms`, Q12 `~153.7ms` vs DuckDB `~124.0ms`. A bundled Q04/Q12 run was discarded as a record because it violates the individual-benchmark rule.
        - Lesson: keeping direct scan I/O under engine ownership is the right architecture, but it confirms again that the next real performance work must target fused Arrow/page decode rather than caching around `ColumnReader::read_records`.
      - Stream fold vectorization pass:
        - Moved additional fallback stream folds from `parallel_batch_fold_chunks` to `parallel_batch_fold_view_chunks`: Q01 projected aggregate fallback, Q12 lineitem stream fallback, and Q03 orders stream fallback now consume `BatchView` directly and keep per-chunk state instead of mapping owned `Vec<RecordBatch>` first.
        - Removed the now-unused Q01 projected `RecordBatch` wrapper. This is mostly structural: it makes the fallback path match the row-group-map/vector consumer shape and reduces query-local batch ownership, but it does not bypass Arrow `RecordBatch` materialization.
        - Validation: `cargo test -q` and release build passed. Individual SF=10 samples: Q01 Dodam `~169.3ms` vs DuckDB `~186.0ms`, Q03 Dodam `~258.1ms` vs DuckDB `~247.0ms`, Q12 Dodam `~156.5ms` vs DuckDB `~126.0ms`.
      - Stream fold vectorization pass 2:
        - Moved Q05 order build, Q05 revenue stream fallback, Q07 order build, Q07 volume stream fallback, and Q08 market-share stream fallback to `parallel_batch_fold_view_chunks`.
        - Added small `BatchView` wrappers for Q05/Q07 order maps and Q08 market-share so fallback stream execution now follows the same vector consumer shape as row-group-map paths. The fallback still clones `RecordBatch` only when the typed/projected layout does not match.
        - Validation: `cargo test -q` and release build passed. Individual SF=10 samples: Q05 Dodam `~278.3ms` vs DuckDB `~296.0ms`, Q07 Dodam `~307.6ms` vs DuckDB `~314.0ms`, Q08 Dodam `~371.6ms` vs DuckDB `~399.0ms`.
      - Parquet page-index metadata pass:
        - Changed the shared Parquet metadata cache to load Arrow reader metadata with `PageIndexPolicy::Optional` by default, with `DODAM_PARQUET_PAGE_INDEX=0` as a rollback knob. The cache key now includes the page-index mode so on/off metadata is not mixed, and dictionary-preserved metadata rebuilds keep the same reader option.
        - Individual SF=10 focused samples before making it default: Q04 improved from `~154.6ms` to `~144.7ms`; Q12 improved from `~151.9ms` to `~146.1ms`.
        - After making page index default, individual SF=10 samples were Q04 `~143.5ms` vs DuckDB `~126.0ms`, and Q12 `~138.2ms` vs DuckDB `~124.0ms`. Disabling it with `DODAM_PARQUET_PAGE_INDEX=0` moved Q12 back to `~150.1ms`, confirming the rollback and effect.
        - Full SF=10 single-process comparison improved in the same-binary A/B area from Dodam `4.920s` vs DuckDB `5.868s` (`0.838x`) with page index disabled/default-old to Dodam `4.737s` vs DuckDB `5.847s` (`0.810x`) with page index enabled. Only Q04 remained slower in that enabled full run.
        - Failed follow-up: forcing Arrow RowFilter on top of page index was still worse for Q04 (`~147.7ms`, slower than page-index default) and much worse for Q12 lineitem (`~236.5ms`). Page index is useful as general scan metadata, but Arrow RowFilter predicate/reconstruction overhead is still not a default-worthy path for these distributions.
      - Deeper vector pipeline boundary pass:
        - Collapsed the engine's non-view late materialization implementation into the `BatchView` implementation. Existing public batch-callback callers now adapt through `BatchView`, so predicate selection and payload selection have one shared execution path.
        - Made `BatchView` an internal enum-backed boundary instead of a direct `RecordBatch` field. It still only has a `RecordBatch` variant today, but the type is now ready for a future page/vector decoded view without changing consumer signatures.
        - Removed the old `parallel_batch_fold_chunks` helper after converting the remaining Q03 fallback and Q16 stream fallback chunk folds to `parallel_batch_fold_view_chunks`. A direct search for `parallel_batch_fold_chunks(` now returns no matches.
        - Moved Q09 and Q11 late-materialized predicate/payload callbacks to `BatchView`; Q09 now uses projected-position typed access in both selection and payload consume, and Q11 does the same for supplier selection and partsupp payload.
        - Validation: `cargo test -q` and release build passed. Individual SF=10 samples: Q03 `~249.4ms` vs DuckDB `~246.0ms`, Q09 `~591.4ms` vs `~611.0ms`, Q11 `~40.1ms` vs `~28.0ms`, Q16 noisy at `~81.6-93.9ms` vs `~61-62ms`. Disabling Q11 late materialization was worse (`~62.5ms`), so Q11's late path remains useful but still not DuckDB parity in isolated runs.
        - Full SF=10 single-process comparison after the pass sampled Dodam `4.721s`, DuckDB `5.880s`, ratio `0.803x`; only Q04 was slower (`~9.1ms` gap). Q11/Q16 were faster than DuckDB in the full run, so their individual slow samples are noisy/context-sensitive rather than a clear retained regression.
      - Q04 vector pipeline follow-up:
        - Re-profiled Q04. Lineitem still dominates; profile summary for `lineitem[l_orderkey,l_commitdate,l_receiptdate]` showed parallel-sum `read_next` around `~784ms` and consume around `~279ms`, so Arrow/Parquet decode remains the larger cost but the atomic consume loop is non-trivial.
        - Re-tested Q04 lineitem row-group chunk sequentially after page-index work. Default chunk `2` remained best/stable enough: default sampled `~142.3ms`, chunk `1` `~148.1ms`, chunk `4` `~144.2ms`, chunk `8` `~150.6ms`. A simultaneous chunk sweep was discarded because it violated the individual-benchmark rule and showed obvious I/O interference.
        - Tried splitting the atomic marker into dense immutable priority bytes plus a separate atomic live marker so non-candidate rows would avoid atomic loads. It regressed clearly (`~171-175ms` focused), likely due worse cache locality/two dense arrays offsetting cheaper non-candidate checks. Reverted the experiment.
        - Moved Q04 lineitem late-materialized opt-in callbacks to the direct `BatchView` late-materialization API. This keeps the page/selection-aware boundary consistent, but it is not a default speed win: focused default samples in this run were noisy around `~156-157ms`, and late opt-in sampled `~153.6ms`, still slower than the best default area.
        - Tried a compact candidate-domain bitset probe: build a dense `orderkey -> candidate-id` map, let each row-group worker set local candidate bits, then OR partial bitsets and count priorities. It regressed badly (`~261.2ms`) because the large `u32` candidate map build/access cost and extra merge work outweighed removing atomic marker writes. Reverted; the single dense `AtomicU8` marker remains the better Q04 duplicate-elimination representation so far.
      - Late-materialization view convergence:
        - Converted the remaining non-view late materialization call sites to `BatchView`: Q07 volume shipping, Q08 market share, and Q19 discounted revenue. A direct search for `late_materialized_parquet_map_with_policy(` and `late_materialized_parquet_map_pruned_with_policy(` in `src/sql.rs` now returns no matches.
        - Q08/Q07/Q19 predicate and payload callbacks now use projected-position typed access where possible, and Q07/Q08 payload decimal loops were split into reusable typed helpers shared by the view paths.
        - Validation: `cargo test -q` and release build passed. Individual SF=10 samples: Q07 `~298.1ms` vs DuckDB `~318.5ms`, Q08 `~365.0ms` vs `~400.5ms`, Q19 `~227.4ms` vs `~261.0ms`.
        - Full SF=10 single-process comparison after this pass sampled Dodam `4.720s`, DuckDB `5.881s`, ratio `0.803x`; no TPC-H query was slower in that run. Q04 sampled `~144.6ms` vs DuckDB `~147.0ms`, but Q04 remains noisy and should still be treated as the primary close-gap query.
      - Scan-aggregate view convergence:
        - Moved the remaining SQL-side `parquet_scan_fold_chunks` users, Q19 fallback lineitem revenue and Q20 lineitem quantity sums, to `parquet_scan_accumulate_chunks_view`.
        - Q19/Q20 now consume fixed-projection `BatchView` typed columns first and keep the older `RecordBatch` name-based logic only as a layout fallback. A direct search for `parquet_scan_fold_chunks(` in `src/sql.rs` and `src/engine.rs` returns no matches.
        - This is a structural vector-pipeline step, not yet a real page/vector decoder fusion: the underlying source is still Arrow `RecordBatch` materialization, but query execution no longer requires owned batch map/fold closures on these scan aggregates.
        - Validation: `cargo test -q` and release build passed. Individual SF=10 samples: Q19 `~428.3ms` vs DuckDB `~483.5ms`; Q20 first sampled cold/noisy at `~480.2ms` vs DuckDB `~367.5ms`, then immediate individual recheck sampled `~206.9ms` vs DuckDB `~236.0ms`.
        - Full SF=10 single-process comparison after this pass sampled Dodam `4.685s`, DuckDB `5.868s`, ratio `0.798x`; only Q04 was slower (`~12.8ms` gap). Treat Q04 as the remaining close-gap target, while the larger structural gap remains Arrow `RecordBatch` materialization rather than true page/vector decoder fusion.
      - Raw-vector BatchView boundary pass:
        - Added the first non-`RecordBatch` `BatchView` variant for direct `i64/i32/i32` vectors and an engine wrapper that exposes direct Parquet column scans as `BatchView` consumers.
        - Rewired the opt-in Q04 direct column reader to reuse the same `q04_count_late_candidate_priorities_atomic_view` consumer as the Arrow row-group-map path. This proves the vectorized consumer boundary can accept non-Arrow batches without changing query logic.
        - This is not a default performance win yet: individual SF=10 Q04 default sampled `~154.4ms` vs DuckDB `~124.5ms`, while the opt-in direct raw-vector path sampled `~189.0ms` vs DuckDB `~122.5ms`. Keep direct column reader disabled by default.
        - Lesson: the useful next step is not public `ColumnReader` loops, but plugging a buffered page/vector decoder into this `BatchView` raw-vector boundary so decode, selection, and payload policy are fused below Arrow `RecordBatch` materialization.
        - Follow-up cleanup removed per-batch count summing from the Q04 direct raw-vector consumer by returning raw-loop hit counts directly. It did not improve the direct path: Q04 direct opt-in still sampled `~191.6ms` vs DuckDB `~124.0ms`, while default sampled `~142.1ms` vs DuckDB `~123.5ms`. This confirms the direct path gap is not SQL consumer accounting but the public `ColumnReader` scan/decode path.
      - Q04 sparse candidate Bloom front-filter experiment:
        - Profiling Q04 showed lineitem row-group-map summary around `read_next ~802ms` and `consume ~214ms` parallel-sum, with wall `Q04 late lineitem probe ~138ms`; read/decode is dominant, but random atomic probe is still visible.
        - Checked row-group pruning potential with DuckDB metadata: all 524 lineitem row groups have at least one Q04 candidate orderkey in their min/max range, so row-group range skip cannot help this dataset.
        - Added an opt-in `DODAM_Q04_ENABLE_CANDIDATE_BLOOM=1` front-filter before atomic marker probes. It regressed badly: default-off Q04 sampled `~145.8ms` vs DuckDB `~123.5ms`, while Bloom opt-in sampled `~192.4ms` vs DuckDB `~124.5ms`.
        - Lesson: for Q04/SF=10, two hash probes plus bit checks cost more than the avoided relaxed atomic loads. The remaining gap still points back to Arrow/Parquet read/decode and the need for page/vector-level fusion rather than another row-loop prefilter.
      - Direct ColumnReader required-level experiment:
        - Added opt-in `DODAM_DIRECT_SKIP_REQUIRED_DEF_LEVELS=1` for the direct `i64/i32/i32` vector scan. When parquet schema says `max_def_level == 0`, it passes `None` for definition-level buffers instead of decoding/filling them.
        - It regressed Q04 direct opt-in: normal direct path sampled `~191.1ms` vs DuckDB `~124.5ms`, while skip-required-levels sampled `~244.7ms` vs DuckDB `~130.0ms`.
        - Lesson: parquet-rs public `ColumnReader::read_records` is not faster on this path when def-level buffers are omitted. Keep this disabled by default; it reinforces that useful decode fusion needs a different buffered page/vector path, not just toggling public ColumnReader arguments.
      - Arrow scan batch-size and p95 profiling pass:
        - Re-tested larger Arrow batch sizes because scan-heavy paths still pay `RecordBatch` materialization/dispatch costs. Q04 worsened at 32K (`~152.5ms`) and 64K (`~163.0ms`) vs the 16K area. Q01 also worsened at 32K (`~196.5ms`) vs default (`~151.5ms`). Keep 16K as the default; larger batches hurt cache/locality more than they reduce dispatch overhead.
        - Added profile-only `reader_next_p95` samples to `ParquetBatchReader` and row-group-map logs. Sampling is enabled only under `DODAM_SCAN_PROFILE`/`DODAM_TPCH_PROFILE`, so normal execution does not store per-batch timings.
        - Q04 profile after this change showed lineitem summary around `reader_next_avg ~0.192ms`, `reader_next_p95_max ~0.847ms`, `reader_next_max ~1.385ms`, `read_next ~765ms`, and `consume ~211ms` parallel-sum. The issue is aggregate decode volume, not a few extreme slow batches.
      - Raw-column BatchView generalization pass:
        - Replaced the query-shaped raw `i64/i32/i32` `BatchView` variant with a generic `RawColumnView` slice. The engine direct scan wrapper now constructs raw column views by projected position and calls the same `BatchView::from_raw_columns` entrypoint that future primitive/page-vector decoders can reuse.
        - Removed the now-unused query-specific constructor so the raw-vector boundary is no longer tied to Q04's exact column layout. This is intentionally infrastructure, not another TPC-H fast path.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q04 default sampled Dodam `~144.7ms` vs DuckDB `~126.5ms`; direct raw-column opt-in sampled Dodam `~196.2ms` vs DuckDB `~121.5ms`.
        - Lesson: the generic raw-column boundary is the right shape for broader vectorized pipeline work, but the public parquet-rs `ColumnReader` path is still too slow. The next real win should come from feeding this boundary from a buffered page/vector decoder or Arrow reader extension, not from more query-local direct-reader special cases.
      - Direct primitive scanner generalization pass:
        - Replaced the storage-level `i64/i32/i32` direct scan loop with a generic `DirectPrimitiveColumnSpec` scanner for primitive `I64`/`I32` columns. The engine now exposes `scan_parquet_primitive_columns_view`, and the old Q04-shaped wrapper is only a thin compatibility helper over that generic API.
        - Added raw `BatchView` debug validation that all raw columns in a batch have the same row count. This is a guardrail for future page/vector decoders feeding the same execution boundary.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q04 default sampled Dodam `~154.4ms` vs DuckDB `~124.0ms`; direct primitive opt-in sampled Dodam `~193.1ms` vs DuckDB `~128.0ms`.
        - Lesson: the generic scanner did not create a speed win, which is expected. Its value is architectural: direct primitive decoding is no longer tied to Q04's exact three-column shape, and future decoder work can target one storage/engine boundary instead of adding query-specific fast paths.
      - Direct primitive logical-type cleanup:
        - Added a logical `Date32` raw primitive view and changed the Q04 direct wrapper to mark `l_commitdate`/`l_receiptdate` as `Date32` instead of generic `I32`. The compatibility consumer still sees physical `i32` slices where needed, but the storage/engine boundary now preserves logical intent for future vectorized date predicates.
        - Avoided per-batch heap allocation for common direct primitive view shapes by using stack arrays for 0-4 projected columns and keeping a Vec fallback only for wider scans.
        - Checked parquet-rs 59.0.0 lower-level APIs. `SerializedPageReader`, `PageReader`, and `GenericColumnReader` are public, but the current public `ColumnReaderImpl` already uses that decoder core. So simply rebuilding the current direct path with page readers is unlikely to close the gap; a useful version needs a real fused page/vector reader with predicate/payload policy, not just another wrapper over `read_records`.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q04 direct opt-in sampled Dodam `~192.1ms` vs DuckDB `~126.0ms`, effectively unchanged from the previous `~193.1ms` area.
      - Q12 pending hash-map pass:
        - Switched Q12's pending order accumulator from the default `HashMap` hasher to the project-wide `FastHashMap`. This is a general hash-heavy accumulation improvement, not a TPC-H-only branch: Q12 lineitem creates many small per-chunk pending maps and then merges them.
        - Retuned `DODAM_Q12_PENDING_MAP_INITIAL_CAPACITY` default from `2048` to `4096`. Individual SF=10 checks: default fast map at `2048` sampled `~141.8ms`; `4096` sampled `~137.1ms` in the env sweep and `~133.6ms` after making it default; `8192` was `~138.4ms`, `16384` `~139.3ms`, and `262144` regressed badly to `~261.1ms` because every parallel partial over-allocated.
        - Also factored Q04 dense atomic marker probing into a single helper with one range check before marker access. Q04 did not improve in the individual sample (`~156.6ms` vs DuckDB `~124.5ms`), so treat that as structural cleanup, not a speed win.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Q12 individual SF=10 improved from the pre-change sample `~147.8ms` vs DuckDB `~124.5ms` to `~133.6ms` vs DuckDB `~128.0ms`, reducing the gap from about `23.3ms` to `5.6ms`.
      - Q12 vector predicate cleanup:
        - Centralized Q12's lineitem date predicate (`commitdate < receiptdate`, `shipdate < commitdate`, and receipt-date range) into an inline predicate kernel used by dictionary, string, fallback, and late-materialization selection loops.
        - Centralized Q12 pending updates behind `q12_pending_increment`/`q12_pending_add_order`, leaving the internal implementation on `entry` because an attempted `get_mut`+`insert` variant double-hashed new keys and was slower (`~135.2ms` vs the prior `~133.6ms` area).
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q12 after the date predicate cleanup sampled Dodam `~128.5ms` vs DuckDB `~125.0ms`, reducing the remaining gap to about `3.5ms`.
      - Dictionary vector view boundary pass:
        - Added `DictionaryI32View` and a raw `RawColumnView::DictionaryI32 { keys, values }` shape so Arrow dictionary arrays and future raw dictionary-key vectors can use the same match-flag helper.
        - Moved Q12 lineitem shipmode and Q12 orders priority dictionary paths to `dictionary_i32_view_match_flags`; added a small `Q12LineitemDictionaryView` layout to bind projected columns once before the dictionary row loop.
        - This is structural vector-pipeline work, not a speed win yet. Individual SF=10 Q12 samples after the view abstraction were around `~129.2-131.8ms` vs DuckDB `~123-124ms`, slightly noisier/slower than the best prior `~128.5ms` sample. Keep it because it creates the common boundary needed for raw dictionary/page decoder work; do not count it as a performance improvement.
        - Broadened the same dictionary view boundary to Q16 part brand/type dictionaries and Q04 candidate order-priority dictionaries, removing the remaining SQL-side direct calls to `dictionary_i32_string_values`. Individual SF=10 samples after broadening: Q16 `~76.0ms` vs DuckDB `~62.5ms`, Q04 `~145.7ms` vs DuckDB `~124.5ms`, Q12 `~132.2ms` vs DuckDB `~126.0ms`. Treat this as infrastructure for raw dictionary/page vectors rather than a measured win.
        - Added a `Q16PartDictionaryView` layout that prebinds `p_partkey`, dictionary `p_brand`/`p_type`, and `p_size` once per batch before the Q16 part-group loop. This removes repeated SQL-local downcast/layout branching from the hot loop and follows the same vector boundary style as Q12.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q16 samples were noisy: first run Dodam `~85.9ms` vs DuckDB `~70.5ms`, immediate rerun Dodam `~74.1ms` vs DuckDB `~66.5ms`. Treat this as structural cleanup only, not a proven speed win.
        - Added a shared `Q16SupplierKeyView` for the supplier-count side so packed distinct, bitset, and hash fallback paths all consume prebound `Int64` part/supplier key arrays from the `BatchView` boundary instead of each helper downcasting `ArrayRef`s.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q16 reruns sampled Dodam `~79.0ms` vs DuckDB `~69.5-70.0ms`. This is neutral/noisy versus the prior Q16 range, so count it as vector-pipeline convergence, not a speed improvement.
        - Added common `BatchView::try_record_batch` and `BatchView::i32/required_i32` accessors, then changed Q11 late-materialized callbacks to avoid raw-view `record_batch()` fallback panics. Q11 now returns a normal fallback signal if a future raw/page vector view cannot satisfy the typed layout.
        - Extended Q16 supplier key views from Arrow-only `Int64Array` references to an Arrow-or-raw `i64` vector wrapper. The same packed distinct/bitset/hash supplier loops can now consume future raw primitive vectors without changing query code.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 samples: Q11 `~38.3ms` vs DuckDB `~28.0ms`, Q16 `~79.2ms` vs DuckDB `~66.0ms`. This is vector-pipeline infrastructure and safety work; current Arrow-backed performance is effectively unchanged.
        - Moved the Arrow-or-raw `i64` wrapper from Q16-local SQL code into `vector::I64VectorView`, with `BatchView::i64_vector(index)` as the common accessor. Q16 supplier counting now uses this shared primitive vector boundary instead of a query-local equivalent.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q16 sampled Dodam `~79.1ms` vs DuckDB `~69.5ms`, matching the prior range. This is a generalization step, not a speed win.
        - Added `vector::I32VectorView` and `vector::Date32VectorView`, plus `BatchView::i32_vector`/`date32_vector`, so raw primitive/date vectors and Arrow arrays share the same hot-loop boundary.
        - Rewired Q04 late lineitem payload date comparison to consume `Date32VectorView`, and Q16 dictionary part-size layout to consume `I32VectorView`/`I64VectorView` instead of SQL-local downcasts.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 samples: Q04 `~152.5ms` vs DuckDB `~123.5ms`, Q16 `~80.2ms` vs DuckDB `~66.0ms`. This is infrastructure for raw/page-vector execution; no current Arrow-backed speed win.
        - Added `vector::Decimal128VectorView` and raw `RawColumnView::Decimal128`, then moved Q06 view execution from Arrow-only `DecimalInput`/`Date32Array` downcasts to `Decimal128VectorView` + `Date32VectorView`.
        - Removed the now-unused Q06 Arrow-array helper variants; Q06 view execution now has one vector-view kernel that can consume Arrow or future raw decimal/date vectors.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q06 sampled Dodam `~94.3ms` vs DuckDB `~123.0ms`. This is primarily vector-pipeline infrastructure; the small improvement versus prior `~100ms` samples may be noise but did not regress.
        - Moved engine-owned Q06 late-materialized selection/payload callbacks and Q14 late-materialized date/payload callbacks onto `BatchView` primitive/decimal vector accessors, avoiding `record_batch().clone()` fallback in their normal projected layouts.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 samples: Q06 `~91.4ms` vs DuckDB `~123.0ms`, Q14 `~161.4ms` vs DuckDB `~238.5ms`. This is a real vector-boundary cleanup with small positive/no-regression performance signal.
        - Moved Q03 late-materialized order/date selection and revenue payload callbacks to `I64VectorView`/`Date32VectorView`/`Decimal128VectorView`, and changed Q12 late i64 selection/payload fallbacks to use `I64VectorView` plus safe `try_record_batch` fallback.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 samples: Q03 `~247.5ms` vs DuckDB `~248.5ms`, Q12 `~130.3ms` vs DuckDB `~124.0ms`. No regression; Q03 is parity in this run and Q12 remains a small close-gap target.
        - Moved Q10 returned-revenue late selection/payload and Q19 late payload onto vector views where their projected layouts allow it, and converted remaining Q10/Q19 `record_batch()` fallbacks to safe `try_record_batch` fallbacks.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 samples: Q10 `~229.4ms` vs DuckDB `~316.5ms`, Q19 `~222.5ms` vs DuckDB `~262.0ms`. No regression; both remain faster than DuckDB in this harness.
        - Extended the generic direct primitive scanner to preserve Decimal128 logical intent for physical INT64 decimal columns, and added an opt-in Q06 direct primitive experiment that feeds raw `Date32`/`Decimal128` vectors into the same vector-view Q06 kernel.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q06 default sampled Dodam `~92.1ms` vs DuckDB `~120.5ms`; opt-in `DODAM_Q06_ENABLE_DIRECT_PRIMITIVE=1` sampled Dodam `~143.1ms` vs DuckDB `~122.0ms`.
        - Lesson: the Decimal128 raw-vector boundary is now general enough for future fused/page-vector sources, but the parquet-rs public `ColumnReader` path is still not a speed path. Keep the Q06 direct primitive route disabled by default; real improvement still needs a lower-level buffered decoder or Arrow reader integration that avoids `RecordBatch` materialization without paying public-column-reader overhead.
        - Added a shared `Utf8VectorView` boundary and moved Q10 returned-revenue returnflag checks plus Q12 lineitem/order priority string paths onto `BatchView` string vectors. Q12's projected typed/profile/sorted order aggregators now consume `I64VectorView + Utf8VectorView` instead of pulling `ArrayRef/StringArray` directly from the `RecordBatch`.
        - This is part of the fast-path removal direction: the TPC-H query handlers still exist, but their hot loops depend less on Arrow-specific `RecordBatch`/downcast shapes and more on reusable vector inputs that planner/execution rules or future page-vector decoders can feed.
        - Validation: `cargo test -q` and release build passed. Individual SF=10 samples: Q10 Dodam `~230.7ms` vs DuckDB `~318.0ms`; Q12 Dodam `~131.3ms` vs DuckDB `~124.5ms`. Q10 remained stable/faster; Q12 remains a close-gap query with no structural regression relative to the recent `~128-132ms` range.
        - Moved Q10 orders-side customer build from Arrow-only `required_i64/required_date32` accessors to `I64VectorView + Date32VectorView`. This is another small fast-path removal step: the loop is still query-shaped, but it no longer requires Arrow arrays when the projected primitive layout is available through the vector boundary.
        - Validation: `cargo test -q` and release build passed. Individual SF=10 Q10 sampled Dodam `~231.7ms` vs DuckDB `~318.0ms`, matching the previous `~230.7ms` range.
        - Moved Q15 lineitem revenue aggregation from Arrow-only projected arrays to `I64VectorView + Date32VectorView + Decimal128VectorView`, including a null-free raw-slice loop for discounted revenue. Raw/page-vector inputs can now feed this aggregation through the same view boundary instead of requiring `RecordBatch` downcasts.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q15 sampled Dodam `~185.4ms` vs DuckDB `~213.5ms`, so the structural move kept Q15 faster in this harness.
        - Moved Q20 lineitem quantity aggregation to `I64VectorView + Decimal128VectorView + Date32VectorView`, with a null-free raw-slice loop for quantity sums. The view path no longer needs `view.column()`/Arrow `ArrayRef` access for the projected lineitem layout.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q20 sampled Dodam `~200.7ms` vs DuckDB `~237.5ms`, keeping Q20 faster while reducing Arrow-specific hot-loop dependency.
        - Moved Q21 lineitem order-state and ordered row-group chunk processing to `I64VectorView + Date32VectorView`. Both the unordered state-map path and the ordered chunk path now use vector inputs for the projected lineitem layout, with `try_record_batch` fallback only when the view cannot expose those vectors.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q21 sampled Dodam `~516.0ms` vs DuckDB `~641.5ms`, keeping the query faster while removing another Arrow-specific hot-loop dependency.
        - Moved Q03 projected lineitem revenue paths to `I64VectorView + Date32VectorView + Decimal128VectorView`, covering both hash-map revenue accumulation and sorted-order lookup accumulation. The view path no longer needs `view.column()`/Arrow `ArrayRef` access for the projected lineitem layout.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q03 sampled Dodam `~254.8ms` vs DuckDB `~245.5ms`, so Q03 remains a close-gap/slower query to revisit even though the vector-boundary move is structurally correct.
        - Moved Q04 candidate-order priority view paths further onto vector views: late candidate selection now uses `I64VectorView + Date32VectorView`, late priority payload uses `Utf8VectorView`, and the row-group candidate UTF8 path no longer requires `view.column()` for the projected primitive/string layout.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q04 sampled Dodam `~143.0ms` vs DuckDB `~123.5ms`; this confirms Q04's remaining gap is not candidate-side Arrow downcast overhead but still mainly lineitem scan/decode/probe work.
        - Moved Q01 projected pricing-summary aggregation to `Utf8VectorView + Date32VectorView + Decimal128VectorView`, preserving the existing contiguous single-byte and raw-complement hot loops through the vector boundary.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q01 sampled Dodam `~158.1ms` vs DuckDB `~184.5ms`, keeping Q01 faster while reducing projected scan dependency on Arrow `ArrayRef` access.
        - Moved Q09 order-year row-group-map view processing to `I64VectorView + Date32VectorView`, and moved Q07 order-customer build to shared `I64VectorView` key vectors. These are small but general vector-boundary reductions for join-build side scans.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 samples: Q07 Dodam `~293.9ms` vs DuckDB `~316.0ms`, Q09 Dodam `~580.5ms` vs DuckDB `~614.0ms`.
        - Moved Q09 default lineitem profit aggregation to `I64VectorView + Decimal128VectorView`, preserving the existing matched-index slice fast path when projected columns are null-free. The row-group-map hot path no longer depends on `view.column()`/Arrow `ArrayRef` access for its projected lineitem layout.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q09 sampled Dodam `~582.7ms` vs DuckDB `~612.5ms`, staying faster with effectively neutral performance versus the prior `~580.5ms` sample.
        - Moved Q09 opt-in late-materialized selection/payload callbacks to `I64VectorView + Decimal128VectorView`, removing `required_i64`/`view.column()`/panic-style `record_batch()` access from the normal projected layout. This keeps the late path compatible with future raw/page-vector batches.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q09 default sampled Dodam `~565.1ms` vs DuckDB `~614.5ms`; opt-in `DODAM_Q09_ENABLE_LATE_MATERIALIZE=1` sampled Dodam `~808.2ms` vs DuckDB `~615.5ms`. Keep Q09 late materialization disabled by default; vectorizing the callback did not change the earlier conclusion that this late path is structurally slower for the current distribution.
        - Moved Q07 late-materialized selection/payload callbacks to `I64VectorView + Date32VectorView + Decimal128VectorView`, removing the normal-layout dependency on `required_i64`/`required_date32` and decimal `view.column()` access.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q07 sampled Dodam `~296.6ms` vs DuckDB `~315.5ms`; explicit late-enabled sample was Dodam `~290.8ms` vs DuckDB `~315.0ms`.
        - Moved Q08 late-materialized market-share selection/payload callbacks to `I64VectorView + Decimal128VectorView`, replacing normal-layout `required_i64` and decimal `view.column()` access. Removed the now-unused Arrow-only Q08 late payload helper.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q08 sampled Dodam `~362.5ms` vs DuckDB `~401.5ms`.
        - Moved Q17 lineitem revenue aggregation to `I64VectorView + Decimal128VectorView`, including a null-free raw-slice loop for quantity/revenue accumulation and a safe `try_record_batch` fallback when vector views cannot expose the projected layout.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q17 sampled Dodam `~195.5ms` vs DuckDB `~316.5ms`, so this is mainly another Arrow-specific hot-loop removal rather than a close-gap speed fix.
        - Moved Q19 late-materialized selection and default lineitem revenue aggregation further onto `I64VectorView + Decimal128VectorView + Utf8VectorView`, and removed the now-unused Arrow-only Q19 typed helpers. Q19 now keeps its projected hot loops behind the shared vector boundary instead of `required_i64`/decimal `view.column()` access.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q19 sampled Dodam `~238.9ms` vs DuckDB `~260.5ms`. This remains faster than DuckDB in the harness, but it is not a speed win versus the recent best `~222-227ms` Q19 samples; count it as vector-pipeline convergence, not a performance improvement.
        - Moved Q11 late-materialized supplier selection and stock payload consume to shared primitive/decimal vector views, and added `I32VectorView::is_null` so the payload loop can consume future raw integer vectors without Arrow-only `required_i32` access.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q11 sampled Dodam `~39.3ms` vs DuckDB `~27.5ms`, still slower and roughly unchanged from the recent `~38ms` area. This is structural vector-pipeline work; Q11 still needs a different improvement to close the remaining gap.
        - Replaced Q16 supplier-count view fallback `record_batch()` calls with safe `try_record_batch` fallbacks for packed, bitset, and hash distinct paths. This removes another raw/page-vector panic point while keeping the existing vector-view hot paths unchanged.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q16 sampled Dodam `~77.7ms` vs DuckDB `~66.5ms`, within the recent noisy Q16 range and still a small slower query.
        - Moved Q10 customer late payload decimal/nation columns to `Decimal128VectorView + I64VectorView` and replaced remaining Q10 order/revenue view fallback `record_batch()` calls with safe `try_record_batch` fallbacks. The string payload is still Arrow-backed `Utf8VectorView`, but the primitive/decimal part no longer requires `view.column()`.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q10 sampled Dodam `~232.9ms` vs DuckDB `~318.5ms`, matching the recent `~230-232ms` range and staying faster.
        - Moved Q12 order-priority dictionary aggregation to `I64VectorView + DictionaryI32View`, removing the remaining `view.column(0)` orderkey dependency from the dictionary path and replacing Q12 order-side `record_batch()` fallbacks with safe `try_record_batch` errors for unsupported raw layouts.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q12 sampled Dodam `~131.5ms` vs DuckDB `~123.5ms`, still a close-gap slower query and roughly unchanged from the recent `~128-132ms` range.
        - Moved Q04 candidate order-priority build to `I64VectorView + Date32VectorView + Utf8VectorView`, removed the old Arrow-only `try_for_each_i64_date32_str` helper use from Q04, and moved lineitem late selection from Arrow `Int64Array` to `I64VectorView` with safe fallback.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q04 sampled Dodam `~144.8ms` vs DuckDB `~126.0ms`, unchanged from the recent Q04 range. The remaining gap is still lineitem scan/decode/probe, not candidate order build downcasts.
        - Moved Q02 region/nation dimension scans to `I64VectorView + Utf8VectorView` and replaced direct `record_batch()` fallback with safe `try_record_batch` errors for unsupported raw layouts.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q02 sampled Dodam `~69.8ms` vs DuckDB `~52.5ms`; no speed win. The Q02 gap is not the tiny region/nation downcast path and should be investigated in supplier/partsupp work.
        - Moved Q12 lineitem string/dictionary projected layouts from Arrow `Int64Array/Date32Array/StringArray` fields to `I64VectorView + Date32VectorView + Utf8VectorView/DictionaryI32View`, and replaced lineitem fallback `record_batch()` calls with safe `try_record_batch` errors for unsupported raw layouts.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q12 sampled Dodam `~130.1ms` vs DuckDB `~124.0ms`, a small improvement versus the previous `~131.5ms` sample but still within the close-gap/noisy range.
        - Moved Q02 supplier, part, and partsupp scans to `BatchView` vector paths. Supplier/part dimension scans now use `I64/I32/Decimal128/Utf8VectorView`, and partsupp min-cost aggregation now runs through `parallel_batch_fold_view_chunks` with an `I64VectorView + Decimal128VectorView` hot path.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q02 improved from the previous `~69.8ms` vs DuckDB `~52.5ms` to Dodam `~64.9ms` vs DuckDB `~52.0ms`, reducing the gap from about `17.3ms` to `12.9ms`.
        - Changed Q02 partsupp part/supplier membership filters from `HashSet<i64>` to `AdaptiveI64Set`, allowing dense membership checks for SF=10 key ranges instead of two hash probes per candidate row.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q02 improved again to Dodam `~59.7ms` vs DuckDB `~52.0ms`, reducing the remaining gap to about `7.7ms`.
        - Switched Q02's nation/supplier/part/min-cost maps to the project `FastHashMap` hasher. This keeps Q02 consistent with other hash-heavy paths, but the measured speed effect was neutral.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q02 sampled Dodam `~59.4ms` vs DuckDB `~51.5ms`, effectively unchanged from the adaptive-set result.
        - Moved Q04 lineitem row-group collect/atomic probe helpers from `view.column()`/Arrow `ArrayRef` access to `I64VectorView + Date32VectorView`, with safe `try_record_batch` fallback for unsupported raw layouts.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q04 sampled Dodam `~146.9ms` vs DuckDB `~123.5ms`, slightly worse than the recent `~144-145ms` area. Count this as vector-pipeline convergence, not a speed win; Q04's remaining gap still points to scan/decode/probe structure.
        - Changed Q02 partsupp partial aggregation to retain only per-part minimum-cost suppliers instead of carrying every candidate row plus a separate min-cost map. This is a more memory-bounded min-by aggregate shape for larger data, but it did not improve the SF=10 benchmark.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q02 sampled Dodam `~60.1ms` vs DuckDB `~52.0ms`, slightly worse than the prior `~59.4ms` sample. Count this as large-data structure cleanup, not a speed win.
        - Full SF=10 single-process snapshot after the recent vector-pipeline passes sampled Dodam `~4.684s` vs DuckDB `~5.850s` total median-sum (`~0.801x`). In that run only Q04 was slower: Dodam `~155.1ms` vs DuckDB `~144.5ms`, gap `~10.6ms`.
        - Q04 follow-up experiments: enabling lineitem selection-vector regressed (`~158.3ms` vs DuckDB `~122.0ms`), forcing row-filter regressed (`~211.8ms`), disabling row-group map regressed badly (`~409.9ms`), and row-group chunks `1`/`4` were worse than the default area in sequential measurements (`~155-156ms`). A Bloom branch-splitting code experiment also regressed (`~155.0ms`) and was reverted. Keep the default Q04 plan; remaining gap is structural scan/decode/probe overhead.
        - Parallelized the dense candidate-priority marker conversion from `Vec<u8>` to `Vec<AtomicU8>` and reused it across the Q04 default, late-materialized, and direct diagnostic paths. This is not tied to a column name or query predicate; it removes a serial conversion cost for large dense marker vectors before parallel probe stages.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q04 improved to Dodam `~128.9ms` vs DuckDB `~124.0ms`, reducing the latest measured Q04 gap to about `4.9ms` (`~1.04x`).
        - Full SF=10 single-process snapshot after the Q04 atomic-marker change sampled Dodam `~4.709s` vs DuckDB `~5.837s` (`~0.807x`). Only Q16 was marginally slower in that run: Dodam `~76.4ms` vs DuckDB `~76.0ms`.
        - Moved Q16 part-group construction from stream-serial dictionary scan consumption to row-group local partials plus final merge. This is a local/final aggregation structure: each chunk does filtering, dictionary lookup, and local string interning before the final global group merge.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q16 improved to Dodam `~74.9ms` vs DuckDB `~70.5ms`. `DODAM_Q16_PART_GROUP_CHUNK_SIZE=2` and `8` were worse (`~80.0ms`, `~81.2ms`), so the default chunk `4` stays. Forcing the bitset distinct path also regressed badly (`~113.9ms`), so packed distinct remains the default.
        - Full SF=10 snapshot after Q16 local/final sampled Dodam `~4.661s` vs DuckDB `~5.873s` (`~0.794x`) with no slower TPC-H query in that run. Q16 moved to `~61.8ms` vs DuckDB `~75.0ms` in the full-run sample.
        - Changed Q09 matched-index pre-materialization to opt-in (`DODAM_Q09_ENABLE_MATCHED_INDEX=1`) instead of default-on. The general lesson is that building a temporary selected-row index vector is not free; for the current Q09 distribution the single-pass predicate/order/supplier/supply lookup loop is slightly better.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q09 default-off sampled Dodam `~570.2ms` vs DuckDB `~615.5ms`; explicit matched-index enable sampled `~577.7ms`, and the prior default rerun sampled `~580.7ms`. Fanout supply-cost layout regressed (`~661.2ms`) and pair-hash stayed worse/no better than packed (`~585.9ms`), so packed supply-cost remains default.
        - Tried moving Q21 final-order key collection (`orders.o_orderstatus = 'F'`) from the existing sequential scan to dictionary row-group local sets plus final merge, mirroring the Q16 local/final pattern. It did not help: individual SF=10 Q21 sampled around `~516.6ms`, and profile showed final-order keys still around `~309-415ms` with higher dictionary row-group consume/merge cost. Reverted the code and kept this as a failed experiment; Q21's remaining bottleneck is not solved by local set partials for low-cardinality status.
        - Reworked Q21 final-order key collection to build one shared dense atomic marker vector in parallel, then convert it once to the normal `AdaptiveI64Set::Dense` membership structure. This avoids both the old serial dense insert loop and the failed row-group-local set merge. It is the same general pattern as the Q04 atomic marker improvement: use a shared dense marker when the key max is known from Parquet statistics and the result is immediately used as a membership filter.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q21 improved from the default rerun `~513.0ms` to `~194.7ms` vs DuckDB `~637.0ms`. Profile showed Q21 final-order keys dropping to about `~69ms` hot-run and ordered lineitem counts also dropping because the dense membership marker is cheaper to probe.
        - Full SF=10 single-process snapshot after the Q21 atomic-marker default sampled Dodam `~4.340s` vs DuckDB `~5.850s` (`~0.742x`). Q21 moved to `~196.3ms` vs DuckDB `~665.0ms`; only Q04 was marginally slower in that run (`~143.9ms` vs `~143.0ms`, gap under `1ms`).
        - Rechecked Q04 chunk tuning after the latest changes. Default individual SF=10 Q04 was `~141.6ms` vs DuckDB `~128.0ms`; candidate row-group chunk `4` stayed around `~141.6ms`, candidate chunk `8` regressed to `~148.6ms`, lineitem chunk `1` sampled `~142.8ms`, and lineitem chunk `4` sampled `~142.9ms`. Keep the current Q04 chunk defaults; the remaining gap is still lineitem scan/decode/probe structure rather than chunk granularity.
        - Generalized the dense atomic marker building block into `dense::DenseAtomicU8`. Q04 now uses the shared parallel `u8 -> AtomicU8` marker builder, and Q21 uses the shared zeroed marker plus `store_present`/`into_adaptive_i64_set` conversion instead of query-local marker construction. This is a vector-pipeline cleanup rather than a new query-specific path.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q21 stayed fast at `~193.1ms` vs DuckDB `~636.5ms`; Q04 sampled `~131.5ms` vs DuckDB `~122.5ms`, within the recent noisy Q04 range and better than the immediate `~141.6ms` rerun.
        - Started BatchView/vector-kernel commonization by moving the `I64 key + DictionaryI32/Utf8 target -> dense marker store` loop into `vector.rs` as reusable kernels. Q21 final-order atomic build now calls the shared vector kernel instead of query-local dictionary/string loops.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q21 stayed stable at `~193.2ms` vs DuckDB `~643.5ms`, so the common kernel did not regress the hot path.
        - Moved cached dense-membership probing from the SQL-local `adaptive_i64_set_contains_cached` helper into `AdaptiveI64Set::contains_cached`. This gives Q08/Q09/Q10/Q11/Q21 and generic late-selection loops one common membership-probe API while keeping the existing dense-slice fast path.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q09 stayed in range at `~575.9ms` vs DuckDB `~612.0ms`; Q21 stayed fast at `~190.7ms` vs DuckDB `~643.5ms`.
        - Moved Q03 orders-side build from `view.column()`/`record_batch()` fallback to `I64VectorView + Date32VectorView` with a safe `try_record_batch` fallback for unsupported raw layouts. This removes another projected scan hot-loop dependency on Arrow arrays and lets future raw/page-vector inputs feed Q03 order filtering directly.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q03 sampled Dodam `~247.4ms` vs DuckDB `~248.0ms`, effectively parity in this run.
        - Moved Q04 late candidate-priority counting from `view.column()`/Arrow `ArrayRef` downcasts to `I64VectorView + Date32VectorView`, and removed the now-unused Arrow-only typed helper. Unsupported raw layouts now use a safe `try_record_batch` fallback error instead of requiring `record_batch()`.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q04 sampled Dodam `~129.2ms` vs DuckDB `~125.0ms`, staying near the recent best but still leaving a small `~4.2ms` gap.
        - Moved Q05 order-side customer/nation build from `view.column()`/Arrow typed downcasts to `I64VectorView + Date32VectorView`, with dense `AdaptiveI64Map` slice probing when available and a safe `try_record_batch` fallback for unsupported raw layouts. This generalizes the common orderkey/custkey/date build pattern used by several TPC-H joins.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q05 sampled Dodam `~277.8ms` vs DuckDB `~300.0ms`, keeping Q05 faster in this harness.
        - Moved Q05 lineitem revenue aggregation from `view.column()`/Arrow typed downcasts to `I64VectorView + Decimal128VectorView`, including a dense `AdaptiveI64Map` supplier-nation slice probe in the null-free vector loop. This applies the same vector+adaptive-map pattern to the join/probe side rather than only the order build side.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q05 improved to Dodam `~265.2ms` vs DuckDB `~301.0ms` (`~0.881x`), a measurable win versus the prior `~277.8ms` sample.
        - Moved Q07 volume lineitem aggregation from `view.column()`/Arrow typed downcasts to `I64VectorView + Date32VectorView + Decimal128VectorView`, including the same dense supplier-nation slice probe in the null-free vector loop. This keeps Q07 aligned with the vector boundary used by Q05/Q03/Q09-style revenue loops.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q07 sampled Dodam `~297.8ms` vs DuckDB `~315.5ms`; this is no regression and still faster, but not a speed win versus the recent `~296ms` area.
        - Moved Q08 order-year build from `view.column()`/Arrow typed downcasts to `I64VectorView + Date32VectorView`, using dense `AdaptiveI64Map` presence slices for customer membership in the null-free vector loop and safe `try_record_batch` fallback for unsupported raw layouts.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q08 sampled Dodam `~362.5ms` vs DuckDB `~398.0ms`, stable with the recent Q08 range and still faster in this harness.
        - Moved Q08 lineitem market-share aggregation from `view.column()`/Arrow typed downcasts to `I64VectorView + Decimal128VectorView`, with dense slice probes for part membership, order-year lookup, and supplier-is-Brazil lookup when available. This removes the remaining Q08 projected lineitem dependency on Arrow arrays.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q08 sampled Dodam `~364.0ms` vs DuckDB `~400.5ms`, effectively unchanged from the prior `~362.5ms` sample and still faster in this harness.
        - Moved Q16 part-group stream fallback from `view.column()`/Arrow `ArrayRef` access to `I64VectorView + I32/I64 size vector` plus existing dictionary/string vector views. `Q16PartDictionaryView` now carries `I64VectorView` for partkeys, so dictionary and plain string layouts share the same primitive key boundary.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q16 sampled Dodam `~79.9ms` vs DuckDB `~66.0ms`; this is structural vector-pipeline cleanup only, not a speed win, and Q16 remains a small slower query in individual runs.
        - Replaced remaining Q03 fallback, Q08 dimension fallback, and Q15 supplier fallback `view.record_batch()` calls with safe `try_record_batch` errors. This does not change the normal vector path, but removes panic-style assumptions when future raw/page-vector views reach fallback branches.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Sequential individual SF=10 samples: Q08 Dodam `~358.8ms` vs DuckDB `~400.0ms`, Q15 Dodam `~186.2ms` vs DuckDB `~215.0ms`. A parallel Q08/Q15 benchmark run produced invalid inflated times, reinforcing that benchmark records should remain one-query-at-a-time.
        - Removed the final direct `view.record_batch()`/`view.column()` dependencies from SQL/engine view consumers and deleted the panic-style `BatchView::record_batch`/Arrow-column accessor APIs. Old RecordBatch callbacks now go through `try_record_batch`, while normal projected hot loops use typed vector accessors.
        - Validation: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q13 sampled Dodam `~219.1ms` vs DuckDB `~293.0ms`, so the generic dense-count/LIKE fallback safety cleanup did not regress Q13.
        - Re-profiled Q16 after the vector-boundary cleanup. Stage timing showed roughly `~14ms` bad-supplier scan, `~40-47ms` part groups, and `~40ms` supplier counts in profiled direct runs; part-group chunking was the most actionable knob.
        - Retuned `DODAM_Q16_PART_GROUP_CHUNK_SIZE` default from `4` to `2`. Sequential individual SF=10 sweep: chunk `1` `~85.9ms`, `2` `~66.8ms`, `4` `~69.7ms`, `5` `~70.7ms`, `10` `~80.8ms`, `20` `~96.1ms`; supplier-count chunk sweep kept default `4` as best/near-best. Confirmation with repeats `13`, warmup `3`: chunk `2` Dodam `~67.0ms` vs DuckDB `~70.0ms`, chunk `4` Dodam `~70.4ms` vs DuckDB `~65.5ms`.
        - Validation after making chunk `2` the default: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q16 sampled Dodam `~67.9ms` vs DuckDB `~69.5ms`, moving Q16 back to slightly faster in this run.
        - Rechecked Q12 after Q16. Current individual SF=10 baseline was Dodam `~130.4ms` vs DuckDB `~123.0ms`, with profile pointing to lineitem/orders scan chunking rather than typed-loop CPU.
        - Rechecked Q04 after the latest dense-marker/vector-boundary work. Direct primitive reader is still a clear loss (`~174.0ms` vs DuckDB `~122.0ms`), forced RowFilter is still slower (`~132.5ms` vs `~124.0ms`), and disabling atomic probe is much worse (`~156.9ms` vs `~125.0ms`).
        - Q04 lineitem late materialization is now slightly better than the default row-group-map path in a longer individual SF=10 run: default `~128.0ms` vs DuckDB `~125.5ms`, late path `~125.7ms` vs DuckDB `~125.0ms`. Made the late lineitem path the default and kept `DODAM_Q04_DISABLE_LINEITEM_LATE_MATERIALIZE=1` as the escape hatch. This is a general late-materialized scan rule use, not a raw column-name fast path; the path still falls back if the engine cannot produce the selected/payload views. After the default flip, Q04 sampled `~129.9ms` vs DuckDB `~126.0ms`, so the win is noisy but still in the recent best range.
        - Added a general `LateSelectionBuilder::push_selected_offsets` API so sparse late-selection builders can append only selected row offsets instead of calling `push(false)` for every non-selected row. Q04 lineitem selection now uses it for null-free orderkey vectors. Individual SF=10 Q04 sampled `~127.4ms` vs DuckDB `~124.0ms`, a small improvement over the immediate default-flip sample.
        - Tried reducing Q04 late payload consume overhead by slicing `selected_orderkeys` per payload batch instead of doing per-row `get(payload_offset)`. It was neutral/noisy rather than a clear win: Q04 sampled `~128.6ms` vs DuckDB `~124.5ms`. Keep it as vector-loop cleanup, not as a claimed speedup.
        - Tried applying the same sparse `push_selected_offsets` late-selection API to Q11 partsupp supplier selection, including an iterator variant without per-batch `Vec` allocation. It did not produce a stable win (`~37-39ms` vs DuckDB `~27ms`) and was reverted for Q11. The remaining Q11 gap is not per-unselected-row selection-builder overhead but partsupp late scan/payload materialization and aggregation cost.
        - Rejected a Q11 late row-group chunk retune from `1` to `2`. An initial sequential SF=10 sweep looked promising (`2` around `~35-37ms` vs chunk `1` around `~40ms`), but the same current binary later measured chunk `2` around `~41-42ms` while chunk `1` recovered to `~31.6ms` vs DuckDB `~28.0ms`. Keep chunk `1`; this is a benchmark-noise trap, not a stable default.
        - Final sequential individual checks after reverting the unstable Q11 changes: Q11 remains the main slower query at Dodam `~39.5ms` vs DuckDB `~27.0ms`; Q04 sampled near parity at Dodam `~126.9ms` vs DuckDB `~125.5ms`.
        - Fixed Q11 by changing the late payload stage from many chunk-local `HashMap<partkey, value>` aggregates plus merge into `(partkey, value)` vectors followed by one final dense `DenseI64F64Sum` aggregate. This removes repeated small hash-table work on the selected 4% partsupp payload and uses the dense integer key range only at the final aggregation boundary. Individual SF=10 Q11 improved to Dodam `~23.2ms` vs DuckDB `~27.0ms`.
        - Short full individual SF=10 recheck after the Q11 fix showed Q11 faster; longer confirmation left Q02 and Q04 as the only slower queries checked: Q02 `~57.7ms` vs DuckDB `~52.5ms`, Q04 `~127.7ms` vs DuckDB `~123.5ms`; Q12 reconfirmed faster at `~123.7ms` vs `~125.5ms`.
        - Improved Q02 supplier scan by moving supplier payload strings (`s_name`, `s_address`, `s_phone`, `s_comment`) behind a late-materialized selection on `s_suppkey`, `s_acctbal`, and `s_nationkey`. The selected supplier ratio is about `20.0%`, so this avoids decoding most wide supplier strings before the partsupp min-cost join. Individual SF=10 Q02 improved from about `~57.7-58.3ms` to `~51.4ms` vs DuckDB `~51.0ms`; disabling the new path with `DODAM_Q02_DISABLE_SUPPLIER_LATE_MATERIALIZE=1` returns to `~58.3ms`.
        - Q02 profile after the change: supplier late materialization selected `20033/100000` rows, supplier stage sampled `~21.1ms`, parts stage `~10.8ms`, partsupp min-cost stage `~35.3ms`. Q04 remains the main small gap query in focused checks, sampling `~125.6ms` vs DuckDB `~123.0ms`.
        - Post-Q02 full sequential short snapshot had only tiny/noisy slower blips. Longer individual confirmations showed those were noise: Q12 `~123.1ms` vs DuckDB `~125.5ms`, Q16 `~67.5ms` vs `~70.5ms`, Q11 `~20.1ms` vs `~27.0ms`, and Q04 `~126.4ms` vs `~126.5ms`.
        - Correctness/robustness pass after the SF=10 performance work:
          - Added DuckDB differential coverage for decimal scale/boundary filters, timestamp/timezone boundary filters, all-null aggregate groups, correlated subquery boolean combinations with `AND`/`OR`/`NOT EXISTS`, and `COPY CSV` header/escaping.
          - The new `OR NOT EXISTS` case exposed a real correlated fallback bug: outer-row binding can turn predicates such as `f.key IS NULL` into literal predicates like `1 IS NULL`, and the subquery-aware filter parser did not fold literal `IS NULL`/`IS NOT NULL`.
          - Fixed `parse_filter_with_subqueries` to handle `NOT` around subquery-aware predicates and to fold literal null predicates to boolean filter expressions. This keeps row-bound correlated subquery fallback compatible with SQL boolean combinations.
          - Validation: `cargo test -q --test duckdb_differential` passed with `26 passed, 1 ignored`; full `cargo check -q && cargo test -q` passed.
        - Extended the robustness pass further:
          - Added a scalar subquery edge matrix covering 0-row scalar subqueries, scalar `NULL` comparison behavior, `ORDER BY ... LIMIT 1` scalar subqueries, and `NOT IN (SELECT ...)` with RHS `NULL`.
          - Added nested/list/struct Parquet COPY roundtrip coverage: Dodam writes `List<Int32>` and `Struct<rank,label>` output to Parquet, reads it back, compares canonical rows, and asserts nested output schema types are preserved.
          - Validation: `cargo test -q --test duckdb_differential` passed with `28 passed, 1 ignored`; full `cargo check -q && cargo test -q` passed.
        - Added the first seeded randomized DuckDB differential smoke test:
          - Seed is fixed at `0xD0DA_2026_0001` and currently generates `96` deterministic cases over the small `facts` and `types` fixtures.
          - The grammar mixes single-table projection expressions, NULL/boolean predicates, decimal/date/timestamp/string filters, grouped aggregates, `IN` subqueries, scalar subqueries, and alias-qualified `EXISTS` predicates. It intentionally stays inside the SQL surface already expected to work, so failures are actionable regressions rather than unsupported random SQL.
          - First run exposed an ambiguity trap: an unqualified correlated-looking `EXISTS` predicate can be bound differently by the two engines. The generator was tightened to always alias the outer table and qualify the outer key in generated `EXISTS` cases.
          - Validation: randomized smoke passed; `cargo test -q --test duckdb_differential` passed with `29 passed, 1 ignored`; full `cargo check -q && cargo test -q` passed.
        - Expanded the correctness safety net across the next five gaps:
          - Added a seeded randomized join differential smoke (`0xD0DA_2026_0002`, `72` cases) covering single-key joins, multi-key joins, NULL join keys, `INNER`/`LEFT`/`FULL OUTER` shapes, join filters, and grouped join aggregates. The comparison uses unordered row multiset equality so it can test SQL result equivalence without relying on accidental output order.
          - Added a negative/error semantics matrix for unknown projection columns, scalar subqueries returning multiple rows, and ambiguous unqualified join columns. These are asserted as "both engines fail" rather than comparing exact error text.
          - Extended the type differential matrix for decimal scale comparisons, decimal NULL/inequality logic, timestamp/date filtering, date grouping, and boolean `CASE`. Timestamp display itself still differs (`T` vs space formatting), so timestamp filter cases compare stable IDs rather than raw display text.
          - Added Parquet COPY interoperability coverage across `snappy`, `uncompressed`, and `zstd`, with dictionary and row-group/write-batch/page-row-count options. Dodam-written Parquet is read back by both Dodam and DuckDB, and a DuckDB-written ZSTD Parquet file is read by Dodam.
          - Added an explicit unordered-result comparison helper and test for `GROUP BY` without `ORDER BY`, documenting the result comparison policy for randomized/differential cases.
          - COPY Parquet compression parsing now accepts `ZSTD` via Arrow parquet's default `ZstdLevel`.
          - Limitation exposed while building the join generator: JOIN projection still does not share the full scalar projection path (`CASE`, `COALESCE`, arithmetic projection over joined rows). The randomized join grammar intentionally avoids those expressions for now; this should be a future SQL compatibility/generalization item.
          - Validation: `cargo test -q --test duckdb_differential` passed with `34 passed, 1 ignored`; full `cargo check -q && cargo test -q` passed.
        - Added the next correctness/robustness layer across JOIN projection, randomized generation, type/cast, nested, COPY, and long-run differential:
          - Generalized JOIN scalar projection parsing so joined rows can use the same scalar projection evaluator for `COALESCE`, arithmetic expressions, and searched `CASE` expressions. The join randomized generator now includes those expressions again.
          - Added a deterministic DuckDB differential case for `LEFT JOIN` scalar projection expressions, compared as an unordered multiset because expression-output alias ordering is still a separate compatibility item.
          - Tightened CAST semantics: invalid string-to-integer/double projection casts now return an error instead of silently producing `NULL`, matching DuckDB's fail-fast behavior. Added valid bool/int/string cast comparison and invalid cast both-error coverage.
          - Added struct field projection support for `attrs.rank` / `attrs.label` over Arrow `StructArray`, plus DuckDB differential coverage. List operations are still not implemented beyond projection/roundtrip.
          - Added an env-gated long randomized differential test: `DODAM_LONG_DIFF=1 cargo test -q --test duckdb_differential duckdb_differential_long_run_seeded_randomized -- --nocapture`. It runs four fixed seeds and 768 generated cases across single-table and join generators.
          - The long run exposed a current unsupported shape: aggregate query with subquery predicate (`GROUP BY ... WHERE id IN (SELECT ...)`). The generator now keeps aggregate random cases inside supported non-subquery predicates; the unsupported shape remains a future SQL compatibility item.
          - Validation: `cargo test -q --test duckdb_differential` passed with `36 passed, 1 ignored`; the env-gated long randomized test passed; full `cargo check -q && cargo test -q` passed.
        - Closed the next six correctness gaps:
          - Expression-output alias `ORDER BY` now works for JOIN scalar projection aliases such as `COALESCE(...) AS dim_name ORDER BY dim_name`. The fix distinguishes real aliases from unresolved identifiers even when the alias target is the alias name itself, and avoids pushing expression-output sort columns into join input projection planning.
          - Aggregate queries with materialized subquery predicates now run through the same aggregate path after rewriting `IN (SELECT ...)` filters, so `GROUP BY ... WHERE id IN (SELECT ...)` is supported and back in the randomized generator.
          - Added list scalar operations for `array_length(list_col)` / `list_length(list_col)` and 1-based `list_col[index]` projection/filter over primitive/string Arrow `ListArray` children, with child NULL semantics matching DuckDB in the differential fixture.
          - Struct/list expressions now work in filters for the covered cases, e.g. `attrs.rank > 1 OR tags[1] = 10`, by collecting the base nested column into scan projection and evaluating the predicate through the scalar expression path.
          - `CAST(Date32 AS VARCHAR)` and `CAST(Timestamp(Millisecond) AS VARCHAR)` now preserve typed scalar information long enough to format DuckDB-compatible `YYYY-MM-DD` and `YYYY-MM-DD HH:MM:SS` strings.
          - Long randomized differential remains env-gated but now exercises aggregate+subquery predicates again: `DODAM_LONG_DIFF=1 cargo test -q --test duckdb_differential duckdb_differential_long_run_seeded_randomized -- --nocapture`.
          - Validation: `cargo test -q --test duckdb_differential` passed with `37 passed, 1 ignored`; the env-gated long randomized test passed; full `cargo check -q && cargo test -q` passed.
        - Expanded the next correctness/robustness layer across decimal, temporal, nested, subquery, and diagnostics:
          - Mixed-scale `Decimal128` comparisons and `+`/`-` scalar expressions now align raw integer scales instead of falling back to `f64`; overflow is surfaced as an error rather than silently producing `NULL`.
          - SQL typed string literals such as `DATE '2024-01-03'` and `TIMESTAMP '2024-01-03 04:05:06.789'` are lowered through the scalar cast evaluator, and timestamp parsing accepts timezone-offset forms for epoch normalization.
          - `array_length` / `list_length` now accepts struct list fields such as `attrs.more_tags`, extending nested/list coverage beyond top-level list columns.
          - Correlated subquery differential coverage now includes a correlated `IN` predicate inside an outer boolean combination, and the row-bound fallback treats unknown outer qualifiers from the new resolver error type as bind-later correlated references.
          - SQL diagnostics now has dedicated `AmbiguousColumn`, `UnknownTableQualifier`, `InvalidCast`, and `TypeMismatch` error variants; existing user-facing ambiguous-column text was preserved for compatibility.
          - Validation: full `cargo test -q` passed.
        - Continued the decimal/nested correctness pass:
          - General `Decimal128 * Decimal128` scalar expressions now preserve decimal output instead of being intercepted by the old generic `f64` product fast path; `Decimal128 / Decimal128` intentionally remains `DOUBLE` to match DuckDB.
          - Single-table `WHERE amount = amount3 OR amount3 > amount` now works for mixed-scale Decimal128 column-to-column predicates by aligning raw decimal scales in the physical filter path.
          - Nested list indexing now supports struct list fields such as `attrs.more_tags[1]`, not only top-level `tags[1]`.
          - Validation: full `cargo fmt && cargo check -q && cargo test -q` passed.
        - Implemented the next 1-4 correctness/generalization pass:
          - Added `CAST(... AS DECIMAL(p,s))` scalar support with DuckDB-compatible scale rounding and precision overflow errors.
          - Extended nested/list scalar projection into JOIN output expressions for alias-qualified struct fields and struct-list operations such as `n.attrs.rank`, `n.attrs.more_tags[1]`, and `array_length(n.attrs.more_tags)`.
          - Added a narrow optimized correlated `IN` rewrite for integer/date pair keys of the shape `outer_value IN (SELECT inner_value WHERE inner_corr = outer_corr AND ...)`; unsupported key types fall back to the correctness-first row-bound path.
          - Routed arithmetic type mismatches through the dedicated `TypeMismatch` error variant instead of generic `UnsupportedSql`.
          - Expanded randomized DuckDB differential generation with correlated pair-key `IN`, mixed-scale decimal column comparisons, Decimal CAST, and Decimal product projection.
          - Added a JOIN expression-filter fallback for nested/list predicates, so alias-qualified predicates such as `n.attrs.more_tags[1] = 9 OR array_length(n.attrs.more_tags) > 1` are evaluated after join materialization with DuckDB differential coverage.
          - Generalized the JOIN expression-filter path further: input scan projection now adds only the base columns referenced by the expression predicate instead of falling back to `Projection::All`, while the joined output still stays wide until the filter is applied.
          - Removed duplicate scalar predicate evaluator logic by routing single-table and JOIN expression predicates through one evaluator with parser-specific scalar binding.
          - Extended JOIN nested/list predicate differential coverage to `COALESCE`/`LIKE`, `CAST`/`IN`/`IS NULL`, and searched `CASE` expression predicates.
          - Added JOIN nested/list error-contract coverage for unknown nested fields, invalid list-index expression types, unknown nested qualifiers, and unsupported nested scalar functions.
          - Tightened JOIN output binding: qualified projected columns now register implicit output aliases, `ORDER BY f.id` can resolve after scalar projection, and output projection/scalar evaluation can resolve qualified names against the materialized batch schema.
          - Reduced JOIN output `Projection::All` for scalar projection/expression-filter cases by pushing only the base columns required by final expressions and expression predicates, while keeping wider output for filters/order/outer join shapes that still need it.
          - Expanded seeded and long randomized JOIN differential generation with facts-to-nested JOIN cases covering nested/list predicates and qualified join-key projections.
          - Reduced remaining inner/semi JOIN output `Projection::All` cases by building a working projection from final output, filter columns, order columns, and expression-filter columns; final output projection is applied only when the working projection carries extra columns.
          - Added `ORDER BY` ordinal support for single-table and JOIN queries, including aggregate ordinal ordering such as `ORDER BY 1`; expression-output ordinal ordering is partially covered when the ordinal resolves to a physical/output-available column.
          - Extended correctness coverage with `count(DISTINCT ...)` aggregate combinations, JOIN aggregate `count(DISTINCT ...)`, and nested LEFT JOIN randomized differential cases.
          - Split `ORDER BY` ordinal metadata out of output aliases so positional ordering no longer participates in output renaming/direct-sink alias checks.
          - Added generic grouped expression materialization for single-table and JOIN aggregates: `GROUP BY lower(...)`, `GROUP BY CASE ...`, alias/ordinal ordering over grouped expressions, and `count(DISTINCT expression)` now share the aggregate input-expression path.
          - Added a first minimal window-function path for `row_number() OVER (ORDER BY ...)` on one-table non-aggregate scans, with DuckDB differential coverage.
          - Extended SQL compatibility with `ORDER BY ... NULLS FIRST/LAST`, `LIMIT ... OFFSET ...`, JOIN `DISTINCT`, `GROUP BY` alias/ordinal references, JOIN qualified wildcard expansion (`alias.*`), and partitioned `row_number`/`rank`/`dense_rank` window functions.
          - Validation: full `cargo fmt && cargo check -q && cargo test -q` passed.
        - Followed up the SQL compatibility pass with a correctness/planner hardening layer:
          - Generalized the one-window projection path into a multi-window projection path for multiple `row_number`/`rank`/`dense_rank` outputs that share the same `OVER` specification. This keeps the implementation safe by using one window sort and appending multiple vector outputs; different `OVER` specifications remain explicitly unsupported.
          - Fixed JOIN `DISTINCT` semantics so non-aggregate joins apply SELECT projection before `DISTINCT`, and apply `ORDER BY`/`LIMIT`/`OFFSET` after `DISTINCT`. The earlier join path could accidentally distinct internal working columns or apply distinct after limit.
          - Added JOIN-level `DISTINCT` validation through the same common `validate_distinct` helper used by single-table SELECT.
          - Added DuckDB differential coverage for multiple window projections with `LIMIT/OFFSET`, JOIN `DISTINCT + ORDER BY + LIMIT/OFFSET`, and error-contract coverage for mixed window specs, `QUALIFY`, and unsupported aggregate window functions.
          - Validation: `cargo test -q --test duckdb_differential` passed with `41 passed`; full `cargo check -q && cargo test -q` passed.
          - Short SF=10 single-process TPC-H regression snapshot after the compatibility changes: median-sum Dodam `~4.269s` vs DuckDB `~5.790s` (`~0.737x`). Slower queries in this run were Q03 `~259.8ms` vs `~250.5ms` (gap `~9.3ms`) and Q04 `~140.5ms` vs `~140.5ms` (effectively parity, gap `~0.013ms`). No broad TPC-H regression from the SQL compatibility hardening was observed.
        - Q03 follow-up after the slower full-run blip:
          - Focused SF=10 Q03 measurements did not reproduce a real slower gap; Q03 sampled around parity/faster (`~245-248ms` Dodam vs `~248-250ms` DuckDB), so the earlier full-run Q03 `+9.3ms` appears to be benchmark noise/interference rather than a stable regression.
          - Applied the existing sparse late-selection API to Q03's lineitem late-materialized selector paths. The selected ratio is about `0.5%`, so null-free vector paths now append selected row offsets instead of pushing one selector state per input row.
          - Retuned `DODAM_Q03_REVENUE_LATE_ROW_GROUP_CHUNK` default from `2` to `8`. Sequential SF=10 sweep showed chunk `8` slightly better (`~243.3ms` in the short sweep; longer confirmation `~244.4ms`) than chunk `2` (`~244.9-246.7ms`). Final default run sampled Dodam `~245.7ms` vs DuckDB `~248.0ms` (`~0.991x`).
          - Full SF=10 single-process recheck after the Q03 sparse-selection/chunk change sampled Dodam `~4.237s` vs DuckDB `~5.785s` (`~0.732x`) with no slower query. Q03 sampled Dodam `~247.3ms` vs DuckDB `~257.0ms` (`~0.962x`), and Q04 sampled `~139.0ms` vs `~143.5ms`.
        - Generalized the Q03/Q04 sparse late-selection pattern one layer deeper:
          - Added `LateSelectionBuilder::push_selected_rows(row_count, predicate)` so sparse late materialization can build selector runs directly from a vectorized predicate loop without allocating a query-local `Vec<usize>` of selected offsets.
          - Rewired Q03 lineitem late selection and Q04 lineitem late selection to use this engine-level primitive. This keeps the TPC-H fast path behavior but moves the reusable part into the execution boundary.
          - Validation: `cargo check -q`, targeted DuckDB differential/sql tests, release build, and full `cargo test -q` passed.
          - Individual SF=10 checks after the change: Q03 Dodam `~243.8ms` vs DuckDB `~251.5ms` (`~0.969x`); Q04 longer confirmation Dodam `~124.4ms` vs DuckDB `~125.0ms` (`~0.995x`).
        - Retuned Q12 defaults: `DODAM_Q12_ROW_GROUP_MAP_CHUNK` from `2` to `4`, and `DODAM_Q12_ORDER_FUSED_ROW_GROUP_CHUNK` from `4` to `1`. Sequential sweeps: lineitem chunk `4` was best in the sampled run (`~129.0ms` vs default area `~129.8-133.5ms`), while order fused chunk `1` was clearly best (`~125.3ms` vs `~132-139ms` for larger chunks). Combined explicit setting sampled Dodam `~123.0ms` vs DuckDB `~125.5ms`.
        - Validation after making these Q12 defaults: `cargo fmt --all --check`, `cargo check -q`, `cargo test -q`, and release build passed. Individual SF=10 Q12 sampled Dodam `~123.8ms` vs DuckDB `~127.5ms`, moving Q12 back to faster in this run.
        - Continued moving TPC-H fast-path behavior into reusable vector/execution rules:
          - Extended the engine-level sparse late-selection primitive `LateSelectionBuilder::push_selected_rows(row_count, predicate)` from Q03/Q04 to Q12 order late materialization and Q19 lineitem late materialization. This keeps sparse selected-row construction at the late-materialization boundary instead of each query manually pushing one selector bit per input row.
          - Added `dictionary_i32_view_match_index(keys, flags, row)` in the vector layer and rewired Q12 dictionary shipmode/order-priority paths to use it. This is a small but useful low-cardinality dictionary rule: query code now asks for a target index, while dictionary key conversion/null-safe flag lookup stays in the vector helper.
          - Added `AdaptiveI64Map::get_cached(dense_slices, key)`, mirroring the existing `AdaptiveI64Set::contains_cached` rule, so hot vector loops can bind the dense/hash lookup strategy once per batch and avoid a per-row enum dispatch. Q12 and Q19 late-selection map probes now use this common adaptive lookup helper.
          - Benchmark note: running Q12 and Q19 comparisons concurrently produced misleading slow numbers because both jobs competed for I/O/cache. TPC-H comparisons should be recorded as individual per-query runs unless the experiment is explicitly about multi-query concurrency.
          - Validation: `cargo fmt --all --check`, `cargo check -q`, targeted DuckDB differential TPC-H-lite, and release build passed.
          - Individual SF=10 checks after rebuilding release: Q12 Dodam `~122.9ms` vs DuckDB `~124.0ms` (`~0.991x`); Q19 Dodam `~230.2ms` vs DuckDB `~260.5ms` (`~0.884x`). No regression was observed in the touched paths.
          - Follow-up after `get_cached`: individual SF=10 Q12 sampled Dodam `~124.4ms` vs DuckDB `~126.0ms` (`~0.987x`); Q19 sampled Dodam `~226.8ms` vs DuckDB `~262.5ms` (`~0.864x`). This is a general adaptive lookup rule with a small positive/no-regression profile in the touched TPC-H paths.
        - Ran the requested 1-5 follow-up pass:
          - Re-ran SF=10 TPC-H as individual per-query comparisons, not one concurrent/mixed benchmark. Median-sum was Dodam `~4.228s` vs DuckDB `~5.598s` (`~0.755x`). Q04 was the only slower query in this snapshot: Dodam `~127.0ms` vs DuckDB `~124.0ms` (`~1.024x`, gap `~3.0ms`). All other queries sampled faster than DuckDB.
          - Added `late_materialization_policy_from_projection_env(...)`, a shared SQL-layer planner policy helper that combines predicate projection width, payload projection width, selected-ratio defaults, selector-run defaults, and env overrides. Q04 candidate late materialization, Q04 lineitem late materialization, and Q19 lineitem late materialization now use the same projection-aware policy constructor instead of hand-written query-local policy setup.
          - Row-group map/fold is already below most SQL fast paths through engine APIs such as `parquet_scan_fold_chunks`, `parquet_scan_accumulate_chunks`, and `parquet_row_group_map_*_view`. The next structural step is not another Q-specific wrapper, but lowering this into a first-class physical operator (`Scan -> LocalFold -> FinalMerge`) so generic aggregate/join planning can select it declaratively.
          - Remaining `Projection::All` work should focus on subquery/CTE/JOIN fallback boundaries. JOIN scalar/expression filters are mostly narrowed now, but CTE/subquery materialization and some distinct/order/outer-join safety paths still force wide batches.
          - RecordBatch materialization is still the main structural gap versus a true fused vector/page pipeline. The practical next step is to keep raw `BatchView`/typed vectors through generic physical operators before attempting deeper Parquet page decoder fusion.
          - Q04 profile with `DODAM_TPCH_PROFILE=1` showed the remaining small gap is split across orders candidate scan (`~57ms` in the profiled run) and lineitem late probe (`~92ms`), not a single obvious typed-loop hotspot. The profile run itself adds noise; individual non-profile Q04 after the policy helper sampled Dodam `~126.8ms` vs DuckDB `~122.0ms`.
        - Followed up on Q04 as the only slower SF=10 query:
          - Re-swept Q04 candidate row-group chunk and lineitem late row-group chunk as individual runs. The existing default area (`candidate chunk=2`, `lineitem late chunk=2`) remained the best/near-best stable setting; larger candidate chunks (`8`, `16`) regressed clearly. This was kept as a failed retune rather than changing defaults.
          - Rechecked the candidate late-materialized path after sparse selector cleanup; it is still much slower than the dictionary row-group-map default (`~140.9ms` vs default area `~125ms`), because `o_orderpriority` is cheap and the second pass costs more than it saves. Keep candidate late materialization as fallback/experiment only.
          - Moved Q04 candidate row-group-map label lookup into the local partial state (`Q04CandidatePartial::label_indices`) instead of rebuilding a tiny `HashMap<label,u8>` for every batch. This is a general local-fold state rule: keep low-cardinality dictionary/string label caches inside the worker-local fold state.
          - Added a shared dense atomic marker presence helper and rewired Q04 lineitem late selection to use it, so selection/probe code shares the same dense marker bounds/load rule.
          - Validation: `cargo fmt --all --check`, `cargo check -q`, release build, and full `cargo test -q` passed.
          - Individual SF=10 Q04 after the local-fold label cache and marker helper sampled Dodam `~123.5ms` vs DuckDB `~126.0ms` (`~0.980x`). Treat this as a useful small improvement, but still close enough to require individual-run confirmation in future sweeps.
        - Continued the requested 1-5 generalization pass:
          - Re-ran SF=10 TPC-H as individual per-query comparisons before the structural aggregate change. Median-sum sampled Dodam `~4.313s` vs DuckDB `~5.946s` (`~0.725x`), with no slower query. Q16 sampled Dodam `~72.2ms` vs DuckDB `~76.5ms`; the earlier Q16 slower result was not stable in this full individual run.
          - Lowered aggregate physical-plan representation from a monolithic `AggregateExec` into `ScanExec -> LocalFoldExec -> FinalMergeExec` for both `AggregatePlan::to_plan_node()` and declarative `PhysicalPlanner` aggregate lowering. This is still a planner/explain/operator-boundary representation, not a new independent runtime executor.
          - Generalized the generic fused Parquet aggregate path so row-group workers convert each batch directly into partial `AggregateMetrics` using `collect_partial_aggregate_batch(...)`, then merge partials. This removes the previous `Vec<RecordBatch> -> MemoryExec -> collect_*` redispatch inside each row-group chunk and keeps the local-fold boundary closer to the vector pipeline model.
          - Reduced another JOIN input-projection wide path: joins now build their working projection from final output, filters, order columns, and expression-filter columns even when there is no explicit expression filter, rather than requiring that special case to trigger narrowing.
          - Validation: full `cargo test -q`, release build, and focused SF=10 aggregate-heavy individual comparison passed. Focused Q01/Q03/Q04/Q05/Q09/Q16/Q18 sampled Dodam `~1.577s` vs DuckDB `~2.040s` (`~0.773x`), no slower query.
          - Full SF=10 individual comparison after the partial-aggregate change sampled Dodam `~4.312s` vs DuckDB `~5.867s` (`~0.735x`), with no slower query across all 22 TPC-H queries. This structural aggregate change was mostly neutral-to-small-positive on the current TPC-H fast-path-heavy workload, but it removes a non-vectorized redispatch layer for generic mergeable aggregates.
          - Correctness safety net status: existing seeded randomized DuckDB differential, long-run gated differential, aggregate/null/type/nested/COPY tests already cover the affected generic aggregate semantics; no new failure was observed in full tests.
        - Completed the next 1-5 pass:
          - Made `LocalFoldExec` and `FinalMergeExec` executable declarative physical operators. `LocalFoldExec` is currently an explicit pass-through execution boundary, while `FinalMergeExec` consumes the child stream, runs aggregate collection, and returns typed aggregate `RecordBatch` output. This keeps the public runtime plan shape aligned with the planner and creates the future insertion point for real partial-state streaming.
          - Moved aggregate metrics-to-RecordBatch conversion out of SQL into `execution::aggregate_metrics_to_batches(...)`, so SQL output and physical aggregate execution share one typed conversion path for group keys, Decimal128, timestamps, dates, strings, and count/sum/min/max outputs.
          - Added `lowered_grouped_aggregate_physical_plan_executes`, proving that `plan.to_logical_plan().to_physical_plan()` with `FinalMergeExec(LocalFoldExec(...))` can execute locally and produce the same grouped aggregate rows.
          - Added `scripts/check_correctness.sh` with `quick` and `long` modes. `quick` runs fmt/check/full tests; `long` additionally runs the env-gated long seeded DuckDB differential. Both modes passed in this run.
          - Added `scripts/compare_generic_duckdb.py`, a non-TPC-H generic DuckDB comparison that creates Parquet fixtures with DuckDB and compares Dodam vs DuckDB using Parquet `COPY` output for decimal/date aggregate, expression join aggregate, and nested/list projection/filter workloads.
          - Generic benchmark smoke (`repeats=3`, warmup `1`) exposed a real non-TPC-H gap: median-sum Dodam `~249.4ms` vs DuckDB `~44.7ms` (`~5.58x`). Breakdown: decimal/date grouped aggregate `~20.7ms` vs `~14.5ms` (`~1.42x`), join expression aggregate `~33.9ms` vs `~17.8ms` (`~1.90x`), nested/list projection/filter `~194.8ms` vs `~12.4ms` (`~15.7x`). This confirms the next broad improvement target should be generic nested/list/vector expression execution, not more TPC-H-only tuning.
          - SF=10 TPC-H individual regression check after the runtime operator work remained clean: Dodam `~4.311s` vs DuckDB `~5.881s` (`~0.733x`), no slower query across all 22.
        - Started fixing the generic benchmark gaps:
          - Added vector predicate fast paths for scalar SQL predicates of the form `array_length(list_or_struct_list) <op> integer_literal` and integer column/struct-field `IN (literal, ...)`. This is column-name independent and applies before the generic `EvaluatedScalar` path.
          - The generic nested/list benchmark improved from about `~192ms` to about `~5.5-5.8ms`, beating DuckDB's CLI comparison around `~12.9ms`. This confirms the prior `~15.7x` nested/list gap was mostly scalar predicate materialization overhead, not Parquet nested decode itself.
          - Extended grouped aggregate fast paths with mixed two-key `count(*) + sum(...)` for `(integer, utf8)` and `(utf8, integer)` group keys. This targets generic expression-join aggregates such as `GROUP BY f.bucket, COALESCE(d.class, 'missing')` without matching table or column names.
          - Removed an unnecessary `collect_batches(stream) -> append expressions -> MemoryExec` materialization in the parsed JOIN aggregate path by reusing `append_aggregate_expression_stream`. This keeps aggregate expression materialization streaming after join output.
          - Generic benchmark after these changes (`repeats=7`, warmup `2`) sampled Dodam `~58.5ms` vs DuckDB `~45.4ms` (`~1.29x`). Remaining gaps are now decimal/date aggregate (`~21.2ms` vs `~14.4ms`) and join expression aggregate (`~31.7ms` vs `~18.2ms`); nested/list is no longer the blocker.
          - Validation: `scripts/check_correctness.sh quick` passed. SF=10 TPC-H individual regression check sampled Dodam `~4.305s` vs DuckDB `~5.854s` (`~0.735x`), no slower query across all 22.
        - Continued generic expression/join aggregate cleanup:
          - Reworked the mixed two-key `count(*) + sum(...)` aggregate index for `(integer, utf8)` and `(utf8, integer)` keys so string group keys are borrowed during lookup and cloned only when a new group is inserted. This is a general grouped-aggregate rule, but the measured win alone was small: generic median-sum moved only from about `~67.3ms` to `~66.8ms`, and `join_expression_projection` stayed around `~33.0ms`.
          - Added a direct scalar-expression evaluator for two-argument `COALESCE(utf8_column, 'literal')` and `COALESCE('literal', utf8_column)`. This avoids materializing the literal side for every row and keeps column resolution aligned with the existing output-batch binding rules.
          - Generic benchmark after the COALESCE path (`repeats=9`, warmup `3`) improved to Dodam `~57.2ms` vs DuckDB `~51.4ms` (`~1.11x`). The main win was `join_expression_projection`, improving from about `~33.1ms` to `~23.2ms`; `nested_projection_filter` stayed faster than DuckDB, and the largest remaining generic gap is `filter_group_decimal_date` (`~28.3ms` vs `~19.5ms`).
          - Validation: `scripts/check_correctness.sh quick` passed. SF=10 TPC-H sequential comparison (`repeats=3`, warmup `1`, DuckDB CLI) sampled Dodam `~4.300s` vs DuckDB `~6.943s` (`~0.619x`) with no slower query across all 22. This change did not regress the TPC-H fast-path-heavy workload.
        - Ran the requested generic 1-5 follow-up:
          - Generalized column/literal `COALESCE` beyond Utf8 to Int32/Int64, Float64, Boolean, Date32, Decimal128, and Utf8. The helper now skips literal-side materialization when the column has no nulls, and only fills literal values for null rows otherwise.
          - Added vector predicate evaluation for primitive/Date32/Decimal128 column-vs-literal comparisons. This is expression-shape and type based, so predicates like `amount >= '10.00'` and `event_date <= '2024-01-20'` can avoid the generic `EvaluatedScalar` comparison path.
          - Added a typed single-key `count(*) + sum(int) + min(decimal) + max(date32)` grouped aggregate fold. On the original generic benchmark, `filter_group_decimal_date` improved from about `~27.9ms` to `~22.5ms`, reducing the original 3-query median-sum from about `~55.7ms` to `~51.0ms` and near DuckDB parity (`~1.01x` in one run).
          - Expanded `scripts/compare_generic_duckdb.py` with `coalesce_type_projection` and `three_key_expression_group` so the benchmark covers typed COALESCE projection and multi-key grouped expression aggregation, not only the earlier three cases.
          - The expanded benchmark exposed a real 3-key grouped-expression gap: initial `three_key_expression_group` sampled about `~94.5ms` vs DuckDB `~19.3ms`. A typed `(integer, date32, utf8) + count/sum` path reduced it to about `~45.2ms`, and then a borrowed nested group index reduced it further to about `~36.4ms`.
          - Latest expanded generic benchmark (`repeats=9`, warmup `3`) sampled Dodam `~98.2ms` vs DuckDB `~85.4ms` (`~1.15x`). Remaining generic gaps are `three_key_expression_group` (`~36.4ms` vs `~19.9ms`), `filter_group_decimal_date` (`~23.9ms` vs `~18.9ms`), and `join_expression_projection` (`~22.6ms` vs `~18.1ms`). `nested_projection_filter` and `coalesce_type_projection` are faster than DuckDB in this harness.
          - Validation: `scripts/check_correctness.sh quick`, release build, and SF=10 TPC-H sequential comparison passed. SF=10 sampled Dodam `~4.271s` vs DuckDB `~6.932s` (`~0.616x`) with no slower query across all 22.
          - Remaining structural work: avoid materializing grouped expressions like `COALESCE(label, 'missing')` as full owned StringArrays before grouping; represent column+fallback-literal as a vector expression/view through aggregate grouping so the string payload is copied only for group creation/output.
        - Moved grouped `COALESCE(utf8, literal)` expression handling one layer closer to vector execution:
          - Added an SQL-side expression-view aggregate bridge for `GROUP BY integer, date32, COALESCE(utf8_column, 'literal')` with `count(*) + sum(integer)`. This skips appending a full owned StringArray for the grouped expression and probes the original Utf8 column plus fallback literal directly during grouping.
          - Rewired the main aggregate path to use a shared `collect_aggregates_with_optional_expression_views(...)` helper before falling back to `append_aggregate_expression_stream(...)`.
          - Added the same expression-view shape for two-key `GROUP BY integer, COALESCE(utf8_column, 'literal')` joins. In the current generic join benchmark this did not materially improve `join_expression_projection`, which points to join/output pipeline cost rather than expression materialization alone.
          - Expanded generic benchmark effect: `three_key_expression_group` improved from the previous `~36.4ms` to `~27.5-28.2ms`, and the expanded 5-query median-sum improved from about `~98.2ms` to `~90.4-91.0ms` (`~1.06-1.08x` vs DuckDB in the sampled runs).
          - Validation: `scripts/check_correctness.sh quick`, release build, and SF=10 TPC-H sequential comparison passed. SF=10 sampled Dodam `~4.261s` vs DuckDB `~6.930s` (`~0.615x`) with no slower query across all 22.
          - Remaining generic gaps after this pass: `three_key_expression_group` is still slower by about `~8ms`, `join_expression_projection` by about `~6ms`, and `filter_group_decimal_date` by about `~4-5ms`. The next structural target should be making expression-view aggregate descriptors part of execution/planner instead of SQL-local bridge code, then reducing join aggregate output pipeline overhead.
        - Followed up on the requested 1-5 package:
          - Removed the remaining join aggregate append/fold path that bypassed expression-view grouping. Both main single-table aggregate and join aggregate paths now route through `collect_aggregates_with_optional_expression_views(...)` before falling back to append-and-fold.
          - Added opt-in generic aggregate timing with `DODAM_GENERIC_PROFILE=1`, scoped to global append/fold, grouped append/fold, and grouped expression-view aggregate paths. This is intentionally off by default and is meant for focused local diagnosis, not normal benchmark output.
          - Added DuckDB differential correctness coverage for grouped `COALESCE` expression aggregates, including the 3-key grouped expression shape and 2-key join grouped expression shape.
          - Rebuilt release and reran the expanded generic benchmark. Latest sample: Dodam `~85.6ms` vs DuckDB `~84.2ms` (`~1.02x`). `join_expression_projection` improved to near parity (`~17.9ms` vs `~17.7ms`) after the join path started using the expression-view helper. `three_key_expression_group` remains the largest gap (`~28.2ms` vs `~19.5ms`), followed by `filter_group_decimal_date` (`~24.0ms` vs `~19.2ms`).
          - SF=10 TPC-H sequential comparison after the changes sampled Dodam `~4.248s` vs DuckDB `~6.905s` (`~0.615x`) with no slower query across all 22.
          - Note on the planner/execution cleanup: this pass centralized the SQL aggregate call sites and added descriptor-like expression-view routing, but the concrete `Coalesce*` reader/group implementations still live in `src/sql.rs`. The next cleanup should move those concrete descriptor/executor types into `execution::aggregate` so SQL only converts parsed expressions into execution descriptors.
        - Moved grouped `COALESCE(utf8, literal)` expression-view aggregate execution out of SQL:
          - Added `execution::aggregate::GroupKeyExpr` and `collect_grouped_aggregates_with_key_exprs(...)`, so SQL now only recognizes parsed group-key expression shapes and passes execution descriptors to the aggregate layer.
          - Moved the concrete `COALESCE` key reader/group implementation from `src/sql.rs` into `src/execution/aggregate.rs`. This keeps the previously useful TPC-H/generic fast path, but makes it an execution aggregate rule rather than SQL-local handwritten grouping code.
          - Tried a small adaptive `Option<K>` group index for the leading grouped-expression keys, but did not keep it. The low-cardinality vector index was a plausible general tiny-cardinality rule, but the expanded generic benchmark was neutral-to-slightly-negative (`~87.6ms` vs DuckDB `~85.8ms`, with `three_key_expression_group` worse at `~30.6ms` vs `~20.1ms`).
          - Final expanded generic benchmark after dropping that failed adaptive-index experiment sampled Dodam `~86.2ms` vs DuckDB `~84.9ms` (`~1.02x`). `join_expression_projection` stayed close to parity (`~19.1ms` vs `~17.9ms`), `three_key_expression_group` remained the main gap (`~28.3ms` vs `~19.7ms`), and `filter_group_decimal_date` remained about `~4.9ms` slower.
          - Validation: `scripts/check_correctness.sh quick` passed. SF=10 TPC-H sequential comparison sampled Dodam `~4.281s` vs DuckDB `~6.982s` (`~0.613x`) with no slower query across all 22.
          - Remaining generic work: the expression-view aggregate boundary is now in execution, but generic 3-key grouping still spends too much time in per-row grouping/hash structure work. Next likely improvements are batch-level tiny-cardinality grouping, stronger vectorized typed aggregation for small group domains, and reducing `RecordBatch` dispatch around generic aggregates.
        - Applied the requested 1-4 generic aggregate follow-up:
          - Added a general dense leading-key rule for grouped `COALESCE` expression aggregates when the first two leading keys are non-null numeric/date values with a small observed domain. This is column-name independent and falls back to the old hash path for nulls or out-of-range later batches.
          - This improved `three_key_expression_group` from the prior area around `~28.3ms` to a best sampled `~24.9ms`; the final all-changes sample was `~25.9ms` vs DuckDB `~19.7ms`. The remaining gap is now smaller but still dominated by row-level grouped-expression aggregation.
          - Reworked the single-key `count(*) + sum(int) + min(decimal) + max(date32)` path so Int32/Int64/UInt64 keys are downcast once per batch and processed in typed loops instead of re-dispatching through `ArrayRef` for every row. `filter_group_decimal_date` improved from the prior area around `~23.5ms` to a best sampled `~21.3ms`; final all-changes sample was `~22.5ms` vs DuckDB `~19.0ms`.
          - Added bound column-index plans for the grouped `COALESCE` expression aggregate and the single-key decimal/date aggregate, reducing per-batch schema/name lookup overhead. This was a small structural cleanup rather than a large standalone win.
          - Generalized the group-key expression descriptor from `CoalesceUtf8Literal` to `CoalesceLiteral { column, fallback: GroupKeyLiteral }`. The current executor still handles the `Utf8` literal case, but SQL no longer has a Utf8-specific descriptor boundary, making Int/Bool/Float literal coalesce group keys the next natural extension.
          - Final expanded generic benchmark after all four changes sampled Dodam `~82.6ms` vs DuckDB `~83.0ms` (`~0.995x`). SF=10 TPC-H regression check sampled Dodam `~4.289s` vs DuckDB `~6.907s` (`~0.621x`) with no slower query across all 22.
          - Validation: `cargo fmt && cargo check -q`, release build, and `scripts/check_correctness.sh quick` passed.
        - Continued generic aggregate tightening:
          - Added a non-null typed update loop for the single-key `count(*) + sum(int) + min(decimal) + max(date32)` aggregate path. When key/sum/min/max columns have no nulls, the loop skips per-row null-aware `Option` construction and uses direct primitive/decimal/date reads; null-containing batches still use the existing safe path.
          - Added a global string interner for grouped `COALESCE` expression aggregates. Repeated third-key strings are now interned once to `u32`, and each dense leading-key slot stores numeric string ids instead of a separate `HashMap<String, group_id>`. This is a general low-cardinality string grouping rule, not tied to column names.
          - Generic benchmark after these changes sampled Dodam `~79.0ms` vs DuckDB `~85.1ms` (`~0.93x`). `three_key_expression_group` improved further to `~23.6ms` vs DuckDB `~19.7ms`; `filter_group_decimal_date` sampled `~21.2ms` vs DuckDB `~19.3ms`.
          - Validation: `scripts/check_correctness.sh quick` passed. SF=10 TPC-H regression check sampled Dodam `~4.266s` vs DuckDB `~6.929s` (`~0.616x`) with no slower query across all 22.
          - Remaining generic gap: `three_key_expression_group` still pays per-row string interning/hash lookup, and `filter_group_decimal_date` still does one group lookup per row. The next larger step is batch-local dictionary/id extraction for string keys and dense numeric key slots for small-domain single-key aggregates.
        - Follow-up experiments that were tried and not kept:
          - Batch-local string id cache for grouped `COALESCE` aggregates: intended to avoid repeated global interner lookups, but the extra batch-local hash table and string cloning made the generic benchmark slightly worse (`~79.7ms` vs the prior `~79.0ms`; `three_key_expression_group` around `~24.0ms` instead of `~23.6ms`). Removed from code.
          - Dense Int32 single-key slots for `count/sum/min(decimal)/max(date32)`: plausible for tiny bucket domains, but the measured `filter_group_decimal_date` did not improve (`~21.5ms` area) and the added variant complicated the already typed loop. Removed from code.
          - Non-null sum fast path for grouped `COALESCE` aggregates: avoiding `Option<i64>` on non-null sum batches was offset by extra branch/method split in the hot row loop; generic benchmark regressed to about `~81.2ms`. Removed from code.
          - Current retained best after reverting those failed attempts remains the previous string-interner commit: generic `~79.0ms` vs DuckDB `~85.1ms`, with remaining gaps in `three_key_expression_group` and `filter_group_decimal_date`.
        - Added a retained aggregate-order cleanup:
          - Aggregate group results are already emitted in group-key order by the aggregate executor. SQL now detects `ORDER BY` clauses that are an ascending prefix of `GROUP BY` and, when runtime group keys in that prefix contain no NULLs or explicitly request `NULLS FIRST`, skips the redundant output sort and only applies `LIMIT/OFFSET`.
          - This is a general execution property rule, not query-name specific. It is intentionally conservative for default `NULLS LAST` ordering because the aggregate comparator sorts `None` before `Some`; in that case it skips only when the relevant group keys have no NULL values.
          - Generic benchmark after the change sampled Dodam `~79.6ms` vs DuckDB `~82.2ms` (`~0.97x`). `three_key_expression_group` improved to `~22.8ms` vs DuckDB `~19.0ms`; the remaining generic gap is mostly aggregate row-loop work, not final output sort.
          - Validation: `scripts/check_correctness.sh quick` passed. SF=10 TPC-H regression check sampled Dodam `~4.288s` vs DuckDB `~6.925s` (`~0.619x`) with no slower query across all 22.
        - Reduced another piece of grouped-expression row-loop overhead:
          - Added a primitive-key direct update path for 3-key grouped `COALESCE` aggregates when the leading numeric/date keys are non-null in the current batch. Instead of constructing `Option<i64>/Option<i32>` keys and then unpacking them again in the dense-leading index, the hot path now probes with `(i64, i32)` directly and falls back to the existing null-safe path for nullable batches and 2-key shapes.
          - Generic benchmark after the change sampled Dodam `~79.1ms` vs DuckDB `~84.7ms` (`~0.93x`). `three_key_expression_group` sampled `~23.3ms` vs DuckDB `~20.1ms`; this is a small retained improvement over the retained string-interner baseline, though benchmark noise overlaps with the prior aggregate-order-skip run.
          - Validation: `scripts/check_correctness.sh quick` passed. SF=10 TPC-H regression check sampled Dodam `~4.253s` vs DuckDB `~6.922s` (`~0.614x`) with no slower query across all 22.
        - Reduced grouped-expression finish cost:
          - Dense-leading 3-key grouped `COALESCE` aggregates now emit groups in key order by walking the dense `(first, second)` slot matrix and sorting only the small third-string ids inside each slot. This avoids building all `GroupAggregateResult`s first and then sorting them with the generic `GroupValue` vector comparator.
          - The optimization is conservative: it is used only for dense 3-key groups with no fallback keys; all other shapes keep the generic sorted finish path.
          - Generic benchmark after the change sampled Dodam `~77.9ms` vs DuckDB `~84.6ms` (`~0.92x`). `three_key_expression_group` sampled `~22.7ms` vs DuckDB `~19.7ms`; the remaining gap is now mostly per-row probe/string-id work.
          - Validation: `scripts/check_correctness.sh quick` passed. SF=10 TPC-H regression check sampled Dodam `~4.256s` vs DuckDB `~6.932s` (`~0.614x`) with no slower query across all 22.
        - Applied the same finish-order idea to single-key decimal/date aggregates:
          - The `count(*) + sum(int) + min(decimal) + max(date32)` fast path now finishes Int32/Int64/UInt64 groups by sorting the existing key-index entries and taking group ids in that order, instead of materializing every `GroupAggregateResult` and sorting via the generic `GroupValue` comparator.
          - Generic benchmark sampled Dodam `~78.4ms` vs DuckDB `~84.2ms` (`~0.93x`). `filter_group_decimal_date` was still in the `~21.4ms` area, so this is a small structural cleanup rather than a large win.
          - Validation: `scripts/check_correctness.sh quick` passed. SF=10 TPC-H regression check sampled Dodam `~4.252s` vs DuckDB `~6.929s` (`~0.614x`) with no slower query across all 22.
        - Added typed partial merge for the same single-key decimal/date aggregate shape:
          - Partial grouped aggregate merge now recognizes `GROUP BY one key` with `count(*) + sum(int) + min(decimal) + max(date32)` and merges partial groups through the same typed group/index state instead of `HashMap<Vec<GroupValue>, Vec<AggregateResult>>`.
          - This avoids vector key allocation/hash and generic aggregate result merging for the generic decimal/date workload. Generic benchmark sampled Dodam `~78.0ms` vs DuckDB `~85.3ms` (`~0.91x`); `filter_group_decimal_date` improved to `~21.1ms` vs DuckDB `~19.0ms`.
          - Validation: `scripts/check_correctness.sh quick` passed. SF=10 TPC-H regression check sampled Dodam `~4.282s` vs DuckDB `~6.922s` (`~0.619x`) with no slower query across all 22.
        - Followed up on the remaining generic aggregate gaps:
          - Tried replacing the late-materialized aggregate closure's `Vec<RecordBatch> -> MemoryExec -> collect_grouped_aggregates` redispatch with per-batch `collect_partial_aggregate_batch` plus typed partial merge. It was slower (`filter_group_decimal_date` around `~22.6ms`), likely because it produced too many tiny partials and increased merge overhead. Removed from code.
          - Retained a lighter `COALESCE` grouped-expression state cleanup: `CoalesceKeyCountSumGroup` now stores primitive key fields and a third-string id, creating `Vec<GroupValue>` only at finish time. This avoids group-creation key vector allocation while keeping output semantics unchanged.
          - Generic benchmark after the retained cleanup sampled Dodam `~78.4ms` vs DuckDB `~82.7ms` (`~0.95x`). `three_key_expression_group` improved to `~21.8ms` vs DuckDB `~18.9ms`; SF=10 TPC-H sampled Dodam `~4.264s` vs DuckDB `~6.954s` (`~0.613x`) with no slower query.
          - Validation: `scripts/check_correctness.sh quick` passed.
        - Applied the requested 1-4 follow-up around measurement breadth and aggregate generalization:
          - Added `DODAM_AGGREGATE_PROFILE=1` aggregate timing, also enabled by `DODAM_GENERIC_PROFILE=1` and `DODAM_TPCH_PROFILE=1`. It logs aggregate fragments/batches/rows/groups plus total, aggregate, merge, and residual wall time. A direct `nullable_key_group` profile showed fused partial aggregate around `~13.2ms` and merge around `~0.9ms`, so the remaining wall-time gap includes scan/materialization/COPY overhead, not only typed loops.
          - Expanded `scripts/compare_generic_duckdb.py` from 5 to 9 queries with nullable single-key grouping, low-cardinality string grouping, and no-order variants for the 3-key expression aggregate and decimal/date aggregate. This exposed a previously hidden gap: `nullable_key_group` initially sampled Dodam `~46.5ms` vs DuckDB `~14.0ms`.
          - Generalized the single-key `COUNT(*) + SUM(...)` path into partial aggregate execution. `collect_partial_aggregate_batch()` now uses the same typed count/sum grouping instead of generic batch grouping, and partial merge has a dedicated count/sum merge path.
          - Reused the existing `AdaptiveCopyGroupIndex` for Int32/Int64/UInt64 count/sum group keys and finishes copy-key groups in key order. This is a general single-key grouped aggregate rule, not tied to TPC-H or column names.
          - Expanded generic benchmark after the retained change sampled Dodam `~150.8ms` vs DuckDB `~147.9ms` (`~1.02x`) across the 9-query suite. The newly exposed `nullable_key_group` improved from `~46.5ms` to `~23.0ms`, still slower than DuckDB `~14.2ms`. Other remaining generic gaps are `decimal_date_group_no_order`, `three_key_expression_group_no_order`, and `filter_group_decimal_date`, each in the low single-digit millisecond range.
          - Validation: `cargo fmt && cargo check -q`, release build, and `scripts/check_correctness.sh quick` passed. SF=10 TPC-H sequential comparison sampled Dodam `~4.282s` vs DuckDB `~6.937s` (`~0.617x`) with no slower query across all 22.
          - Next likely improvement: the expanded suite now points to remaining RecordBatch/Parquet materialization and output overhead for simple full-scan grouped aggregates. The aggregate loop itself is much smaller than before, so deeper scan/vector fusion or lower-overhead partial aggregation dispatch is the next structural area.
        - Completed the next 1-4 pass:
          - Changed unfiltered fused parquet aggregate from per-RecordBatch partials to one partial per row-group chunk. The old path produced many tiny partial `AggregateMetrics` values and paid finish/merge overhead per batch; the new path keeps chunk batches together and runs one aggregate collector per chunk before final merge.
          - This was the largest retained win in the pass. Expanded generic 9-query `nullable_key_group` improved from the previous `~23.0ms` area to `~13.8ms`, essentially matching DuckDB (`~13.7ms`) in the final sample.
          - Added a bounded dense Int32 group index for small-range integer group keys and reused it in both `COUNT(*) + SUM(...)` and `COUNT(*) + SUM(...) + MIN(decimal) + MAX(date32)` single-key aggregate paths. The index starts as a small linear vector, converts to dense slots only when the observed range is bounded by `DODAM_DENSE_I32_GROUP_INDEX_MAX_SLOTS` (default `65536`), and falls back to HashMap when the range is too wide.
          - Final expanded generic benchmark sampled Dodam `~140.0ms` vs DuckDB `~147.4ms` (`~0.95x`). Remaining slower generic cases are small: `three_key_expression_group` (`~22.0ms` vs `~19.4ms`), `three_key_expression_group_no_order` (`~21.4ms` vs `~19.2ms`), `decimal_date_group_no_order` (`~16.7ms` vs `~14.7ms`), and `join_expression_projection` (`~19.0ms` vs `~17.7ms`).
          - Tried a filtered fused aggregate path that evaluated the filter inside `parquet_row_group_map` and then aggregated chunk batches. This was not kept: it regressed `string_low_cardinality_group` from `~11ms` to `~23ms` because it bypassed the normal ScanExec pruning/pushdown path and read too much data before filtering. Conclusion: filtered aggregate fusion must preserve row-group pruning and filter pushdown; a simple batch-level fused filter is structurally wrong for selective predicates.
          - Validation: `cargo fmt && cargo check -q`, release build, and `scripts/check_correctness.sh quick` passed. SF=10 TPC-H sequential comparison sampled Dodam `~4.269s` vs DuckDB `~6.887s` (`~0.620x`) with no slower query across all 22.
        - Continued the grouped-expression aggregate cleanup:
          - Replaced the per-leading-slot `u32 -> group_id` index for interned string keys with a bounded dense u32 index. It starts linear, converts to dense slots for low-cardinality interned ids, and falls back to HashMap when `DODAM_DENSE_U32_GROUP_INDEX_MAX_SLOTS` would be exceeded.
          - This is a general rule for interned string/group ids, not tied to a query or column name. It directly targets the remaining `GROUP BY int/date/coalesce(utf8,literal)` row-loop cost where the third key is already interned to dense ids.
          - Expanded generic benchmark samples after the change were noise-sensitive but improved overall: best sample Dodam `~137.8ms` vs DuckDB `~147.1ms` (`~0.94x`), rerun Dodam `~139.5ms` vs DuckDB `~144.9ms` (`~0.96x`). `three_key_expression_group` improved to the `~20.5-20.9ms` area from the prior `~22.0ms` sample.
          - Validation: `scripts/check_correctness.sh quick` passed. SF=10 TPC-H sequential comparison sampled Dodam `~4.246s` vs DuckDB `~6.918s` (`~0.614x`) with no slower query across all 22.
        - Tightened JOIN aggregate output projection:
          - Removed the conservative `Projection::All` early return for JOIN aggregates with grouped scalar expressions. `aggregate_join_output_projection()` already adds columns referenced by aggregate expressions, so JOIN aggregate output can now stay on the same minimal projection rule as other aggregate joins instead of widening just because a grouped expression exists.
          - This is a general planner rule, not query-specific. It reduces unnecessary join output materialization for queries like `GROUP BY f.bucket, coalesce(d.class, 'missing')` while preserving expression column requirements.
          - Tried a non-null sum fast path inside the 3-key grouped `COALESCE` aggregate loop. It did not improve the generic benchmark enough to justify extra branching/code complexity, so it was removed.
          - Generic 9-query sample after the retained projection cleanup sampled Dodam `~137.9ms` vs DuckDB `~147.4ms` (`~0.94x`). `join_expression_projection` remains noise-sensitive around the `~19ms` area, but no regression was observed in correctness or TPC-H.
          - Validation: `cargo fmt && cargo check -q`, release build, and `scripts/check_correctness.sh quick` passed. SF=10 TPC-H sequential comparison sampled Dodam `~4.290s` vs DuckDB `~6.927s` (`~0.619x`) with no slower query across all 22.
        - Added a direct primitive/vector fused aggregate path for the generic `one Int32 group key + count(*) + sum(Int64) + min(Decimal128 backed by INT64) + max(Date32)` shape with conjunctive decimal/date filters:
          - The planner recognizes the aggregate/filter shape from column names and schema, preserves existing row-group pruning through `PredicateSet`/`plan_parquet_scan_tasks`, and falls back to the normal path when the physical/logical types, nullability behavior, or predicates are unsupported.
          - The first sequential direct-primitive attempt was not kept as a speed win: it avoided Arrow `RecordBatch` materialization but lost the normal parallel Parquet scan benefit, so `filter_group_decimal_date` regressed to about `~24.4ms`.
          - The retained version scans pruned row-group chunks in parallel, feeds raw `BatchView` primitive vectors directly into a typed partial aggregate state, then merges partial states. This removes `RecordBatch -> MemoryExec -> aggregate` redispatch for this common fixed-width aggregate shape.
          - Expanded generic benchmark after the retained parallel path sampled Dodam `~119.7ms` vs DuckDB `~142.8ms` (`~0.84x`). `filter_group_decimal_date` improved to `~8.7ms` vs DuckDB `~15.1ms`, and `decimal_date_group_no_order` improved to `~8.9ms` vs DuckDB `~14.2ms`.
          - This is a meaningful vector-pipeline step but still not full DuckDB-style page/operator fusion: it uses parquet-rs `ColumnReader::read_records` as the page decoder boundary. The next deeper step would be a reusable direct primitive scan operator/partial aggregate interface instead of a shape-local engine helper, then lower-level page/selection-aware payload decode.
          - Validation: `cargo fmt && cargo check -q`, `cargo test -q`, release build, `scripts/check_correctness.sh quick`, and `scripts/compare_generic_duckdb.py --repeats 9 --warmup 3` passed.
